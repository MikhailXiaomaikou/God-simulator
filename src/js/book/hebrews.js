/* ─────────────────────────────────────────────────────────────
 * book/hebrews.js —— 希伯来书 · 信心（希伯来书 1 — 13）
 *
 * 一封没有具名的信，讲的是一个比影子更真的世界。这一幕里，地上的事物是「影像」，天上的是「本物」：
 *   夜里，近处的山岭与远处的丘陵上，一盏一盏的小灯此起彼落——「神既在古时藉着众先知多次多方地晓谕列祖」（开篇）。
 *   「你是我的儿子，我今日生你」——众灯自四处升起，汇成一点；天开了一道光，那一点光顺着光降下，儿子站在地上，
 *     他是神荣耀所发的光辉；天亮了；两位天使在他两旁下拜（1）。
 *   「看哪，我与神所给我的儿女」——一群人坐在冷灰的影子里（一生因怕死而为奴仆）；他走到他们中间，
 *     跪下，他的光低到几乎熄灭（为人人尝了死味），又重新亮起；影子退散，众人站起来，围着他（2）。
 *   「你们今日若听他的话，就不可硬着心」——风吹过，地一时成了旷野，中丘上是四十年间苍白的帐棚；
 *     「今日」：地又青了，一层安息的金光落在地上，众人坐下安歇；神的道如一道光的剑掠过全地（3—4）。
 *   「你是照着麦基洗德的等次永远为祭司」——他举手祝福，升上高天，一朵光明的云把他接去；
 *     云升到天上，天上的圣所显现：真帐幕浮在云上，幔子后是至大者的荣光；施恩的宝座的光斜斜地照下来（4—5）。
 *   「主起了誓，决不后悔」——风浪里一只小船漂来；一条光的锚链自船上升起，直通入天上的幔内，
 *     锚钩住幔子；风浪平息，光一粒一粒自天上顺着链子下来（替他们祈求）（6—7）。
 *   「都要照着在山上指示你的样式」——天上的圣所放下一条条光的线，地上照着样式立起帐幕：院子、铜坛、
 *     圣所里的灯台与桌子、蓝紫朱红的幔子、幔子后的至圣所；祭司进头一层帐幕，大祭司独自进到幔子后面（8—9）。
 *   「神啊，我来了，为要照你的旨意行」——一道光自天上的至圣所降到地上的至圣所；坛上的火熄了；
 *     幔子从上到下裂开，荣光涌出，一条又新又活的路从至圣所一直铺到众人脚前，一道光的阶梯通到天上；
 *     众人坦然走进院子，跪下（10）。
 *   「信就是所望之事的实底」——天黑了，天上的圣所隐去（未见之事），夜空里一幅一幅光的画：
 *     亚伯的坛与直上的烟、以诺行走而被接去、挪亚的方舟与虹（11:1–7）。
 *   「论子孙，我必叫你的子孙多起来」——亚伯拉罕（光的人影）在中丘上往不知道的地方走去，住在帐棚里，
 *     与以撒、雅各一样；满天的星一颗一颗多起来，海边的沙也闪着光（6:13–14，11:8–12）。
 *   「我拉着他们祖宗的手，领他们出埃及」——摩西与那不能看见的主（一根光柱）；海水分开，众人走过干地；
 *     七次围绕耶利哥，城墙倒塌，惟有挂着朱红线绳的那一扇窗仍立着（8:9，11:23–31）。
 *   「为我们信心创始成终的耶稣」——天快亮了，那一幅幅光的画都升入一圈光明的云：许多的见证人如同云彩围着我们；
 *     一个人放下重担，在一条光的路上奔跑，仰望天上的光（11:32，12:1–2）。
 *   「再一次我不单要震动地，还要震动天」——右边升起一座黑暗的山：火焰、密云、暴风、角声；又沉下去；
 *     天上的耶路撒冷显现，千万的天使如星；天地震动，人手所造的帐幕化作光尘散去，不被震动的常存；
 *     我们的神乃是烈火（12）。
 *   「我总不撇下你，也不丢弃你」——黄昏，众人摆上桌子，接待一位客旅（不知不觉就接待了天使）；
 *     天上的光落在他们身上；夜过去，天又亮了——昨日、今日、一直到永远，天上的光不变；愿恩惠常与你们众人同在（13）。
 *
 * 父不显为人形：只有光、云与旁白的声音。子在地上时是无面目的人（GS.cast.LOOK.jesus，光比平常亮些）；
 * 升天之后，只以天上圣所里的光显出——坐在至大者的右边。圣灵是灵自己的光与风。
 * 不画血，不画伤：「尝了死味」只是他的光低下去；「藉着死败坏那掌死权的」只是影子退散。
 *
 * 画面的方位（桌面）：左 = 海（经文在海面上）；近地 —— 众人（0.47–0.62），帐幕与院子（0.8）；
 *   天上的圣所悬在帐幕的正上方（0.8, 0.16）；夜里的光画在天的中间（0.36–0.64）。
 * 竖屏的手机：位置经 PX 收拢到 0.4–0.9；天上的圣所与光画都在经文之下的天空里。
 * 一切位置都以画面的比例记下；一切情节都可瞬间重演（恢复存档 / 提前言说）。
 * ───────────────────────────────────────────────────────────── */
(function (GS) {
  'use strict';
  const U = GS.util, W = GS.W;
  const fx = () => GS.fx, cast = () => GS.cast, T = GS.book.timeline, au = () => GS.audio;
  const { clamp, lerp, TAU, smoothstep } = U;
  const ACT = 'hebrews';
  const isCur = () => GS.book.current(ACT);
  const LV = W.lv;

  // ── 本幕的程度（缓动的量；恢复存档时由 W.snapAll 对齐）──────────
  const LEVELS = {
    hbLamps: ['exp', 0.9],      // 众先知的灯（多次多方）
    hbGather: ['lin', 0.34],    // 众灯升起，汇成一点
    hbDescend: ['lin', 0.8],    // 那一点光顺着天光降下
    hbBeam: ['exp', 0.9],       // 天开了，父的光照下来
    hbVoice: ['exp', 1.2],      // 父的声音：天光更亮
    hbRadiance: ['exp', 0.7],   // 神荣耀所发的光辉
    hbOil: ['exp', 0.6],        // 众人身旁的一盏油灯（夜里）
    hbShadow: ['exp', 0.45],    // 怕死的冷影
    hbWild: ['exp', 0.5],       // 旷野：四十年间苍白的帐棚
    hbRest: ['exp', 0.4],       // 安息的金光
    hbCloud: ['exp', 0.8],      // 接他的光明的云
    hbCloudUp: ['lin', 0.16],   // 云升到天上的圣所
    hbHeaven: ['exp', 0.4],     // 天上的圣所（真帐幕）
    hbThrone: ['exp', 0.5],     // 施恩的宝座的光
    hbHVeil: ['exp', 0.5],      // 天上的幔子打开
    hbBoat: ['exp', 1.2],       // 船
    hbBoatIn: ['lin', 0.22],    // 船漂进来
    hbToss: ['exp', 0.35],      // 风浪中的颠簸
    hbAnchor: ['lin', 0.2],     // 锚链升起，通入幔内
    hbAnchorA: ['exp', 0.5],    // 锚链的亮度
    hbPulse: ['exp', 0.6],      // 替他们祈求：光顺着链子下来
    hbPattern: ['exp', 0.7],    // 山上指示的样式：天上放下的光线
    hbTab: ['lin', 0.11],       // 地上的帐幕（立起 / 化去）
    hbLamp: ['exp', 0.6],       // 灯台
    hbAltar: ['exp', 0.5],      // 铜坛上的火
    hbIncense: ['exp', 0.5],    // 至圣所里的香
    hbCome: ['exp', 0.7],       // 「我来了」：光自天上的至圣所降下
    hbTear: ['lin', 0.4],       // 幔子从上到下裂开
    hbGlory: ['exp', 0.5],      // 至圣所的荣光
    hbWay: ['lin', 0.25],       // 又新又活的路
    hbWayA: ['exp', 0.5],       // 那条路的明暗
    hbStair: ['lin', 0.3],      // 通到天上的光的阶梯
    hbDimA: ['exp', 0.6],       // 第一组光画（亚伯、以诺、挪亚）的明暗
    hbDimB: ['exp', 0.6],       // 第二组光画（摩西、耶利哥）的明暗
    hbAbel: ['lin', 0.4],
    hbEnoch: ['lin', 0.16],
    hbNoah: ['lin', 0.35],
    hbStars: ['lin', 0.035],    // 如同天上的星那样众多
    hbTents: ['exp', 0.6],      // 帐棚
    hbSand: ['exp', 0.5],       // 海边的沙
    hbMoses: ['lin', 0.3],
    hbSea: ['lin', 0.2],
    hbJericho: ['lin', 0.2],
    hbRahab: ['exp', 0.8],
    hbWit: ['lin', 0.14],       // 许多的见证人如同云彩围着我们
    hbWitA: ['exp', 0.6],       // 见证人与光画的明暗（震动天地时隐去）
    hbRace: ['exp', 0.7],       // 摆在我们前头的路程
    hbSinai: ['lin', 0.3],      // 那能摸的山
    hbZion: ['exp', 0.35],      // 锡安山，天上的耶路撒冷
    hbAngels: ['exp', 0.4],     // 千万的天使
    hbFire: ['exp', 0.8],       // 我们的神乃是烈火
    hbTable: ['exp', 0.8],      // 接待客旅的桌子
    hbGuest: ['exp', 0.5],      // 客旅身上隐约的光
    hbBless: ['exp', 0.6],      // 天上的光落在众人身上
  };
  for (const k in LEVELS) W.defineLevel(k, LEVELS[k][0], LEVELS[k][1]);
  const MY = Object.keys(LEVELS);

  // ── 位置（桌面的画面宽度比例）；竖屏经 PX 收拢 ─────────────────
  const XL = {
    p1: 0.468, p2: 0.497, p3: 0.527, p4: 0.556, p5: 0.585, p6: 0.612,
    jesus: 0.655, jmid: 0.572, asc: 0.668, oil: 0.535,
    tab: 0.8, table: 0.548, start: 0.462, goal: 0.842, angA: 0.585, angB: 0.735,
    abr0: 0.985, abr1: 0.745, tent0: 0.7, tent1: 0.765, tent2: 0.83,
  };
  const X = Object.assign({}, XL);
  let PORT = false;
  const PX = x => (!PORT || x >= 1.001 ? x : 0.4 + (x - 0.45) * 0.9);
  function layout() {
    PORT = W.w < W.h * 0.9;
    for (const k in XL) X[k] = PX(XL[k]);
  }
  // 众人（弟兄、圣徒、儿女）：各自在近地里的纵深 v 错开，免得挤成一团
  const PEOPLE = [
    { id: 'p1', label: '弟兄', sex: 'm', age: 'adult', robe: [118, 98, 80], v: 0.44 },
    { id: 'p2', label: '圣徒', sex: 'f', age: 'adult', robe: [152, 110, 98], accent: [226, 214, 194], v: 0.2 },
    { id: 'p3', label: '弟兄', sex: 'm', age: 'adult', robe: [96, 108, 134], v: 0.36 },
    { id: 'p4', label: '弟兄', sex: 'm', age: 'elder', robe: [128, 116, 98], v: 0.14, beard: true },
    { id: 'p5', label: '圣徒', sex: 'f', age: 'adult', robe: [116, 126, 104], accent: [220, 206, 180], v: 0.4 },
    { id: 'p6', label: '儿女', sex: 'f', age: 'child', robe: [178, 140, 96], v: 0.28 },
  ];
  const PIDS = PEOPLE.map(p => p.id);

  // ── 本幕的状态（只在 setup / apply / 情节里改动，重演时一样）───────
  let S = fresh();
  function fresh() { return { beamX: XL.jesus, gone: false, bundle: false, runner: false }; }

  // ════════════════════════════════════════════════════════════
  //  小工具
  // ════════════════════════════════════════════════════════════
  // 物件与人物同一尺度（人物模块在七日之后把人画大些：近地 ×1.3；手机上 ×1.55）
  const LK = [1.1, 1.2, 1.3];
  const boost = () => (W.w < 600 ? 1.55 : 1);
  const LS = l => W.layerScale(l) * boost() * (LK[l] || 1);
  const DEP = l => (W.LAYERS[l] ? W.LAYERS[l].depth : 0);
  const gY = (l, xf) => { const x = xf * W.w; const y = GS.land && GS.land.groundY ? GS.land.groundY(l, x) : W.ridgeY(l, x); return isFinite(y) ? y : W.ridgeY(l, x); };
  const fieldH = (l, g) => (l === 2 ? Math.max(0, W.h - g) : Math.max(0, W.waterlineY(l) - g));
  const fY = (xf, v, l) => { const L = l == null ? 2 : l; const g = gY(L, xf); return g + (v || 0) * fieldH(L, g) * 0.8; };
  const css = (rgb, l, a, ex) => W.shadeCSS(rgb, DEP(l), a, ex);
  const nightK = () => clamp(W.night * 1.15 + W.dusk * 0.35, 0, 1);
  const M = () => Math.min(W.w, W.h);
  const SU = () => Math.max(0.6, W.unit);
  const litX = () => (W.night > 0.55 && W.lv.moon > 0.3 ? W.moon.x : W.core.x);
  const hsh = n => { const s = Math.sin(n * 127.1 + 311.7) * 43758.5453; return s - Math.floor(s); };
  const mix = (a, b, t) => [a[0] + (b[0] - a[0]) * t, a[1] + (b[1] - a[1]) * t, a[2] + (b[2] - a[2]) * t];
  // AK：整组淡入淡出时的透明度倍数（见 withAlpha）
  let AK = 1;
  const rgba = (c, a) => 'rgba(' + (c[0] | 0) + ',' + (c[1] | 0) + ',' + (c[2] | 0) + ',' + clamp(a * AK, 0, 1).toFixed(3) + ')';
  const sm = (a, b, x) => smoothstep(a, b, x);
  const ease = t => { t = clamp(t, 0, 1); return t * t * (3 - 2 * t); };
  const inst = b => !!(b && b.instant) || !!W.replaying;
  // 白昼里光要画得浓一些，夜里淡一些也看得见
  const dayBoost = () => 0.55 + 0.65 * W.daylight;

  // 人物（皆经人物模块）
  const C = () => cast();
  const LOOK = () => (GS.cast && GS.cast.LOOK) || {};
  const has = id => { const c = C(); return !!c && (c.has ? c.has(id) : !!(c.get && c.get(id))); };
  function fig(id) { const c = C(); return c && c.get ? c.get(id) : null; }
  function add(id, o) { return C().add(id, Object.assign({ from: W.replaying ? 'none' : 'fade' }, o)); }
  function walk(id, x, o) { if (has(id)) C().walk(id, x, o); }
  function run(id, x, o) { if (has(id)) C().walk(id, x, Object.assign({ run: true }, o || {})); }
  function pose(id, p, o) { if (has(id)) C().pose(id, p, o); }
  function face(id, d) { if (has(id)) C().face(id, d); }
  function glow(id, v) { if (has(id)) C().glow(id, v); }
  function hold(id, p) { const c = C(); if (c.prop && has(id)) U.safe('cast.prop', () => c.prop(id, p || null)); }
  function fly(id, x, y, o) { const c = C(); if (c.fly && has(id)) c.fly(id, x, y, o); }
  function follow(id, other, dx) { const c = C(); if (c.follow && has(id)) c.follow(id, other, dx); }
  function setV(id, v) { const f = fig(id); if (f) f.v = v; }
  // 退场：先放下一切牵挂（跟随、行走、飞），免得淡出之后又被叫回来时带着旧的样子
  function rm(id, now) {
    const f = fig(id);
    if (!f) return;
    f.follow = null; f.tx = null; f.fly = null; f.ny = null;
    C().remove(id, now ? { fade: false } : undefined);
  }
  const each = fn => PIDS.forEach((id, i) => fn(id, i));
  function allPose(p) { each(id => pose(id, p)); }
  function allFace(d) { each(id => face(id, d)); }
  function allGlow(v) { each(id => glow(id, v)); }
  // 众人各回各位（或走到某处附近：dx 为相对原位的偏移）
  function home(o) {
    o = o || {};
    each((id, i) => {
      const q = PEOPLE[i], x = PX(XL[id] + (o.dx || 0) + (o.spread ? (i - 2.5) * o.spread : 0));
      if (o.walk === false) { C().place && C().place(id, x); } else walk(id, x, { speed: o.speed || 0.03, pose: o.pose || 'stand' });
      sink(id, o.v != null ? o.v : q.v);
    });
  }

  function sfx(b, name, o) {
    if (inst(b)) return;
    const a = au();
    if (a && a.sfx) U.safe('audio.sfx', () => a.sfx(name, o || {}));
  }
  function shake(b, k) { if (!inst(b)) W.shake = Math.max(W.shake || 0, k); }
  function flashW(b, k) { if (!inst(b)) W.flash = Math.max(W.flash || 0, k); }
  function lv(name, v, b) { W.set(name, v, inst(b)); }
  function bless(x, y, r) { if (GS.bus) U.safe('hebrews.bless', () => GS.bus.emit('bless', { x, y, r })); }
  // 人物身上的一点（像素）：frac 0 = 脚，1 = 头顶
  function headOf(id, frac) {
    const f = fig(id);
    const k = frac == null ? 1 : frac;
    if (!f) return [W.w * 0.7, W.h * 0.8];
    if (f._vis && isFinite(f._x) && isFinite(f._y)) return [f._x, f._y - (f._h || 30) * k];
    const l = f.layer == null ? 2 : f.layer;
    return [f.nx * W.w, fY(f.nx, f.v || 0, l) - 34 * LS(l) * k];
  }
  // 名字在某处上方、干净的天上以光聚成
  function nameAt(b, str, x, y, o) {
    if (inst(b) || !fx()) return;
    o = o || {};
    const n = Array.from(str).length, u = SU();
    const size = Math.max(0.034 * M(), Math.min(o.size || 40 * u, (W.w * 0.6) / (n * 1.08)));
    const half = (size * 1.08 * (n - 1)) / 2 + size * 0.6;
    const cx = clamp(x, half + 8, W.w - half - 8);
    const cy = clamp(y, (PORT ? W.h * 0.34 : W.h * 0.08) + size * 0.5, W.h * 0.9);
    const src = o.src || (() => [x + (Math.random() - 0.5) * 50 * u, y + (Math.random() - 0.2) * 40 * u]);
    fx().nameStr(str, cx, cy, size, o.rgb || [255, 232, 176], src, { hold: o.hold || 3.4 });
    const a = au();
    if (a && a.nameChime) U.safe('audio.nameChime', () => a.nameChime(str));
  }
  function sparkleAt(b, x, y, n, rgb, spread, pass) {
    if (inst(b) || !fx()) return;
    fx().sparkle(x, y, n || 20, rgb || [255, 236, 190], (spread || 14) * SU(), pass || 'top');
  }
  function ringAt(b, x, y, r, rgb, dur, w) {
    if (inst(b) || !fx()) return;
    fx().ring(x, y, rgb || [255, 240, 204], r, dur || 2.4, w || 1.6);
  }
  function sparkleOn(b, id, n, rgb, frac) { const h = headOf(id, frac == null ? 0.6 : frac); sparkleAt(b, h[0], h[1], n, rgb); }
  function ringOn(b, id, r, rgb, frac) { const h = headOf(id, frac == null ? 0.55 : frac); ringAt(b, h[0], h[1], M() * (r || 0.2), rgb); }
  function avoid(...rs) { W.beastAvoid = rs.map(r => [clamp(Math.min(r[0], r[1]), 0, 1), clamp(Math.max(r[0], r[1]), 0, 1)]); }
  function hint(b, text, sec) { if (inst(b) || !GS.ui || !GS.ui.hint) return; U.safe('hebrews.hint', () => GS.ui.hint(text, sec || 5)); }

  // ── 转瞬的光（不属于世界的状态；重演时不放）───────────────
  const FXL = [];
  function flash(b, o) { if (!inst(b)) FXL.push(Object.assign({ t: 0, dur: 3 }, o)); }

  // ════════════════════════════════════════════════════════════
  //  精灵图（离屏预绘）
  // ════════════════════════════════════════════════════════════
  let SP = null;
  function cnv(w, h) { const c = document.createElement('canvas'); c.width = w; c.height = h; return c; }
  function radial(rgb, a0, mid) {
    const c = cnv(64, 64), g = c.getContext('2d'), gr = g.createRadialGradient(32, 32, 0, 32, 32, 32);
    gr.addColorStop(0, U.rgba(rgb[0], rgb[1], rgb[2], a0));
    gr.addColorStop(mid || 0.35, U.rgba(rgb[0], rgb[1], rgb[2], a0 * 0.32));
    gr.addColorStop(1, U.rgba(rgb[0], rgb[1], rgb[2], 0));
    g.fillStyle = gr; g.fillRect(0, 0, 64, 64);
    return c;
  }
  function sprites() {
    if (SP) return SP;
    try {
      SP = {
        gold: radial([255, 228, 166], 1), white: radial([244, 246, 255], 1), warm: radial([255, 176, 96], 1),
        pale: radial([255, 246, 222], 1, 0.5), soot: radial([30, 34, 46], 0.9, 0.55), cloud: radial([252, 250, 246], 1, 0.62),
        cloudW: radial([255, 238, 206], 1, 0.6), blue: radial([170, 206, 255], 1, 0.45), red: radial([236, 76, 64], 1, 0.4),
        fire: radial([255, 150, 60], 1, 0.4), smoke: radial([70, 62, 66], 0.85, 0.55), dim: radial([150, 160, 180], 0.8, 0.55),
      };
      // 竖直的光柱：中间亮，两边淡；上下两端淡去
      const b = cnv(64, 256), g = b.getContext('2d');
      const hz = g.createLinearGradient(0, 0, 64, 0);
      hz.addColorStop(0, 'rgba(255,244,214,0)'); hz.addColorStop(0.5, 'rgba(255,248,230,1)'); hz.addColorStop(1, 'rgba(255,244,214,0)');
      g.fillStyle = hz; g.fillRect(0, 0, 64, 256);
      g.globalCompositeOperation = 'destination-in';
      const vt = g.createLinearGradient(0, 0, 0, 256);
      vt.addColorStop(0, 'rgba(0,0,0,0)'); vt.addColorStop(0.12, 'rgba(0,0,0,0.85)'); vt.addColorStop(0.9, 'rgba(0,0,0,1)'); vt.addColorStop(1, 'rgba(0,0,0,0.2)');
      g.fillStyle = vt; g.fillRect(0, 0, 64, 256);
      SP.column = b;
      // 横的光带（贴着地面的一道光）
      const bd = cnv(4, 64), bg = bd.getContext('2d'), bv = bg.createLinearGradient(0, 0, 0, 64);
      bv.addColorStop(0, 'rgba(255,240,206,0)'); bv.addColorStop(0.45, 'rgba(255,238,200,0.22)'); bv.addColorStop(0.7, 'rgba(255,242,212,0.62)');
      bv.addColorStop(0.82, 'rgba(255,248,226,0.95)'); bv.addColorStop(0.9, 'rgba(255,244,216,0.5)'); bv.addColorStop(1, 'rgba(255,240,206,0)');
      bg.fillStyle = bv; bg.fillRect(0, 0, 4, 64);
      SP.band = bd;
    } catch (e) { SP = null; }
    return SP;
  }
  // 画完即把 globalAlpha 还原为 1（其后以 rgba 填色的笔画不受影响）
  function glowAt(ctx, img, x, y, r, a) {
    a *= AK;
    if (!img || a < 0.004 || r < 0.5 || !isFinite(x) || !isFinite(y)) return;
    ctx.globalAlpha = Math.min(1, a);
    ctx.drawImage(img, x - r, y - r, r * 2, r * 2);
    ctx.globalAlpha = 1;
  }
  function glowE(ctx, img, x, y, rx, ry, a) {
    a *= AK;
    if (!img || a < 0.004 || rx < 0.5 || ry < 0.3 || !isFinite(x) || !isFinite(y)) return;
    ctx.globalAlpha = Math.min(1, a);
    ctx.drawImage(img, x - rx, y - ry, rx * 2, ry * 2);
    ctx.globalAlpha = 1;
  }
  // 离屏的层：参数（sig）不变时不重画；每隔 period 秒重画一次，好让云与光慢慢地动
  // 画面上大片的光（天上的圣所、锡安、满天的星）都经这里：每帧只贴一张图
  const LAYERS = {};
  const qz = v => Math.round(clamp(v, -9, 9) * 50);
  function layer(key, x, y, w, h, sig, period, render) {
    x = Math.floor(x); y = Math.floor(y);
    const iw = Math.max(1, Math.ceil(w)), ih = Math.max(1, Math.ceil(h));
    // 高清屏上按像素比画（最多 2 倍），线才不糊
    const sc = clamp((W.dpr || 1) * ((W.quality || 1) < 0.75 ? 0.75 : 1), 0.75, 2);
    const pw = Math.max(1, Math.round(iw * sc)), ph = Math.max(1, Math.round(ih * sc));
    let L = LAYERS[key];
    if (!L) { L = LAYERS[key] = { c: cnv(pw, ph), sig: null, t: -1e9, x, y, w: iw, h: ih }; }
    const moved = L.x !== x || L.y !== y || L.c.width !== pw || L.c.height !== ph;
    const due = W.t - L.t >= period || W.t < L.t;
    if (moved || L.sig !== sig ? (moved || W.t - L.t >= 0.05 || W.t < L.t || L.sig === null) : due) {
      if (L.c.width !== pw || L.c.height !== ph) { L.c.width = pw; L.c.height = ph; }
      const g = L.c.getContext('2d');
      g.setTransform(1, 0, 0, 1, 0, 0);
      g.globalAlpha = 1; g.globalCompositeOperation = 'source-over';
      g.clearRect(0, 0, pw, ph);
      g.setTransform(pw / iw, 0, 0, ph / ih, -x * pw / iw, -y * ph / ih);
      try { render(g); } catch (err) { /* 画不出就空着 */ }
      L.sig = sig; L.t = W.t; L.x = x; L.y = y; L.w = iw; L.h = ih; L.n = (L.n || 0) + 1;
    }
    return L;
  }
  // 画进离屏层再贴上：bbox = [x, y, w, h]（裁到画面之内）
  function cached(ctx, key, bb, sig, period, mode, fn) {
    const x0 = Math.max(0, bb[0]), y0 = Math.max(0, bb[1]), x1 = Math.min(W.w, bb[0] + bb[2]), y1 = Math.min(W.h, bb[1] + bb[3]);
    if (x1 - x0 < 2 || y1 - y0 < 2) return;
    blit(ctx, layer(key, x0, y0, x1 - x0, y1 - y0, sig + '|' + qz(AK), period, fn), mode);
  }
  function blit(ctx, L, mode) {
    if (!L) return;
    ctx.globalCompositeOperation = mode || 'source-over';
    ctx.globalAlpha = 1;
    ctx.drawImage(L.c, L.x, L.y, L.w, L.h);
    ctx.globalCompositeOperation = 'source-over';
  }
  // 一道光柱：自 (x, y0) 到 (x, y1)，宽 w
  function column(ctx, x, y0, y1, w, a) {
    a *= AK;
    if (!SP || a < 0.004 || w < 0.5 || Math.abs(y1 - y0) < 1) return;
    ctx.globalAlpha = Math.min(1, a);
    ctx.drawImage(SP.column, x - w / 2, Math.min(y0, y1), w, Math.abs(y1 - y0));
    ctx.globalAlpha = 1;
  }
  // 斜的光柱：自 (x0, y0) 到 (x1, y1)
  function beamTo(ctx, x0, y0, x1, y1, w, a) {
    a *= AK;
    if (!SP || a < 0.004) return;
    const L = Math.hypot(x1 - x0, y1 - y0);
    if (L < 1) return;
    ctx.save();
    ctx.translate(x0, y0);
    ctx.rotate(Math.atan2(y1 - y0, x1 - x0) - Math.PI / 2);
    ctx.globalAlpha = Math.min(1, a);
    ctx.drawImage(SP.column, -w / 2, 0, w, L);
    ctx.restore();
    ctx.globalAlpha = 1;
  }
  // 整组淡入淡出：组里各笔的透明度都乘上 k
  function withAlpha(ctx, k, fn) {
    if (k < 0.005) return;
    const prev = AK;
    AK = prev * clamp(k, 0, 1);
    try { fn(); } finally { AK = prev; }
  }

  // ════════════════════════════════════════════════════════════
  //  光的人影（天上的见证人、光画里的人）：无面目，只是一个发光的形
  //  (x, y) 为脚下；h 为身高；dir 面向（1 右 / -1 左）
  // ════════════════════════════════════════════════════════════
  function lightFig(ctx, x, y, h, pose, dir, o) {
    o = o || {};
    const d = dir || 1, lw = Math.max(0.6, h * 0.06);
    ctx.beginPath();
    if (pose === 'kneel' || pose === 'bow') {
      const bow = pose === 'bow' ? 1 : 0;
      const hipX = x - d * h * 0.05, hipY = y - h * 0.26;
      const shX = hipX + d * h * (0.08 + 0.26 * bow), shY = y - h * (0.62 - 0.22 * bow);
      // 跪着的衣袍：自肩到膝到脚跟
      ctx.moveTo(shX - d * h * 0.08, shY);
      ctx.quadraticCurveTo(hipX - d * h * 0.16, hipY - h * 0.05, hipX - d * h * 0.18, y);
      ctx.lineTo(hipX + d * h * 0.3, y);
      ctx.quadraticCurveTo(hipX + d * h * 0.2, hipY, shX + d * h * 0.08, shY + h * 0.02);
      ctx.closePath();
      const hx = shX + d * h * (0.03 + 0.1 * bow), hy = shY - h * (0.09 - 0.03 * bow);
      ctx.moveTo(hx + h * 0.075, hy); ctx.arc(hx, hy, h * 0.075, 0, TAU);
      ctx.fill();
      if (o.arms !== false) {
        ctx.lineWidth = lw; ctx.lineCap = 'round';
        ctx.beginPath(); ctx.moveTo(shX, shY + h * 0.04);
        ctx.lineTo(shX + d * h * (0.14 + 0.1 * bow), shY + h * (0.16 + 0.1 * bow)); ctx.stroke();
      }
      return;
    }
    const walkK = pose === 'walk' ? 1 : 0, ph = o.ph || 0;
    const sw = walkK * Math.sin(ph) * 0.07 * h;
    ctx.moveTo(x - h * 0.08, y - h * 0.78);
    ctx.quadraticCurveTo(x - h * 0.14, y - h * 0.4, x - h * 0.16 - sw, y);
    ctx.lineTo(x + h * 0.16 + sw, y);
    ctx.quadraticCurveTo(x + h * 0.14, y - h * 0.4, x + h * 0.08, y - h * 0.78);
    ctx.closePath();
    const hx = x + d * h * 0.012, hy = y - h * 0.88;
    ctx.moveTo(hx + h * 0.078, hy); ctx.arc(hx, hy, h * 0.078, 0, TAU);
    ctx.fill();
    ctx.lineWidth = lw; ctx.lineCap = 'round';
    ctx.beginPath();
    if (pose === 'raise') {
      ctx.moveTo(x - h * 0.07, y - h * 0.74); ctx.lineTo(x - h * 0.2, y - h * 1.02);
      ctx.moveTo(x + h * 0.07, y - h * 0.74); ctx.lineTo(x + h * 0.2, y - h * 1.02);
    } else if (pose === 'staff') {
      ctx.moveTo(x + d * h * 0.07, y - h * 0.74); ctx.lineTo(x + d * h * 0.24, y - h * 0.98);
      ctx.moveTo(x + d * h * 0.26, y + h * 0.02); ctx.lineTo(x + d * h * 0.22, y - h * 1.12);
    } else if (pose === 'walk' && o.staff) {
      ctx.moveTo(x + d * h * 0.07, y - h * 0.72); ctx.lineTo(x + d * h * 0.2, y - h * 0.55);
      ctx.moveTo(x + d * h * 0.22, y + h * 0.01); ctx.lineTo(x + d * h * 0.19, y - h * 0.95);
    } else {
      ctx.moveTo(x - h * 0.08, y - h * 0.74); ctx.lineTo(x - h * 0.12, y - h * 0.42);
      ctx.moveTo(x + h * 0.08, y - h * 0.74); ctx.lineTo(x + h * 0.12, y - h * 0.42);
    }
    ctx.stroke();
  }
  // 小小的人影（船上的人）：剪影，迎光一侧一道暖边
  function smallPerson(ctx, x, y, h, rgb, dir, l) {
    ctx.fillStyle = css(rgb, l == null ? 1 : l);
    ctx.beginPath();
    ctx.moveTo(x - h * 0.09, y - h * 0.76);
    ctx.quadraticCurveTo(x - h * 0.15, y - h * 0.35, x - h * 0.17, y);
    ctx.lineTo(x + h * 0.17, y);
    ctx.quadraticCurveTo(x + h * 0.15, y - h * 0.35, x + h * 0.09, y - h * 0.76);
    ctx.closePath();
    ctx.moveTo(x + h * 0.08, y - h * 0.87); ctx.arc(x, y - h * 0.87, h * 0.08, 0, TAU);
    ctx.fill();
  }

  // ════════════════════════════════════════════════════════════
  //  线描（光画）：pts 为单位坐标；f 为画出的比例（0..1）
  // ════════════════════════════════════════════════════════════
  function pathLen(pts) { let L = 0; for (let i = 1; i < pts.length; i++) L += Math.hypot(pts[i][0] - pts[i - 1][0], pts[i][1] - pts[i - 1][1]); return L; }
  function partial(ctx, pts, f, ox, oy, R) {
    if (f <= 0 || pts.length < 2) return;
    const L = pathLen(pts) * Math.min(1, f);
    ctx.moveTo(ox + pts[0][0] * R, oy + pts[0][1] * R);
    let acc = 0;
    for (let i = 1; i < pts.length; i++) {
      const a = pts[i - 1], b = pts[i], d = Math.hypot(b[0] - a[0], b[1] - a[1]);
      if (acc + d >= L) { const t = d > 0 ? (L - acc) / d : 0; ctx.lineTo(ox + (a[0] + (b[0] - a[0]) * t) * R, oy + (a[1] + (b[1] - a[1]) * t) * R); return; }
      ctx.lineTo(ox + b[0] * R, oy + b[1] * R);
      acc += d;
    }
  }
  // 一笔光：宽而淡的一层 + 细而亮的一层
  function lightStroke(ctx, paths, ox, oy, R, a, rgb, w) {
    if (a < 0.005) return;
    const u = SU(), lw = (w || 1) * Math.max(0.9, 1.25 * u);
    ctx.lineCap = 'round'; ctx.lineJoin = 'round';
    ctx.strokeStyle = rgba(rgb || [255, 226, 170], a * 0.22);
    ctx.lineWidth = lw * 4.2;
    ctx.beginPath();
    for (const p of paths) partial(ctx, p[0], p[1], ox, oy, R);
    ctx.stroke();
    ctx.strokeStyle = rgba(mix(rgb || [255, 226, 170], [255, 252, 240], 0.55), a * 0.9);
    ctx.lineWidth = lw;
    ctx.beginPath();
    for (const p of paths) partial(ctx, p[0], p[1], ox, oy, R);
    ctx.stroke();
  }
  const arcPts = (cx, cy, rx, ry, a0, a1, n) => { const out = []; for (let i = 0; i <= n; i++) { const a = lerp(a0, a1, i / n); out.push([cx + Math.cos(a) * rx, cy + Math.sin(a) * ry]); } return out; };
  const linePts = (x0, y0, x1, y1) => [[x0, y0], [x1, y1]];

  // ════════════════════════════════════════════════════════════
  //  几何
  // ════════════════════════════════════════════════════════════
  // 天上的圣所（真帐幕）：浮在云上，悬在地上帐幕的正上方
  function heaven() {
    const w = PORT ? W.w * 0.44 : Math.min(W.w * 0.2, W.h * 0.34);
    const x = (PORT ? 0.665 : 0.8) * W.w, y = (PORT ? 0.385 : 0.165) * W.h;
    const h = w * 0.36;
    const x0 = x - w / 2, x1 = x + w / 2, xv = x0 + w * 0.64;
    return { x, y, w, h, x0, x1, xv, top: y - h / 2, bot: y + h / 2, mx: (xv + x1) / 2, ax: (xv + x1) / 2, ay: y + h * 0.18 };
  }
  // 地上的帐幕：院子（院帷）、铜坛、洗濯盆；帐幕的前面敞开（看得见圣所里的灯台、桌子、香坛），至圣所前挂着幔子
  function tabG() {
    const s = LS(2), cx = X.tab * W.w;
    const cw = (PORT ? 150 : 214) * s, tl = (PORT ? 100 : 136) * s, th = tl * 0.42;
    const tx0 = cx - tl * 0.36, tx1 = tx0 + tl, tv = tx0 + tl * 0.64;
    const g = x => gY(2, x / W.w) + 1.5 * s;
    const base = Math.max(g(tx0), g(tx1), g((tx0 + tx1) / 2)) - 0.5 * s;
    return { s, cx, cw, tl, th, tx0, tx1, tv, x0: cx - cw / 2, x1: cx + cw / 2, g, base, top: base - th, mx: (tv + tx1) / 2,
      altar: cx - cw * 0.37, laver: cx - cw * 0.25, ch: 20 * s, back: 6 * s };
  }
  // 船（中丘与近地之间的水面上；竖屏时在左边的海上）
  function boatPos() {
    const i = ease(LV.hbBoatIn);
    const bx = PORT ? 0.3 : 0.557, by = PORT ? 0.8 : 0.797;
    const x = (bx - (1 - i) * (PORT ? 0.34 : 0.3)) * W.w;
    const L = (PORT ? 70 : 84) * SU();
    const tz = LV.hbToss;
    const ang = tz * 0.13 * Math.sin(W.t * 1.9) + 0.012 * Math.sin(W.t * 0.8);
    const bob = (tz * 3.2 + 0.6) * SU() * Math.sin(W.t * 2.3 + 1);
    return { x, y: by * W.h + bob, L, ang };
  }
  function bowPt(B) { return [B.x + Math.cos(B.ang) * B.L * 0.44, B.y + Math.sin(B.ang) * B.L * 0.44 - B.L * 0.12]; }
  // 光画的位置（中心、半径）
  const GAL = {
    abel: { d: [0.33, 0.31], p: [0.27, 0.425], slot: 234, lv: 'hbAbel', grp: 'hbDimA', name: '亚伯' },
    enoch: { d: [0.485, 0.19], p: [0.72, 0.535], slot: 249, lv: 'hbEnoch', grp: 'hbDimA', name: '以诺' },
    noah: { d: [0.64, 0.32], p: [0.3, 0.605], slot: 264, lv: 'hbNoah', grp: 'hbDimA', name: '挪亚' },
    moses: { d: [0.36, 0.28], p: [0.28, 0.44], slot: 289, lv: 'hbMoses', grp: 'hbDimB', name: '摩西' },
    jericho: { d: [0.58, 0.3], p: [0.71, 0.56], slot: 304, lv: 'hbJericho', grp: 'hbDimB', name: '喇合' },
  };
  const galR = () => (PORT ? W.w * 0.17 : Math.min(W.w * 0.07, W.h * 0.115));
  // 许多的见证人：一圈光明的云（像看台一样围着地上奔跑的人）
  function witArc() {
    return PORT ? { cx: 0.5 * W.w, cy: 1.06 * W.h, rx: 0.62 * W.w, ry: 0.55 * W.h, a0: 208, a1: 332 }
      : { cx: 0.53 * W.w, cy: 0.95 * W.h, rx: 0.5 * W.w, ry: 0.65 * W.h, a0: 218, a1: 322 };
  }
  function arcAt(A, deg, lift) { const a = deg * Math.PI / 180; return [A.cx + Math.cos(a) * A.rx, A.cy + Math.sin(a) * A.ry - (lift || 0)]; }
  function galPos(key) {
    const g = GAL[key], R = galR();
    const own = PORT ? g.p : g.d;
    const w = ease(LV.hbWit);
    if (w <= 0) return { x: own[0] * W.w, y: own[1] * W.h, R, k: 1 };
    const s = arcAt(witArc(), g.slot, R * 0.2);
    return { x: lerp(own[0] * W.w, s[0], w), y: lerp(own[1] * W.h, s[1], w), R: R * lerp(1, 0.42, w), k: 1 - 0.35 * w };
  }
  // 天上的耶路撒冷（锡安山）
  function zionG() {
    const H = heaven();
    if (PORT) return { x0: 0.08 * W.w, x1: 0.97 * W.w, y: H.bot + H.h * 1.25, h: W.h * 0.03, H };
    return { x0: 0.555 * W.w, x1: 0.995 * W.w, y: H.bot + H.h * 0.95, h: W.h * 0.045, H };
  }
  // 那能摸的山（西奈）：自中丘之后升起
  function sinaiG() {
    const k = ease(LV.hbSinai);
    const px = (PORT ? 0.86 : 0.915) * W.w, wl = W.waterlineY(1);
    const hgt = (PORT ? 0.2 : 0.3) * W.h, half = (PORT ? 0.3 : 0.17) * W.w;
    return { px, base: wl + 4, top: wl - hgt * k, half, k, hgt };
  }

  // ════════════════════════════════════════════════════════════
  //  画：众先知的灯（开篇）与汇成一点的光（1）
  // ════════════════════════════════════════════════════════════
  // [层, 桌面 x, 竖屏 x]
  const LAMPS = [
    [0, 0.62, 0.55], [0, 0.705, 0.64], [0, 0.79, 0.74], [0, 0.875, 0.83], [0, 0.955, 0.93],
    [1, 0.57, 0.52], [1, 0.645, 0.6], [1, 0.72, 0.68], [1, 0.8, 0.78], [1, 0.88, 0.86], [1, 0.955, 0.95],
    [2, 0.74, 0.7], [2, 0.905, 0.87],
  ];
  function lampXY(i) {
    const q = LAMPS[i], xf = PORT ? q[2] : q[1], l = q[0];
    return [xf * W.w, gY(l, xf) - (l === 2 ? 10 : l === 1 ? 5 : 3) * LS(l)];
  }
  function gatherTarget() { return [S.beamX * W.w, (PORT ? 0.46 : 0.3) * W.h]; }
  function lampT(i) { return ease(clamp(LV.hbGather * 1.35 - i * 0.027, 0, 1)); }
  function drawLampsLayer(ctx, l) {
    const k = LV.hbLamps;
    if (k < 0.005 || !SP) return;
    ctx.globalCompositeOperation = 'lighter';
    for (let i = 0; i < LAMPS.length; i++) {
      if (LAMPS[i][0] !== l) continue;
      const [x, y] = lampXY(i), s = LS(l), t = lampT(i);
      // 多次多方：一盏一盏此起彼落
      const ph = Math.sin(W.t * (0.55 + (i % 4) * 0.13) + i * 1.9);
      const on = 0.3 + 0.7 * Math.pow(Math.max(0, ph), 1.5);
      const a = k * (1 - t) * on * (0.4 + 0.6 * nightK());
      glowAt(ctx, SP.warm, x, y, 36 * s, a * 0.6);
      glowAt(ctx, SP.gold, x, y, 6.5 * s, a);
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  function drawGather(ctx) {
    const g = LV.hbGather, d = LV.hbDescend;
    if (g < 0.005 || d >= 0.999 || !SP) return;
    const [tx, ty] = gatherTarget(), u = SU();
    ctx.globalCompositeOperation = 'lighter';
    let merged = 0;
    for (let i = 0; i < LAMPS.length; i++) {
      const t = lampT(i);
      if (t <= 0) continue;
      if (t >= 1) { merged++; continue; }
      const [x0, y0] = lampXY(i);
      const cx = lerp(x0, tx, 0.5), cy = Math.min(y0, ty) - W.h * 0.12;
      const P = q => { const m = 1 - q; return [m * m * x0 + 2 * m * q * cx + q * q * tx, m * m * y0 + 2 * m * q * cy + q * q * ty]; };
      const [x, y] = P(t);
      for (let j = 1; j <= 5; j++) { const [ax, ay] = P(Math.max(0, t - j * 0.035)); glowAt(ctx, SP.gold, ax, ay, (5 - j * 0.6) * u, 0.3 * (1 - j / 6)); }
      glowAt(ctx, SP.warm, x, y, 16 * u, 0.5);
      glowAt(ctx, SP.gold, x, y, 5 * u, 1);
    }
    if (merged > 0) {
      const f = merged / LAMPS.length;
      const y = lerp(ty, gY(2, S.beamX) - 26 * LS(2), ease(d));
      const r = (10 + 16 * f) * u * (1 + 0.06 * Math.sin(W.t * 5));
      glowAt(ctx, SP.gold, tx, y, r * 3.2, 0.55 * f * (1 - d));
      glowAt(ctx, SP.white, tx, y, r, Math.min(1, f * 1.2) * (1 - d * 0.7));
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  // 父的光：天开了一道光，照在子身上
  function drawBeam(ctx) {
    const b = LV.hbBeam, v = LV.hbVoice;
    if (b < 0.005 || !SP) return;
    const x = S.beamX * W.w, g = gY(2, S.beamX) + 4 * LS(2), w = (PORT ? 0.2 : 0.075) * W.w;
    ctx.globalCompositeOperation = 'lighter';
    const a = b * (0.28 + 0.24 * v) * (0.7 + 0.5 * nightK());
    column(ctx, x, -10, g, w * (1 + 0.25 * v), a);
    column(ctx, x, -10, g, w * 0.35, a * 1.2);
    glowE(ctx, SP.pale, x, W.h * 0.02, w * 2.4, W.h * 0.07, b * (0.4 + 0.3 * v));
    glowE(ctx, SP.gold, x, g - 2, w * 0.9, 9 * LS(2), a * 1.4);
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  // 神荣耀所发的光辉：子身后柔和的光与缓缓转动的光芒
  function drawRadiance(ctx) {
    const r = LV.hbRadiance;
    if (r < 0.005 || !SP || !has('jesus')) return;
    const f = fig('jesus');
    if (!f || f.alpha < 0.02) return;
    const h = headOf('jesus', 0.55), s = LS(2), br = 0.94 + 0.06 * Math.sin(W.t * 1.3);
    ctx.globalCompositeOperation = 'lighter';
    const a = r * f.alpha * (0.45 + 0.45 * nightK() + 0.25 * W.daylight);
    glowE(ctx, SP.gold, h[0], h[1], 30 * s * br, 44 * s * br, a * 0.5);
    glowAt(ctx, SP.pale, h[0], h[1] - 8 * s, 70 * s, a * 0.22);
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  // 众人身旁的一盏油灯（夜里）
  function drawOil(ctx) {
    const k = LV.hbOil;
    if (k < 0.005 || !SP) return;
    const s = LS(2), x = X.oil * W.w, y = fY(X.oil, 0.36);
    ctx.fillStyle = css([150, 104, 70], 2);
    ctx.beginPath(); ctx.ellipse(x, y - 1.6 * s, 4.2 * s, 1.8 * s, 0, 0, TAU); ctx.fill();
    ctx.globalCompositeOperation = 'lighter';
    const fl = 0.85 + 0.15 * Math.sin(W.t * 9) * Math.sin(W.t * 5.3);
    glowAt(ctx, SP.warm, x, y - 5 * s, 70 * s * fl, k * (0.2 + 0.4 * nightK()));
    glowAt(ctx, SP.gold, x + 2 * s, y - 5 * s, 3.5 * s * fl, k);
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }

  // ════════════════════════════════════════════════════════════
  //  画：怕死的冷影（2）、旷野与安息（3—4）
  // ════════════════════════════════════════════════════════════
  function drawShadow(ctx) {
    const k = LV.hbShadow;
    if (k < 0.005 || !SP) return;
    const s = LS(2), xa = PX(0.45), xb = PX(0.635), rise = (1 - k) * W.h * 0.22, drift = (1 - k) * W.w * 0.1;
    const mx = (xa + xb) / 2;
    ctx.globalCompositeOperation = 'source-over';
    // 一层冷灰的雾罩着众人（怕死的影子）；退散时升起、向海那边飘去、变淡
    glowE(ctx, SP.soot, mx * W.w - drift, fY(mx, 0.3) - 30 * s - rise, (xb - xa) * W.w * 0.72, 56 * s, k * 0.46);
    for (let i = 0; i < 14; i++) {
      const f = (i + 0.5) / 14, xf = lerp(xa, xb, f);
      const x = xf * W.w + Math.sin(W.t * 0.23 + i * 1.7) * 10 * s - drift * (0.6 + 0.8 * hsh(i));
      const y = fY(xf, 0.3) - (16 + 36 * hsh(i + 5)) * s - rise * (0.5 + hsh(i + 3));
      const r = (19 + 12 * hsh(i + 7)) * s * (1 + 0.6 * (1 - k));
      glowE(ctx, SP.soot, x, y, r * 1.3, r, k * (0.4 + 0.1 * Math.sin(W.t * 0.4 + i)));
    }
    ctx.globalAlpha = 1;
  }
  // 旷野：中丘上四十年间苍白的帐棚
  function drawWild(ctx) {
    const k = LV.hbWild;
    if (k < 0.005) return;
    const s = LS(1);
    for (let i = 0; i < 15; i++) {
      const xf = PX(0.545 + i * 0.03 + (hsh(i) - 0.5) * 0.012);
      if (xf > 0.99) continue;
      const x = xf * W.w, g = gY(1, xf) + 1, w = (15 + 6 * hsh(i + 5)) * s, h = w * 0.62;
      ctx.fillStyle = css([226, 212, 178], 1, k * 0.7 * (0.65 + 0.35 * hsh(i + 9)), 0.1);
      ctx.beginPath(); ctx.moveTo(x - w, g); ctx.lineTo(x - w * 0.1, g - h); ctx.lineTo(x + w * 0.15, g - h * 0.96); ctx.lineTo(x + w, g); ctx.closePath(); ctx.fill();
      ctx.fillStyle = css([120, 100, 80], 1, k * 0.4);
      ctx.beginPath(); ctx.moveTo(x - w * 0.18, g); ctx.lineTo(x, g - h * 0.55); ctx.lineTo(x + w * 0.18, g); ctx.closePath(); ctx.fill();
    }
    ctx.globalAlpha = 1;
  }
  // 安息：一层暖金的光落在近地上；金色的微尘缓缓飘着
  function drawRest(ctx) {
    const k = LV.hbRest;
    if (k < 0.005 || !SP) return;
    const s = LS(2), x0 = PX(0.4) * W.w, x1 = W.w, gy = gY(2, PX(0.6));
    ctx.globalCompositeOperation = 'lighter';
    ctx.globalAlpha = k * 0.34 * dayBoost();
    ctx.drawImage(SP.band, x0, gy - 70 * s, x1 - x0, 110 * s);
    glowE(ctx, SP.gold, PX(0.56) * W.w, gy, 190 * s, 50 * s, k * 0.3);
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  function drawRestMotes(ctx) {
    const k = LV.hbRest;
    if (k < 0.02 || !SP) return;
    const s = LS(2), x0 = PX(0.42) * W.w, x1 = PX(0.98) * W.w;
    ctx.globalCompositeOperation = 'lighter';
    for (let i = 0; i < 26; i++) {
      const x = x0 + ((hsh(i) * (x1 - x0) + W.t * (4 + 3 * hsh(i + 1)) * s) % (x1 - x0));
      const y = gY(2, x / W.w) - (14 + 46 * hsh(i + 2)) * s + Math.sin(W.t * 0.6 + i) * 5 * s;
      glowAt(ctx, SP.gold, x, y, (2.4 + 1.6 * hsh(i + 4)) * s, k * (0.35 + 0.35 * Math.sin(W.t * 1.3 + i * 2.1)));
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }

  // ════════════════════════════════════════════════════════════
  //  画：天上的圣所（真帐幕）、施恩的宝座的光、接他的云
  // ════════════════════════════════════════════════════════════
  const VEIL_H = [[140, 176, 255], [196, 160, 250], [255, 164, 184]];
  function drawHeaven(ctx) {
    const k = LV.hbHeaven, pt = LV.hbPattern;
    if (k < 0.005 || !SP) return;
    const H = heaven(), th = LV.hbThrone, ov = LV.hbHVeil, day = W.daylight, fire = LV.hbFire;
    const sig = [qz(k), qz(pt), qz(th), qz(ov), qz(day), qz(fire), W.w, W.h].join(',');
    const bx0 = Math.max(0, H.mx - H.w * 1.5), bx1 = Math.min(W.w, H.mx + H.w * 1.5);
    const by0 = Math.max(0, H.top - H.w * 1.5), by1 = Math.min(W.h, H.bot + H.w * 0.4);
    if (bx1 - bx0 < 2 || by1 - by0 < 2) return;
    const back = layer('hvBack', bx0, by0, bx1 - bx0, by1 - by0, sig, 0.31, g => heavenBack(g, H, k, day));
    const light = layer('hvLight', bx0, by0, bx1 - bx0, by1 - by0, sig, 0.2, g => heavenLight(g, H, k, pt, th, ov, day, fire));
    const front = layer('hvFront', bx0, by0, bx1 - bx0, by1 - by0, sig, 0.37, g => heavenFront(g, H, k, day));
    blit(ctx, back, 'source-over');
    blit(ctx, light, 'lighter');
    // 施恩的宝座的光：斜斜地照到地上
    if (th > 0.01) {
      ctx.globalCompositeOperation = 'lighter';
      for (let i = 0; i < 5; i++) {
        const tx = lerp(PX(0.46), PX(0.9), i / 4) * W.w, ty = gY(2, tx / W.w);
        beamTo(ctx, H.mx, H.bot, tx, ty, H.w * (0.18 + 0.06 * (i % 2)), th * k * 0.065 * (0.8 + 0.2 * Math.sin(W.t * 0.7 + i)));
      }
      ctx.globalCompositeOperation = 'source-over';
    }
    blit(ctx, front, 'source-over');
    ctx.globalAlpha = 1;
  }
  // 后面一层云
  function heavenBack(ctx, H, k, day) {
    const u = SU();
    ctx.globalCompositeOperation = 'source-over';
    for (let i = 0; i < 9; i++) {
      const f = i / 8, cx = lerp(H.x0 - H.w * 0.2, H.x1 + H.w * 0.2, f) + Math.sin(W.t * 0.11 + i) * 3 * u;
      const cy = H.bot - H.h * (0.05 + 0.12 * hsh(i + 40)), r = H.w * (0.13 + 0.06 * hsh(i));
      glowE(ctx, SP.cloudW, cx, cy, r * 1.4, r * 0.62, k * (0.3 + 0.3 * day) * (1 - 0.4 * Math.abs(f - 0.5)));
    }
  }
  // 前面一层云（遮住帐幕的底）
  function heavenFront(ctx, H, k, day) {
    const u = SU();
    ctx.globalCompositeOperation = 'source-over';
    for (let i = 0; i < 12; i++) {
      const f = i / 11, cx = lerp(H.x0 - H.w * 0.1, H.x1 + H.w * 0.1, f) + Math.sin(W.t * 0.13 + i * 1.3) * 3 * u;
      const cy = H.bot + H.h * (0.16 + 0.1 * Math.sin(i * 1.9)), r = H.w * (0.1 + 0.05 * hsh(i + 20));
      glowE(ctx, SP.cloudW, cx, cy, r * 1.4, r * 0.62, k * (0.42 + 0.35 * day) * (1 - 0.35 * Math.abs(f - 0.5)));
    }
  }
  // 光：荣光、光芒、帐幕的身、光的柱子、灯台、幔子、亮边
  function heavenLight(ctx, H, k, pt, th, ov, day, fire) {
    const u = SU(), arch = H.w * 0.045;
    ctx.globalCompositeOperation = 'lighter';
    // 至大者的荣光：在至圣所之后、之上，缓缓放射（父不显为人形，只是光）
    glowAt(ctx, SP.gold, H.mx, H.top, H.w * (0.9 + 0.25 * th + 0.2 * ov), k * (0.24 + 0.16 * th + 0.14 * ov) * dayBoost());
    glowAt(ctx, SP.white, H.mx, H.top - H.h * 0.1, H.w * (0.24 + 0.14 * ov), k * (0.28 + 0.26 * ov + 0.1 * th));
    if (fire > 0.01) glowAt(ctx, SP.warm, H.x, H.y, H.w * 1.5, fire * 0.3 * k);
    for (let i = 0; i < 11; i++) {
      const an = -Math.PI * (0.06 + 0.88 * (i / 10)) + Math.sin(W.t * 0.07 + i) * 0.03;
      const L = H.w * (0.7 + 0.4 * hsh(i + 3)) * (1 + 0.3 * ov + 0.15 * th);
      const oy = H.y - H.h * 0.2;
      beamTo(ctx, H.mx, oy, H.mx + Math.cos(an) * L, oy + Math.sin(an) * L, H.w * 0.1,
        k * (0.045 + 0.04 * th + 0.05 * ov) * (0.65 + 0.35 * Math.sin(W.t * 0.45 + i * 1.3)) * dayBoost());
    }
    ctx.globalCompositeOperation = 'lighter';
    // 帐幕的身：不是一个盒子，只是柔和的光（上亮下淡，两端淡入天空）
    glowE(ctx, SP.pale, H.x, H.y - H.h * 0.05, H.w * 0.62, H.h * 0.75, k * (0.42 + 0.12 * day));
    const gr = ctx.createLinearGradient(H.x0, 0, H.x1, 0);
    gr.addColorStop(0, rgba([255, 244, 222], 0));
    gr.addColorStop(0.12, rgba([255, 246, 228], k * (0.16 + 0.08 * day)));
    gr.addColorStop(0.88, rgba([255, 246, 228], k * (0.16 + 0.08 * day)));
    gr.addColorStop(1, rgba([255, 244, 222], 0));
    ctx.fillStyle = gr;
    tentPath(ctx, H.x0, H.x1, H.top, H.bot, arch);
    ctx.fill();
    // 光的柱子
    for (let j = 0; j <= 5; j++) {
      const x = lerp(H.x0 + H.w * 0.025, H.xv - H.w * 0.02, j / 5);
      column(ctx, x, H.top + arch * 0.6, H.bot, H.w * 0.034, k * 0.32);
    }
    ctx.globalCompositeOperation = 'lighter';
    // 圣所里的灯台：七点光
    const lx = H.x0 + (H.xv - H.x0) * 0.5, ly = H.y + H.h * 0.08;
    for (let j = 0; j < 7; j++) glowAt(ctx, SP.gold, lx + (j - 3) * H.w * 0.02, ly - (3 - Math.abs(j - 3)) * 0.8 * u, 3 * u, k * (0.75 + 0.25 * Math.sin(W.t * 3 + j)));
    // 幔子
    drawHVeil(ctx, H, k, ov);
    // 顶上一道亮边、地上一道柔光
    ctx.lineCap = 'round';
    ctx.strokeStyle = rgba([255, 238, 200], k * 0.28);
    ctx.lineWidth = 5 * u;
    ctx.beginPath(); ctx.moveTo(H.x0, H.top + arch); ctx.quadraticCurveTo((H.x0 + H.x1) / 2, H.top - arch, H.x1, H.top + arch); ctx.stroke();
    ctx.strokeStyle = rgba([255, 252, 240], k * 0.8);
    ctx.lineWidth = Math.max(1, 1.3 * u);
    ctx.beginPath(); ctx.moveTo(H.x0, H.top + arch); ctx.quadraticCurveTo((H.x0 + H.x1) / 2, H.top - arch, H.x1, H.top + arch); ctx.stroke();
    glowE(ctx, SP.gold, H.x, H.bot, H.w * 0.62, H.h * 0.12, k * 0.45);
    // 山上指示的样式：整个的轮廓亮起来
    if (pt > 0.01) {
      ctx.strokeStyle = rgba([255, 244, 214], pt * k * 0.8);
      ctx.lineWidth = Math.max(1, 1.5 * u);
      tentPath(ctx, H.x0, H.x1, H.top, H.bot, arch);
      ctx.moveTo(H.xv, H.top); ctx.lineTo(H.xv, H.bot);
      ctx.stroke();
      glowAt(ctx, SP.gold, H.x, H.y, H.w * 0.8, pt * k * 0.3);
    }
  }
  // 天上的幔子：一幅光的幔，蓝、紫、朱红柔和地交融，几道褶的亮；打开时两半拉向两旁
  function drawHVeil(ctx, H, k, ov) {
    const vx0 = H.xv + H.w * 0.012, vx1 = H.x1 - H.w * 0.012, vy0 = H.top + H.h * 0.16, vy1 = H.bot - H.h * 0.03;
    const vw = vx1 - vx0, mid = (vx0 + vx1) / 2, cy = (vy0 + vy1) / 2;
    glowAt(ctx, SP.white, mid, cy, vw * (0.45 + 0.5 * ov), k * (0.12 + 0.48 * ov));
    for (const side of [-1, 1]) {
      const outer = side < 0 ? vx0 : vx1;
      const inT = mid + side * vw * 0.5 * ov * 0.9, inB = mid + side * vw * 0.5 * ov * 0.55, bulge = side * vw * 0.2 * ov;
      const gr = ctx.createLinearGradient(outer, 0, mid, 0);
      gr.addColorStop(0, rgba(side < 0 ? VEIL_H[0] : VEIL_H[2], k * 0.5));
      gr.addColorStop(1, rgba(VEIL_H[1], k * 0.5));
      ctx.fillStyle = gr;
      ctx.beginPath();
      ctx.moveTo(outer, vy0); ctx.lineTo(inT, vy0);
      ctx.quadraticCurveTo(lerp(inT, inB, 0.5) + bulge, cy, inB, vy1);
      ctx.lineTo(outer, vy1); ctx.closePath(); ctx.fill();
      ctx.lineWidth = Math.max(0.8, H.w * 0.006);
      for (let j = 1; j <= 3; j++) {
        const f = j / 4, xt = lerp(outer, inT, f), xb = lerp(outer, inB, f);
        ctx.strokeStyle = rgba([255, 250, 240], k * 0.3 * (0.7 + 0.3 * Math.sin(W.t * 0.8 + j + side)));
        ctx.beginPath(); ctx.moveTo(xt, vy0 + 1); ctx.quadraticCurveTo(lerp(xt, xb, 0.5) + bulge * f, cy, xb, vy1 - 1); ctx.stroke();
      }
    }
    // 绣在幔上的基路伯（金线，幔子打开时隐去）
    const ck = k * (1 - ov) * 0.6;
    if (ck > 0.01) {
      ctx.strokeStyle = rgba([255, 226, 150], ck);
      ctx.lineWidth = Math.max(0.7, H.w * 0.005);
      ctx.beginPath();
      for (const d of [-1, 1]) {
        const bx = mid + d * vw * 0.24, by = cy + H.h * 0.12, r = vw * 0.16;
        ctx.moveTo(bx, by + r * 0.6); ctx.lineTo(bx, by - r * 0.1);
        ctx.moveTo(bx, by); ctx.quadraticCurveTo(bx - d * r * 0.9, by - r * 0.8, bx - d * r * 0.1, by - r * 1.15);
        ctx.moveTo(bx, by); ctx.quadraticCurveTo(bx + d * r * 0.8, by - r * 0.6, bx + d * r * 0.6, by - r * 1.05);
      }
      ctx.stroke();
    }
  }
  // 帐幕的轮廓（顶微微拱起）
  function tentPath(ctx, x0, x1, top, bot, arch) {
    ctx.beginPath();
    ctx.moveTo(x0, bot); ctx.lineTo(x0, top + arch);
    ctx.quadraticCurveTo((x0 + x1) / 2, top - arch, x1, top + arch);
    ctx.lineTo(x1, bot); ctx.closePath();
  }
  // 接他的光明的云：云在人之前（遮住他）
  function cloudPos() {
    const u = ease(LV.hbCloudUp), H = heaven();
    const x0 = X.asc * W.w, y0 = (PORT ? 0.52 : 0.42) * W.h;
    return [lerp(x0, H.x, u), lerp(y0, H.bot + H.h * 0.1, u)];
  }
  function drawCloud(ctx) {
    const k = LV.hbCloud;
    if (k < 0.005 || !SP) return;
    const [x, y] = cloudPos(), R = (PORT ? 0.13 : 0.06) * W.w * (1 - 0.35 * LV.hbCloudUp), u = SU();
    ctx.globalCompositeOperation = 'lighter';
    glowAt(ctx, SP.gold, x, y, R * 2.6, k * 0.38);
    ctx.globalCompositeOperation = 'source-over';
    // 一朵积起来的云：底平，上面一团一团隆起；底下透出金光
    const PUFF = [[-0.85, 0.12, 0.42], [-0.5, -0.05, 0.55], [-0.12, -0.28, 0.68], [0.28, -0.16, 0.6], [0.66, 0.02, 0.48], [0.95, 0.16, 0.34],
      [-0.4, 0.2, 0.5], [0.1, 0.18, 0.58], [0.55, 0.22, 0.46]];
    for (let i = 0; i < PUFF.length; i++) {
      const [px, py, pr] = PUFF[i], dx = Math.sin(W.t * 0.3 + i * 1.7) * 1.5 * u;
      glowE(ctx, SP.cloud, x + px * R + dx, y + py * R, pr * R * 1.25, pr * R * 1.0, k * 0.9);
    }
    ctx.globalCompositeOperation = 'lighter';
    glowE(ctx, SP.gold, x, y + R * 0.3, R * 1.2, R * 0.28, k * 0.55);
    glowAt(ctx, SP.white, x, y - R * 0.1, R * 0.55, k * 0.45);
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  // ════════════════════════════════════════════════════════════
  //  画：船与灵魂的锚（6—7）
  // ════════════════════════════════════════════════════════════
  function drawBoat(ctx) {
    const k = LV.hbBoat;
    if (k < 0.005) return;
    const B = boatPos(), L = B.L, u = SU(), tz = LV.hbToss;
    ctx.save();
    ctx.globalAlpha = k;
    // 水上的倒影与浪
    ctx.fillStyle = css([12, 26, 44], 1, 0.35);
    ctx.beginPath(); ctx.ellipse(B.x, B.y + L * 0.1, L * 0.62, L * 0.07, 0, 0, TAU); ctx.fill();
    ctx.translate(B.x, B.y);
    ctx.rotate(B.ang);
    // 桅杆与收起的帆
    ctx.strokeStyle = css([86, 64, 46], 1);
    ctx.lineWidth = Math.max(1, L * 0.022);
    ctx.beginPath(); ctx.moveTo(-L * 0.05, -L * 0.08); ctx.lineTo(-L * 0.05, -L * 0.66); ctx.stroke();
    ctx.beginPath(); ctx.moveTo(-L * 0.3, -L * 0.58); ctx.lineTo(L * 0.2, -L * 0.62); ctx.stroke();
    ctx.fillStyle = css([214, 200, 176], 1);
    ctx.beginPath(); ctx.ellipse(-L * 0.05, -L * 0.6, L * 0.24, L * 0.028, -0.08, 0, TAU); ctx.fill();
    // 船上的三个人（坐在船里，抬头）
    const P = [[-0.28, [104, 112, 138]], [0.02, [128, 96, 72]], [0.26, [150, 110, 98]]];
    for (const [px, c] of P) smallPerson(ctx, px * L, -L * 0.08, L * 0.3, c, 1, 1);
    // 船身
    ctx.fillStyle = css([92, 66, 46], 1);
    ctx.beginPath();
    ctx.moveTo(-L * 0.5, -L * 0.16);
    ctx.lineTo(L * 0.5, -L * 0.2);
    ctx.quadraticCurveTo(L * 0.42, L * 0.04, L * 0.26, L * 0.06);
    ctx.lineTo(-L * 0.3, L * 0.06);
    ctx.quadraticCurveTo(-L * 0.46, L * 0.02, -L * 0.5, -L * 0.16);
    ctx.closePath(); ctx.fill();
    ctx.strokeStyle = css([188, 150, 104], 1, 0.5 + 0.3 * W.daylight, 0.1);
    ctx.lineWidth = Math.max(0.7, L * 0.014);
    ctx.beginPath(); ctx.moveTo(-L * 0.5, -L * 0.16); ctx.lineTo(L * 0.5, -L * 0.2); ctx.stroke();
    ctx.restore();
    // 浪花
    if (tz > 0.05) {
      ctx.strokeStyle = rgba([236, 244, 250], 0.55 * tz * k);
      ctx.lineWidth = Math.max(0.8, 1.2 * u);
      ctx.beginPath();
      for (let i = 0; i < 4; i++) {
        const ph = W.t * 1.6 + i * 1.7, x = B.x + (i - 1.5) * L * 0.42 + Math.sin(ph) * 4 * u, y = B.y + L * (0.1 + 0.04 * (i % 2));
        ctx.moveTo(x - L * 0.14, y); ctx.quadraticCurveTo(x, y - L * 0.08 * (0.6 + 0.4 * Math.sin(ph * 1.3)), x + L * 0.14, y);
      }
      ctx.stroke();
    }
    ctx.globalAlpha = 1;
  }
  // 锚链：自船头升起，直通入天上的幔内；锚钩住幔子
  function anchorCurve(t) {
    const B = boatPos(), H = heaven(), p0 = bowPt(B), p1 = [H.ax, H.ay];
    const c = [lerp(p0[0], p1[0], 0.25), lerp(p0[1], p1[1], 0.55)];
    const m = 1 - t;
    return [m * m * p0[0] + 2 * m * t * c[0] + t * t * p1[0], m * m * p0[1] + 2 * m * t * c[1] + t * t * p1[1]];
  }
  function drawAnchor(ctx) {
    const hv = 0.04 + 0.96 * clamp(LV.hbHeaven, 0, 1);
    const a = LV.hbAnchor * LV.hbAnchorA * LV.hbBoat * hv;
    if (a < 0.005 || !SP) return;
    const f = LV.hbAnchor, u = SU(), p0 = anchorCurve(0), p1 = anchorCurve(1), H = heaven(), pad = Math.max(30 * u, H.w * 0.2);
    const bb = [Math.min(p0[0], p1[0]) - pad, Math.min(p0[1], p1[1]) - pad, Math.abs(p1[0] - p0[0]) + pad * 2, Math.abs(p1[1] - p0[1]) + pad * 2];
    cached(ctx, 'anchor', bb, [qz(a), qz(f), qz(W.daylight), Math.round(p0[0] / 2), Math.round(p0[1] / 2), W.w, W.h].join(','), 1, 'lighter', c => drawAnchor0(c, a, f));
    // 替他们祈求：光一粒一粒自天上顺着链子下来
    const pu = LV.hbPulse;
    if (pu > 0.01 && f > 0.98) {
      ctx.globalCompositeOperation = 'lighter';
      for (let i = 0; i < 4; i++) {
        const t = 1 - ((W.t * 0.16 + i / 4) % 1), p = anchorCurve(t);
        glowAt(ctx, SP.gold, p[0], p[1], 9 * u, pu * a * 0.7);
        glowAt(ctx, SP.white, p[0], p[1], 3 * u, pu * a);
      }
      ctx.globalCompositeOperation = 'source-over';
    }
  }
  function drawAnchor0(ctx, a, f) {
    const u = SU(), N = 48;
    ctx.globalCompositeOperation = 'lighter';
    ctx.lineCap = 'round';
    for (const [w, al, c] of [[5, 0.16, [255, 214, 140]], [1.5, 0.85, [255, 244, 214]]]) {
      ctx.strokeStyle = rgba(c, al * a * dayBoost());
      ctx.lineWidth = w * u;
      ctx.beginPath();
      for (let i = 0; i <= N; i++) {
        const t = (i / N) * f, p = anchorCurve(t);
        if (i) ctx.lineTo(p[0], p[1]); else ctx.moveTo(p[0], p[1]);
      }
      ctx.stroke();
    }
    // 链上一节一节的光
    for (let i = 1; i < 18; i++) { const t = i / 18; if (t > f) break; const p = anchorCurve(t); glowAt(ctx, SP.gold, p[0], p[1], 2.2 * u, a * 0.6); }
    // 锚：钩在幔子上（锚冠朝上，锚环在下，系着链子）
    if (f > 0.94) {
      const k = sm(0.94, 1, f) * a, H = heaven(), x = H.ax, y = H.ay, r = Math.max(7, H.w * 0.07);
      glowAt(ctx, SP.gold, x, y - r * 0.5, r * 2.2, k * 0.35);
      ctx.strokeStyle = rgba([255, 228, 160], k * 0.35);
      ctx.lineWidth = Math.max(2, 3.6 * u);
      anchorGlyph(ctx, x, y, r);
      ctx.stroke();
      ctx.strokeStyle = rgba([255, 250, 232], k);
      ctx.lineWidth = Math.max(1, 1.5 * u);
      anchorGlyph(ctx, x, y, r);
      ctx.stroke();
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  function anchorGlyph(ctx, x, y, r) {
    ctx.beginPath();
    ctx.moveTo(x, y + r * 0.2); ctx.lineTo(x, y - r * 1.1);                         // 锚杆
    ctx.moveTo(x - r * 0.38, y - r * 0.02); ctx.lineTo(x + r * 0.38, y - r * 0.02);   // 横杆
    ctx.moveTo(x - r * 0.7, y - r * 0.55); ctx.quadraticCurveTo(x - r * 0.62, y - r * 1.2, x, y - r * 1.1);
    ctx.quadraticCurveTo(x + r * 0.62, y - r * 1.2, x + r * 0.7, y - r * 0.55);        // 锚臂
    ctx.moveTo(x + r * 0.13, y + r * 0.34); ctx.arc(x, y + r * 0.34, r * 0.13, 0, TAU); // 锚环
  }
  // ════════════════════════════════════════════════════════════
  //  画：地上的帐幕（8—10）
  // ════════════════════════════════════════════════════════════
  // 幔子：蓝色的底，紫色、朱红色的线织在其中；金线绣的基路伯
  const VEIL = [[46, 64, 142], [104, 58, 126], [176, 50, 56], [232, 226, 212]];
  const GOLD = [214, 170, 80], BRONZE = [170, 112, 60], LINEN = [226, 216, 196];
  function tabA(a, b) { return sm(a, b, LV.hbTab); }
  // 化去（12:27）：人手所造的，一块一块化成光，散去
  function goneK() { return S.gone ? 1 - LV.hbTab : 0; }
  function drawTabernacle(ctx) {
    if (LV.hbTab < 0.004 || !SP) return;
    const G = tabG(), s = G.s;
    const sig = [qz(LV.hbTab), qz(LV.hbLamp), qz(LV.hbAltar), qz(LV.hbIncense), qz(LV.hbGlory), qz(LV.hbCome), S.gone ? 1 : 0,
      qz(W.daylight), qz(W.dusk), qz(W.night), qz(LV.storm || 0), qz(LV.gloom || 0), W.w, W.h].join(',');
    cached(ctx, 'tab', [G.x0 - 30 * s, G.top - 90 * s, G.cw + 60 * s, G.base - G.top + 110 * s], sig, 0.1, 'source-over', c => drawTabernacle0(c));
  }
  function drawTabernacle0(ctx) {
    const G = tabG(), s = G.s, dis = goneK(), fade = S.gone ? LV.hbTab : 1, ex = 0.25 * dis;
    ctx.save();
    // ── 院子：后面一道院帷与柱子
    const fk = tabA(0, 0.25) * fade;
    if (fk > 0.01) {
      const x0 = G.x0 + G.back, x1 = G.x1 - G.back, n = 22, yb = x => G.g(x) - G.back;
      ctx.fillStyle = css(LINEN, 2, fk, ex);
      ctx.beginPath();
      for (let i = 0; i <= n; i++) { const x = lerp(x0, x1, i / n), y = yb(x) - G.ch * sm(i / n - 0.2, i / n, tabA(0, 0.25) * 1.2); if (i) ctx.lineTo(x, y); else ctx.moveTo(x, y); }
      for (let i = n; i >= 0; i--) { const x = lerp(x0, x1, i / n); ctx.lineTo(x, yb(x)); }
      ctx.closePath(); ctx.fill();
      ctx.fillStyle = css([150, 138, 116], 2, 0.3 * fk, ex);
      ctx.beginPath();
      for (let i = 0; i <= n; i++) { const x = lerp(x0, x1, i / n), y = yb(x) - G.ch * 0.4; if (i) ctx.lineTo(x, y); else ctx.moveTo(x, y); }
      for (let i = n; i >= 0; i--) { const x = lerp(x0, x1, i / n); ctx.lineTo(x, yb(x)); }
      ctx.closePath(); ctx.fill();
      ctx.fillStyle = css([120, 96, 66], 2, fk, ex);
      const pw = Math.max(1, 1.5 * s);
      for (let i = 0; i <= 12; i++) { const x = lerp(x0, x1, i / 12); ctx.fillRect(x - pw / 2, yb(x) - G.ch - 2.5 * s, pw, G.ch + 2.5 * s); }
      // 两头斜伸向后的院帷；东头（左）是门帘：蓝色、紫色、朱红色线和捻的细麻
      for (const end of [0, 1]) {
        const xa = end ? G.x1 : G.x0, xb = end ? x1 : x0, ya = G.g(xa) + 1 * s, ybk = yb(xb), h = G.ch;
        if (!end) {
          for (let j = 0; j < 8; j++) {
            const t0 = j / 8, t1 = (j + 1) / 8;
            ctx.fillStyle = css(j % 2 ? VEIL[0] : VEIL[1 + ((j >> 1) % 2)], 2, fk, 0.06 + ex);
            ctx.beginPath();
            ctx.moveTo(lerp(xa, xb, t0), lerp(ya, ybk, t0) - h * lerp(1.1, 1, t0));
            ctx.lineTo(lerp(xa, xb, t1), lerp(ya, ybk, t1) - h * lerp(1.1, 1, t1));
            ctx.lineTo(lerp(xa, xb, t1), lerp(ya, ybk, t1));
            ctx.lineTo(lerp(xa, xb, t0), lerp(ya, ybk, t0));
            ctx.closePath(); ctx.fill();
          }
        } else {
          ctx.fillStyle = css(LINEN, 2, fk, ex);
          ctx.beginPath(); ctx.moveTo(xa, ya - h * 1.1); ctx.lineTo(xb, ybk - h); ctx.lineTo(xb, ybk); ctx.lineTo(xa, ya); ctx.closePath(); ctx.fill();
        }
        ctx.fillStyle = css([120, 96, 66], 2, fk, ex);
        ctx.fillRect(xa - pw / 2, ya - h * 1.1 - 2.5 * s, pw, h * 1.1 + 2.5 * s);
      }
    }
    // ── 帐幕：后墙（包金的板）、地、顶上的罩棚
    const bk = tabA(0.25, 0.55) * fade;
    if (bk > 0.01) {
      const rise = G.th * (1 - tabA(0.25, 0.5));
      const top = G.top + rise;
      ctx.fillStyle = css(GOLD, 2, bk, 0.05 + ex);
      ctx.fillRect(G.tx0, top, G.tl, G.base - top);
      // 里面稍暗（帐幕里没有日光，只有灯台的光）
      ctx.fillStyle = css([60, 44, 30], 2, bk * 0.38);
      ctx.fillRect(G.tx0, top, G.tl, G.base - top);
      ctx.strokeStyle = css([120, 88, 40], 2, bk * 0.55, ex);
      ctx.lineWidth = Math.max(0.5, 0.8 * s);
      ctx.beginPath();
      for (let x = G.tx0 + 5 * s; x < G.tx1 - 1; x += 5.4 * s) { ctx.moveTo(x, top); ctx.lineTo(x, G.base); }
      ctx.stroke();
      // 地面（银座）
      ctx.fillStyle = css([200, 202, 210], 2, bk, 0.08 + ex);
      ctx.fillRect(G.tx0, G.base - 2 * s, G.tl, 2.4 * s);
    }
    // ── 圣所里的物件：灯台、陈设饼的桌子、香坛
    const ik = tabA(0.75, 1) * fade;
    if (ik > 0.01) drawHolyThings(ctx, G, ik, ex);
    // ── 至圣所里的约柜（幔子裂开之前被幔子遮着）
    if (ik > 0.01) drawArk(ctx, G, ik, ex);
    // ── 顶上的罩棚：海狗皮（深色），两头垂下；罩棚下露出一道绣着基路伯的幔子的边
    const rk = tabA(0.6, 0.85) * fade;
    if (rk > 0.01) {
      const droop = G.th * 0.5, sag = 2.2 * s, top = G.top - (1 - tabA(0.6, 0.8)) * 8 * s;
      ctx.fillStyle = css(VEIL[0], 2, rk, 0.04 + ex);
      ctx.fillRect(G.tx0, top + G.th * 0.14, G.tl, G.th * 0.08);
      ctx.fillStyle = css([236, 196, 110], 2, rk, 0.3 + ex);
      for (let j = 0; j < 14; j++) { const x = lerp(G.tx0 + 3 * s, G.tx1 - 3 * s, j / 13); ctx.fillRect(x - 0.7 * s, top + G.th * 0.17, 1.4 * s, 1.4 * s); }
      ctx.fillStyle = css([66, 56, 50], 2, rk, ex);
      ctx.beginPath();
      ctx.moveTo(G.tx0 - 2 * s, top + droop * 0.5);
      ctx.quadraticCurveTo(G.tx0 - 1 * s, top - 1 * s, G.tx0 + 4 * s, top);
      ctx.quadraticCurveTo((G.tx0 + G.tx1) / 2, top - sag, G.tx1 - 4 * s, top);
      ctx.quadraticCurveTo(G.tx1 + 1 * s, top - 1 * s, G.tx1 + 2 * s, top + droop);
      ctx.lineTo(G.tx1 - 0.5 * s, top + droop);
      ctx.lineTo(G.tx1 - 0.5 * s, top + G.th * 0.15);
      ctx.lineTo(G.tx0 + 0.5 * s, top + G.th * 0.15);
      ctx.lineTo(G.tx0 + 0.5 * s, top + droop * 0.5);
      ctx.closePath(); ctx.fill();
      const d = litX() >= G.cx ? 1 : -1;
      ctx.strokeStyle = css([236, 214, 180], 2, rk * (0.2 + 0.4 * W.daylight), 0.3);
      ctx.lineWidth = Math.max(0.7, 1.1 * s);
      ctx.beginPath();
      if (d > 0) { ctx.moveTo((G.tx0 + G.tx1) / 2, top - sag * 0.95); ctx.quadraticCurveTo(G.tx1 - 8 * s, top - sag * 0.4, G.tx1 - 4 * s, top); }
      else { ctx.moveTo((G.tx0 + G.tx1) / 2, top - sag * 0.95); ctx.quadraticCurveTo(G.tx0 + 8 * s, top - sag * 0.4, G.tx0 + 4 * s, top); }
      ctx.stroke();
      // 帐幕的门：五根包金的柱子（门帘拉开，看得见里面）
      ctx.fillStyle = css([226, 184, 88], 2, rk, 0.15 + ex);
      for (let j = 0; j < 2; j++) { const x = G.tx0 + j * 2.6 * s; ctx.fillRect(x - 0.8 * s, top + G.th * 0.12, 1.7 * s, G.base - top - G.th * 0.12); }
      ctx.fillStyle = css(VEIL[0], 2, rk, 0.05 + ex);
      ctx.beginPath(); ctx.moveTo(G.tx0 + 3 * s, top + G.th * 0.16); ctx.quadraticCurveTo(G.tx0 + 9 * s, top + G.th * 0.4, G.tx0 + 4 * s, top + G.th * 0.62); ctx.lineTo(G.tx0 + 2 * s, top + G.th * 0.62); ctx.closePath(); ctx.fill();
    }
    // ── 铜坛与洗濯盆（院子里，帐幕门前）
    const ak = tabA(0.12, 0.32) * fade;
    if (ak > 0.01) drawAltar(ctx, G, ak, ex);
    const lk = tabA(0.2, 0.36) * fade;
    if (lk > 0.01) drawLaver(ctx, G, lk, ex);
    ctx.restore();
    ctx.globalAlpha = 1;
    ctx.globalCompositeOperation = 'source-over';
  }
  function drawHolyThings(ctx, G, k, ex) {
    const s = G.s, base = G.base - 2 * s, h = G.th;
    // 灯台：一根干，三对枝，七盏灯
    const lx = G.tx0 + G.tl * 0.17, lh = h * 0.5;
    ctx.strokeStyle = css(GOLD, 2, k, 0.3 + ex);
    ctx.lineWidth = Math.max(0.8, 1.3 * s);
    ctx.beginPath();
    ctx.moveTo(lx - 3 * s, base); ctx.lineTo(lx + 3 * s, base);
    ctx.moveTo(lx, base); ctx.lineTo(lx, base - lh);
    for (let j = 1; j <= 3; j++) {
      const r = j * 3.2 * s;
      ctx.moveTo(lx - r, base - lh); ctx.quadraticCurveTo(lx - r, base - lh * 0.5 + j * s, lx, base - lh * 0.44);
      ctx.moveTo(lx + r, base - lh); ctx.quadraticCurveTo(lx + r, base - lh * 0.5 + j * s, lx, base - lh * 0.44);
    }
    ctx.stroke();
    const lamp = LV.hbLamp * k;
    if (lamp > 0.01) {
      ctx.globalCompositeOperation = 'lighter';
      glowAt(ctx, SP.warm, lx, base - lh, 26 * s, lamp * 0.45);
      for (let j = -3; j <= 3; j++) glowAt(ctx, SP.gold, lx + j * 3.2 * s, base - lh - 1.4 * s, 2.2 * s * (0.85 + 0.15 * Math.sin(W.t * 7 + j)), lamp);
      ctx.globalCompositeOperation = 'source-over';
    }
    // 陈设饼的桌子：包金，两摞饼
    const tx = G.tx0 + G.tl * 0.42, tw = 9 * s, ty = base - h * 0.26;
    ctx.fillStyle = css(GOLD, 2, k, 0.25 + ex);
    ctx.fillRect(tx - tw, ty, tw * 2, 1.4 * s);
    ctx.fillRect(tx - tw + 0.8 * s, ty, 1 * s, base - ty);
    ctx.fillRect(tx + tw - 1.8 * s, ty, 1 * s, base - ty);
    ctx.fillStyle = css([226, 196, 150], 2, k, 0.2 + ex);
    for (const ox of [-0.5, 0.5]) for (let j = 0; j < 3; j++) { ctx.beginPath(); ctx.ellipse(tx + ox * tw, ty - 0.8 * s - j * 1.5 * s, 3.4 * s, 0.8 * s, 0, 0, TAU); ctx.fill(); }
    // 香坛（在幔子前）
    const ix = G.tv - G.tl * 0.075, ih = h * 0.28;
    ctx.fillStyle = css(GOLD, 2, k, 0.3 + ex);
    ctx.fillRect(ix - 3 * s, base - ih, 6 * s, ih);
    ctx.fillRect(ix - 3.6 * s, base - ih - 1.2 * s, 1.2 * s, 1.6 * s);
    ctx.fillRect(ix + 2.4 * s, base - ih - 1.2 * s, 1.2 * s, 1.6 * s);
    const inc = Math.max(LV.hbIncense, 0.25 * LV.hbLamp) * k;
    if (inc > 0.01) smokeCol(ctx, ix, base - ih - 1 * s, s, inc * 0.8, 22, [250, 232, 196]);
  }
  function drawArk(ctx, G, k, ex) {
    const s = G.s, base = G.base - 2 * s, cx = G.mx, w = (G.tx1 - G.tv) * 0.5, h = G.th * 0.2;
    const gl = Math.max(LV.hbGlory, LV.hbCome * 0.8);
    if (gl > 0.01) {
      ctx.globalCompositeOperation = 'lighter';
      glowAt(ctx, SP.white, cx, base - h * 1.8, G.th * 0.55, gl * 0.4 * k);
      glowAt(ctx, SP.gold, cx, base - h * 2, G.th * 1.2, gl * 0.3 * k);
      ctx.globalCompositeOperation = 'source-over';
    }
    ctx.fillStyle = css(GOLD, 2, k, 0.35 + gl * 0.3 + ex);
    ctx.fillRect(cx - w / 2, base - h, w, h);
    ctx.fillStyle = css([236, 200, 110], 2, k, 0.4 + gl * 0.3 + ex);
    ctx.fillRect(cx - w / 2 - 0.8 * s, base - h - 1.4 * s, w + 1.6 * s, 1.6 * s);
    // 施恩座上两个基路伯，翅膀相对，遮掩施恩座
    ctx.strokeStyle = css([240, 206, 120], 2, k, 0.4 + gl * 0.3 + ex);
    ctx.lineWidth = Math.max(0.8, 1.1 * s);
    ctx.beginPath();
    for (const d of [-1, 1]) {
      const bx = cx + d * w * 0.36, by = base - h - 1.4 * s;
      ctx.moveTo(bx, by); ctx.lineTo(bx, by - 3.6 * s);
      ctx.moveTo(bx, by - 3 * s); ctx.quadraticCurveTo(bx - d * w * 0.1, by - 9 * s, bx - d * w * 0.34, by - 5.6 * s);
    }
    ctx.stroke();
  }
  function drawAltar(ctx, G, k, ex) {
    const s = G.s, x = G.altar, y = G.g(x) + 1 * s, w = 10 * s, h = 13 * s * (0.3 + 0.7 * k), top = y - h;
    ctx.fillStyle = css(BRONZE, 2, k, ex);
    ctx.fillRect(x - w, top, w * 2, h);
    ctx.fillStyle = css([70, 48, 32], 2, k);
    ctx.fillRect(x - w, top + h * 0.4, w * 2, h * 0.22);
    ctx.fillStyle = css(BRONZE, 2, k, 0.08 + ex);
    for (const d of [-1, 1]) { ctx.beginPath(); ctx.moveTo(x + d * w, top + 1 * s); ctx.lineTo(x + d * (w - 3 * s), top + 1 * s); ctx.lineTo(x + d * (w + 0.6 * s), top - 3.4 * s); ctx.closePath(); ctx.fill(); }
    const f = LV.hbAltar * k;
    if (f > 0.01) {
      smokeCol(ctx, x, top - 4 * s, s, f * 0.7, 40, [200, 196, 190]);
      flame(ctx, x, top + 0.5 * s, 9 * s, f, 3);
    }
  }
  function drawLaver(ctx, G, k, ex) {
    const s = G.s, x = G.laver, y = G.g(x) + 1.5 * s;
    ctx.fillStyle = css([96, 64, 38], 2, k);
    ctx.beginPath(); ctx.moveTo(x - 3.4 * s, y); ctx.lineTo(x - 1.4 * s, y - 5.4 * s); ctx.lineTo(x + 1.4 * s, y - 5.4 * s); ctx.lineTo(x + 3.4 * s, y); ctx.closePath(); ctx.fill();
    ctx.fillStyle = css([176, 122, 66], 2, k, ex);
    ctx.beginPath(); ctx.moveTo(x - 6.4 * s, y - 9 * s); ctx.quadraticCurveTo(x, y - 2 * s, x + 6.4 * s, y - 9 * s); ctx.closePath(); ctx.fill();
    ctx.fillStyle = css([150, 190, 214], 2, 0.8 * k, 0.2);
    ctx.beginPath(); ctx.ellipse(x, y - 9 * s, 6 * s, 1.1 * s, 0, 0, TAU); ctx.fill();
  }
  function flame(ctx, x, y, h, k, seed) {
    if (k < 0.01 || h < 0.5) return;
    ctx.globalCompositeOperation = 'lighter';
    const f = 0.82 + 0.12 * Math.sin(W.t * 13 + seed) + 0.08 * Math.sin(W.t * 23.7 + seed * 2);
    glowAt(ctx, SP.warm, x, y - h * 0.45, h * 2.1, k * (0.3 + 0.45 * nightK()));
    const T4 = [[0, 1, [255, 112, 40]], [-0.3, 0.68, [255, 160, 64]], [0.28, 0.72, [255, 140, 56]], [0, 0.52, [255, 238, 176]]];
    for (let i = 0; i < 4; i++) {
      const q = T4[i], H = h * q[1] * f * (0.86 + 0.14 * Math.sin(W.t * 9 + i * 2.3 + seed)), w = h * 0.26 * q[1];
      const sx = x + q[0] * h * 0.5 + Math.sin(W.t * 6 + i * 3 + seed) * h * 0.06;
      ctx.fillStyle = rgba(q[2], k * 0.8);
      ctx.beginPath();
      ctx.moveTo(sx - w, y); ctx.quadraticCurveTo(sx - w * 0.9, y - H * 0.5, sx, y - H);
      ctx.quadraticCurveTo(sx + w * 0.9, y - H * 0.5, sx + w, y);
      ctx.closePath(); ctx.fill();
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  // 一缕上升的烟（香 / 坛上的烟）
  function smokeCol(ctx, x, y, s, k, H, rgb) {
    if (k < 0.01) return;
    for (let i = 0; i < 7; i++) {
      const t = ((W.t * 0.22 + i / 7) % 1), r = (3 + 7 * t) * s;
      const px = x + Math.sin(W.t * 0.9 + i * 1.3 + t * 4) * 3 * s * t, py = y - t * H * s;
      ctx.fillStyle = rgba(W.shade(rgb, 0, 0.15), k * 0.28 * Math.sin(t * Math.PI));
      ctx.beginPath(); ctx.arc(px, py, r, 0, TAU); ctx.fill();
    }
  }
  // 幔子：挂在至圣所前（画在人之前——大祭司进到幔子后面就看不见了）；「从上到下裂为两半」
  function drawVeil(ctx) {
    const k = tabA(0.45, 0.7) * (S.gone ? LV.hbTab : 1);
    if (k < 0.01 || !SP) return;
    const G = tabG(), s = G.s, ex = 0.2 * goneK();
    const top = G.top + G.th * 0.22 + (1 - tabA(0.45, 0.62)) * G.th * 0.5, bot = G.base - 0.5 * s;
    const x0 = G.tv, x1 = G.tx1 - 0.5 * s, xm = (x0 + x1) / 2, half = (x1 - x0) / 2;
    const tau = LV.hbTear, D = Math.min(1, tau / 0.75), e = sm(0.72, 1, tau);
    const N = 18;
    // 裂口：自上而下；裂开之后两半略向两旁拉开，边上参差
    const gap = y => {
      const fr = (y - top) / Math.max(1, bot - top);
      if (tau <= 0 || fr > D) return 0;
      return half * (0.08 + 0.46 * e) * Math.pow(Math.max(0, 1 - fr / Math.max(D, 0.01)), 0.9 - 0.8 * e) + half * 0.04 * e;
    };
    const jag = (i, side) => (tau > 0.01 ? (hsh(i * 3.7 + side) - 0.5) * 2.2 * s * Math.min(1, tau * 3) : 0);
    const inner = (i, side) => { const y = lerp(top, bot, i / N); const gp = gap(y); return gp > 0 ? xm + side * (gp + jag(i, side)) : xm; };
    // 裂开处：至圣所的荣光（不是一片白，看得见约柜）
    const gl = LV.hbGlory;
    if (tau > 0.01 && gl > 0.01) {
      ctx.globalCompositeOperation = 'lighter';
      ctx.fillStyle = rgba([255, 240, 206], gl * k * 0.28);
      ctx.beginPath();
      for (let i = 0; i <= N; i++) ctx.lineTo(inner(i, -1), lerp(top, bot, i / N));
      for (let i = N; i >= 0; i--) ctx.lineTo(inner(i, 1), lerp(top, bot, i / N));
      ctx.closePath(); ctx.fill();
      ctx.globalCompositeOperation = 'source-over';
    }
    for (const side of [-1, 1]) {
      const outer = side < 0 ? x0 : x1;
      const halfPath = () => {
        ctx.beginPath();
        ctx.moveTo(outer, top);
        for (let i = 0; i <= N; i++) ctx.lineTo(inner(i, side), lerp(top, bot, i / N));
        ctx.lineTo(outer, bot); ctx.closePath();
      };
      ctx.fillStyle = css(VEIL[0], 2, k, 0.04 + ex);
      halfPath(); ctx.fill();
      // 织在其中的紫色、朱红色线
      ctx.lineWidth = Math.max(0.6, 1.1 * s);
      for (let j = 1; j <= 5; j++) {
        const f = j / 6;
        ctx.strokeStyle = css(VEIL[j % 2 ? 1 : 2], 2, k * 0.9, 0.05 + ex);
        ctx.beginPath();
        for (let i = 0; i <= N; i++) { const y = lerp(top, bot, i / N), x = lerp(outer, inner(i, side), f); if (i) ctx.lineTo(x, y); else ctx.moveTo(x, y); }
        ctx.stroke();
      }
      // 金线绣的基路伯：身子与向上举起的两翅
      const cy = lerp(top, bot, 0.5), cxh = lerp(outer, inner(Math.round(N * 0.5), side), 0.5), r = half * 0.42;
      ctx.strokeStyle = css([236, 196, 110], 2, k * 0.85, 0.3 + ex);
      ctx.lineWidth = Math.max(0.6, 0.9 * s);
      ctx.beginPath();
      ctx.moveTo(cxh, cy + r * 0.9); ctx.lineTo(cxh, cy - r * 0.1);
      ctx.moveTo(cxh + r * 0.13, cy - r * 0.28); ctx.arc(cxh, cy - r * 0.28, r * 0.13, 0, TAU);
      ctx.moveTo(cxh, cy + r * 0.1); ctx.quadraticCurveTo(cxh - r * 0.9, cy - r * 0.2, cxh - r * 0.55, cy - r * 1.05);
      ctx.moveTo(cxh, cy + r * 0.1); ctx.quadraticCurveTo(cxh + r * 0.9, cy - r * 0.2, cxh + r * 0.55, cy - r * 1.05);
      ctx.stroke();
    }
    // 至圣所里的香：烟云自幔子后升起
    const inc = LV.hbIncense * k;
    if (inc > 0.01) {
      ctx.globalCompositeOperation = 'lighter';
      glowAt(ctx, SP.gold, xm, top - 4 * s, (x1 - x0) * 0.9, inc * 0.35);
      ctx.globalCompositeOperation = 'source-over';
      smokeCol(ctx, xm, top - 2 * s, s, inc, 34, [255, 236, 200]);
    }
    // 幔子的两边：包金的柱子
    ctx.fillStyle = css([226, 184, 88], 2, k, 0.15 + ex);
    ctx.fillRect(x0 - 0.9 * s, top - 1 * s, 1.8 * s, bot - top + 1 * s);
    ctx.fillRect(x1 - 0.9 * s, top - 1 * s, 1.8 * s, bot - top + 1 * s);
    if (tau > 0.01 && gl > 0.01) {
      ctx.globalCompositeOperation = 'lighter';
      glowAt(ctx, SP.gold, xm, lerp(top, bot, 0.6), (bot - top) * 0.75, gl * k * 0.32);
      ctx.globalCompositeOperation = 'source-over';
    }
    ctx.globalAlpha = 1;
  }
  // 山上指示的样式：天上的圣所放下一条条光的线，落在地上帐幕的角上
  function drawPattern(ctx) {
    const p = LV.hbPattern * LV.hbHeaven;
    if (p < 0.005 || !SP) return;
    const H = heaven(), G = tabG(), u = SU();
    const pairs = [[H.x0, H.bot, G.tx0, G.top], [H.xv, H.bot, G.tv, G.top], [H.x1, H.bot, G.tx1, G.top], [H.x0 - H.w * 0.1, H.bot + H.h * 0.2, G.x0, G.g(G.x0) - G.ch], [H.x1 + H.w * 0.1, H.bot + H.h * 0.2, G.x1, G.g(G.x1) - G.ch]];
    ctx.globalCompositeOperation = 'lighter';
    ctx.lineCap = 'round';
    for (let i = 0; i < pairs.length; i++) {
      const [ax, ay, bx, by] = pairs[i];
      ctx.strokeStyle = rgba([255, 226, 160], p * 0.2 * dayBoost());
      ctx.lineWidth = 4 * u;
      ctx.beginPath(); ctx.moveTo(ax, ay); ctx.lineTo(bx, by); ctx.stroke();
      ctx.strokeStyle = rgba([255, 246, 220], p * 0.7);
      ctx.lineWidth = Math.max(0.6, 0.9 * u);
      ctx.beginPath(); ctx.moveTo(ax, ay); ctx.lineTo(bx, by); ctx.stroke();
      for (let j = 0; j < 3; j++) {
        const t = (W.t * 0.35 + j / 3 + i * 0.17) % 1;
        glowAt(ctx, SP.gold, lerp(ax, bx, t), lerp(ay, by, t), 5 * u, p * 0.8 * Math.sin(t * Math.PI));
      }
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  // 「我来了」：一道光自天上的至圣所降到地上的至圣所；又新又活的路；通到天上的阶梯
  function drawCome(ctx) {
    const c = LV.hbCome;
    if (c < 0.005 || !SP || LV.hbTab < 0.3) return;
    const H = heaven(), G = tabG();
    ctx.globalCompositeOperation = 'lighter';
    beamTo(ctx, H.mx, H.bot - H.h * 0.3, G.mx, G.base, (PORT ? 0.12 : 0.05) * W.w, c * 0.55);
    beamTo(ctx, H.mx, H.bot - H.h * 0.3, G.mx, G.base, (PORT ? 0.04 : 0.016) * W.w, c * 0.7);
    glowAt(ctx, SP.white, G.mx, G.top, G.th * 1.3, c * 0.5);
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  function wayPts() {
    const G = tabG(), s = G.s, pts = [];
    const xEnd = PX(0.515) * W.w, gate = G.x0;
    // 自至圣所（约柜前）经过裂开的幔子、圣所、帐幕的门，出了院子的门，到众人脚前
    for (let i = 0; i <= 10; i++) { const x = lerp(G.mx, G.tx0 - 2 * s, i / 10); pts.push([x, G.base + 1 * s]); }
    const y0 = G.base + 1 * s;
    for (let i = 1; i <= 8; i++) { const x = lerp(G.tx0 - 2 * s, gate, i / 8); pts.push([x, lerp(y0, fY(x / W.w, 0.2), i / 8)]); }
    for (let i = 1; i <= 12; i++) { const x = lerp(gate, xEnd, i / 12); pts.push([x, fY(x / W.w, 0.2 + 0.1 * (i / 12))]); }
    return pts;
  }
  function drawWay(ctx) {
    const w = LV.hbWay;
    if (w < 0.005 || !SP || LV.hbWayA < 0.005) return;
    withAlpha(ctx, LV.hbWayA, () => drawWay0(ctx, w));
  }
  function drawWay0(ctx, w) {
    const pts = wayPts(), s = LS(2), n = pts.length - 1;
    let bx0 = 1e9, by0 = 1e9, bx1 = -1e9, by1 = -1e9;
    for (const p of pts) { bx0 = Math.min(bx0, p[0]); by0 = Math.min(by0, p[1]); bx1 = Math.max(bx1, p[0]); by1 = Math.max(by1, p[1]); }
    const pad = 14 * s;
    cached(ctx, 'way', [bx0 - pad, by0 - pad, bx1 - bx0 + pad * 2, by1 - by0 + pad * 2], [qz(w), qz(W.daylight), W.w, W.h, Math.round(pts[0][1])].join(','), 2, 'lighter', c => drawWayLines(c, pts, w));
    ctx.globalCompositeOperation = 'lighter';
    for (let j = 0; j < 5; j++) {
      const t = ((W.t * 0.12 + j / 5) % 1) * Math.min(1, w);
      const q = t * n, i = Math.min(n - 1, Math.floor(q)), a = pts[i], b = pts[i + 1], r = q - i;
      glowAt(ctx, SP.gold, lerp(a[0], b[0], r), lerp(a[1], b[1], r), 7 * s, 0.6 * Math.sin(t * Math.PI));
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  function drawWayLines(ctx, pts, w) {
    const s = LS(2), n = pts.length - 1, upto = w * n;
    ctx.globalCompositeOperation = 'lighter';
    ctx.lineCap = 'round'; ctx.lineJoin = 'round';
    for (const [lw, al, c] of [[9, 0.14, [255, 214, 150]], [3.4, 0.4, [255, 236, 190]], [1.2, 0.85, [255, 250, 232]]]) {
      ctx.strokeStyle = rgba(c, al * dayBoost());
      ctx.lineWidth = lw * s;
      ctx.beginPath();
      for (let i = 0; i <= n; i++) {
        if (i > upto) { const a = pts[i - 1], b = pts[i], t = upto - (i - 1); ctx.lineTo(lerp(a[0], b[0], t), lerp(a[1], b[1], t)); break; }
        if (i) ctx.lineTo(pts[i][0], pts[i][1]); else ctx.moveTo(pts[i][0], pts[i][1]);
      }
      ctx.stroke();
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  // 通到天上的光的阶梯：自地上的至圣所（帐幕化去之后，自那片地上）直通到天上的圣所
  function stairEnds() {
    const H = heaven(), G = tabG();
    const bx = G.mx, by = LV.hbTab > 0.2 && !S.gone ? G.top : G.base;
    return [bx, S.gone ? lerp(G.base, G.top, LV.hbTab) : by, H.mx, H.bot];
  }
  function drawStair(ctx) {
    const st = LV.hbStair;
    if (st < 0.005 || !SP) return;
    const e = stairEnds(), s = LS(2), pad = 40 * s;
    const bb = [Math.min(e[0], e[2]) - pad, Math.min(e[1], e[3]) - pad, Math.abs(e[2] - e[0]) + pad * 2, Math.abs(e[3] - e[1]) + pad * 2];
    cached(ctx, 'stair', bb, [qz(st), qz(W.daylight), e.map(Math.round).join(':'), W.w, W.h].join(','), 0.1, 'lighter', c => drawStair0(c));
  }
  function drawStair0(ctx) {
    const st = LV.hbStair;
    const [bx, by, tx, ty] = stairEnds(), u = SU(), s = LS(2);
    const ex = lerp(bx, tx, st), ey = lerp(by, ty, st);
    const wB = (PORT ? 18 : 24) * s, wT = wB * 0.5, db = dayBoost();
    ctx.globalCompositeOperation = 'lighter';
    beamTo(ctx, bx, by, ex, ey, wB * 2.6, 0.12 * db);
    beamTo(ctx, bx, by, ex, ey, wB * 0.6, 0.26 * db);
    // 一级一级的光：柔和的横光，缓缓向上流动
    const L = Math.hypot(tx - bx, ty - by), nR = Math.max(6, Math.round(L / (15 * s)));
    for (let i = 1; i < nR; i++) {
      const t = i / nR;
      if (t > st) break;
      const cx = lerp(bx, tx, t), cy = lerp(by, ty, t), hw = lerp(wB, wT, t) / 2;
      const sh = 0.35 + 0.65 * Math.pow(Math.max(0, Math.sin((t * 5 - W.t * 0.3) * Math.PI)), 2);
      glowE(ctx, SP.gold, cx, cy, hw * 1.25, Math.max(1.1, 2 * s * (1 - 0.5 * t)), 0.5 * sh * db);
    }
    for (let j = 0; j < 3; j++) {
      const t = ((W.t * 0.07 + j / 3) % 1) * st;
      glowAt(ctx, SP.white, lerp(bx, tx, t), lerp(by, ty, t), 5 * u, 0.45 * Math.sin(Math.min(1, t / Math.max(st, 0.01)) * Math.PI));
    }
    glowAt(ctx, SP.white, ex, ey, 9 * u, 0.45 * sm(0, 0.2, st));
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  // 帐幕化去时升起的光尘（只是装饰）
  function drawDissolve(ctx) {
    if (!S.gone || LV.hbTab < 0.01 || LV.hbTab > 0.995 || !SP) return;
    const G = tabG(), s = G.s, k = LV.hbTab;
    ctx.globalCompositeOperation = 'lighter';
    for (let i = 0; i < 40; i++) {
      const t = (W.t * 0.3 + hsh(i)) % 1;
      const x = lerp(G.x0, G.x1, hsh(i + 11)), y0 = lerp(G.top, G.base, hsh(i + 23));
      glowAt(ctx, SP.gold, x + Math.sin(W.t + i) * 4 * s, y0 - t * 60 * s, 2.6 * s, k * (1 - k) * 3 * Math.sin(t * Math.PI));
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }

  // ════════════════════════════════════════════════════════════
  //  画：夜空里的光画——古人在这信上得了美好的证据（11）
  //  每一幅：一团柔和的光（异象），里面以光的线描出；单位坐标（半径 1，y 向下）
  // ════════════════════════════════════════════════════════════
  const GL = [255, 226, 170], GW = [255, 242, 214], SEA = [180, 214, 255];
  function figFill(ctx, a) { ctx.fillStyle = rgba([255, 240, 206], a * 0.9); ctx.strokeStyle = rgba([255, 240, 206], a * 0.9); }
  function galAbel(ctx, x, y, R, p, a) {
    lightStroke(ctx, [[linePts(-0.8, 0.56, 0.8, 0.56), sm(0, 0.2, p)]], x, y, R, a * 0.55, GL, 1);
    const st = sm(0.08, 0.35, p);
    lightStroke(ctx, [[[[-0.3, 0.56], [-0.3, 0.26], [0.3, 0.26], [0.3, 0.56]], st], [linePts(-0.3, 0.41, 0.3, 0.41), st],
      [linePts(-0.06, 0.26, -0.06, 0.41), st], [linePts(0.13, 0.41, 0.13, 0.56), st], [linePts(-0.19, 0.41, -0.19, 0.56), st]], x, y, R, a, GL, 1);
    const fk = sm(0.28, 0.45, p) * a;
    if (fk > 0.01) flame(ctx, x, y + 0.25 * R, 0.2 * R, fk, 7);
    // 烟直上（蒙悦纳的祭）
    const sk = sm(0.35, 0.85, p), pts = [];
    for (let i = 0; i <= 22; i++) { const t = i / 22, yy = 0.16 - t * 1.08; pts.push([0.035 * Math.sin(yy * 7 + W.t * 1.3) * t, yy]); }
    lightStroke(ctx, [[pts, sk]], x, y, R, a * 0.75, [255, 240, 214], 1.7);
    // 羊羔
    const lk = sm(0.4, 0.6, p);
    if (lk > 0.01) {
      const lx = -0.58, ly = 0.44;
      lightStroke(ctx, [[arcPts(lx, ly, 0.14, 0.075, 0, TAU, 16), lk], [arcPts(lx - 0.17, ly - 0.05, 0.05, 0.045, 0, TAU, 10), lk],
        [linePts(lx - 0.08, ly + 0.06, lx - 0.08, ly + 0.12), lk], [linePts(lx + 0.08, ly + 0.06, lx + 0.08, ly + 0.12), lk]], x, y, R, a * 0.9, GW, 0.9);
    }
    // 亚伯跪在坛前
    const ak = sm(0.5, 0.75, p) * a;
    if (ak > 0.01) { figFill(ctx, ak); lightFig(ctx, x + 0.56 * R, y + 0.56 * R, 0.6 * R, 'kneel', -1); }
  }
  function galEnoch(ctx, x, y, R, p, a) {
    lightStroke(ctx, [[linePts(-0.8, 0.6, 0.8, 0.6), sm(0, 0.12, p)]], x, y, R, a * 0.55, GL, 1);
    const w = sm(0.05, 0.45, p), tk = sm(0.5, 0.92, p);
    // 他走过的脚印：到这里就没有了
    ctx.fillStyle = rgba(GW, a * 0.6);
    for (let i = 0; i < 9; i++) { const f = i / 8; if (f > w) break; ctx.beginPath(); ctx.arc(x + lerp(-0.62, -0.04, f) * R, y + (0.63 + (i % 2) * 0.03) * R, Math.max(0.8, 0.018 * R), 0, TAU); ctx.fill(); }
    // 自上面来的光
    const lc = sm(0.42, 0.55, p) * (1 - 0.6 * sm(0.9, 1, p));
    if (lc > 0.01) { ctx.globalCompositeOperation = 'lighter'; column(ctx, x - 0.02 * R, y - 1.0 * R, y + 0.62 * R, 0.34 * R, lc * a * 0.55); }
    // 以诺：走着，然后被接去（越升越亮，越淡）
    const fa = (1 - sm(0.72, 0.96, p)) * sm(0.02, 0.12, p) * a;
    if (fa > 0.01) {
      const fx0 = lerp(-0.62, -0.02, w), fy0 = lerp(0.6, -0.78, ease(tk));
      figFill(ctx, fa);
      lightFig(ctx, x + fx0 * R, y + fy0 * R, 0.62 * R, tk > 0.02 ? 'raise' : 'walk', 1, { ph: w * 16 });
      if (tk > 0.02) { ctx.globalCompositeOperation = 'lighter'; glowAt(ctx, SP.gold, x + fx0 * R, y + (fy0 - 0.3) * R, 0.4 * R, tk * fa); }
    }
    const top = sm(0.62, 1, p) * a;
    if (top > 0.01) { ctx.globalCompositeOperation = 'lighter'; glowAt(ctx, SP.white, x - 0.02 * R, y - 0.82 * R, 0.16 * R, top); glowAt(ctx, SP.gold, x - 0.02 * R, y - 0.82 * R, 0.45 * R, top * 0.4); }
  }
  function galNoah(ctx, x, y, R, p, a) {
    const wv = sm(0, 0.3, p), paths = [];
    for (let j = 0; j < 3; j++) {
      const pts = [], yy = 0.44 + j * 0.13, amp = 0.035 - j * 0.006;
      for (let i = 0; i <= 26; i++) { const xx = -0.86 + i * (1.72 / 26); pts.push([xx, yy + amp * Math.sin(xx * 9 + W.t * 1.3 + j * 1.7)]); }
      paths.push([pts, wv]);
    }
    lightStroke(ctx, paths, x, y, R, a * 0.55, SEA, 0.9);
    const ak = sm(0.2, 0.55, p), bob = Math.sin(W.t * 1.1) * 0.015;
    lightStroke(ctx, [
      [[[-0.62, 0.22 + bob], [0.62, 0.22 + bob], [0.5, 0.44 + bob], [-0.5, 0.44 + bob], [-0.62, 0.22 + bob]], ak],
      [[[-0.34, 0.22 + bob], [-0.34, 0.05 + bob], [0.36, 0.05 + bob], [0.36, 0.22 + bob]], ak],
      [[[-0.44, 0.07 + bob], [0.01, -0.1 + bob], [0.46, 0.07 + bob]], ak],
      [[[0.1, 0.1 + bob], [0.2, 0.1 + bob], [0.2, 0.16 + bob], [0.1, 0.16 + bob], [0.1, 0.1 + bob]], ak],
    ], x, y, R, a, GL, 1.1);
    // 虹
    const rk = sm(0.6, 0.9, p) * a;
    if (rk > 0.01) {
      const RB = [[255, 110, 100], [255, 180, 90], [255, 236, 120], [140, 230, 140], [120, 180, 255], [180, 140, 255]];
      ctx.globalCompositeOperation = 'lighter';
      ctx.lineWidth = Math.max(1, 0.035 * R);
      for (let i = 0; i < RB.length; i++) {
        ctx.strokeStyle = rgba(RB[i], rk * 0.42);
        ctx.beginPath(); ctx.arc(x, y + 0.5 * R, (0.98 - i * 0.045) * R, Math.PI * 1.08, Math.PI * 1.92); ctx.stroke();
      }
    }
    // 鸽子衔着橄榄叶
    const dk = sm(0.78, 1, p);
    if (dk > 0.01) {
      const dx = 0.48, dy = -0.4 + Math.sin(W.t * 2) * 0.02, f = Math.sin(W.t * 6) * 0.05;
      lightStroke(ctx, [[[[dx - 0.12, dy - 0.04 - f], [dx, dy], [dx + 0.12, dy - 0.04 - f]], dk], [[[dx - 0.02, dy + 0.02], [dx - 0.09, dy + 0.07]], dk]], x, y, R, a, GW, 1);
      ctx.globalCompositeOperation = 'lighter';
      glowAt(ctx, SP.gold, x + (dx - 0.1) * R, y + (dy + 0.08) * R, 0.05 * R, dk * a);
    }
  }
  function galMoses(ctx, x, y, R, p, a) {
    const s = LV.hbSea;
    lightStroke(ctx, [[linePts(-0.86, 0.62, -0.2, 0.66), sm(0, 0.15, p)]], x, y, R, a * 0.55, GL, 1);
    // 如同看见那不能看见的主：一根光柱（不是人形）
    const pk = sm(0.05, 0.4, p) * a;
    if (pk > 0.01) {
      ctx.globalCompositeOperation = 'lighter';
      column(ctx, x + 0.66 * R, y - 0.95 * R, y + 0.62 * R, 0.24 * R, pk * 0.6);
      column(ctx, x + 0.66 * R, y - 0.95 * R, y + 0.62 * R, 0.08 * R, pk * 0.8);
      glowAt(ctx, SP.warm, x + 0.66 * R, y + 0.5 * R, 0.26 * R, pk * 0.6);
    }
    // 海水分开：两旁的水直立如垒，中间一条干地
    if (s > 0.005) {
      const hN = 0.72 * ease(s), hF = 0.28 * ease(s);
      const wall = side => [[side * 0.19, 0.68], [side * 0.035, 0.02], [side * 0.05, 0.02 - hF], [side * 0.26, 0.68 - hN]];
      ctx.globalCompositeOperation = 'lighter';
      for (const side of [-1, 1]) {
        const q = wall(side);
        ctx.fillStyle = rgba(SEA, a * 0.16 * s);
        ctx.beginPath(); ctx.moveTo(x + q[0][0] * R, y + q[0][1] * R); for (let i = 1; i < 4; i++) ctx.lineTo(x + q[i][0] * R, y + q[i][1] * R); ctx.closePath(); ctx.fill();
        const crest = [];
        for (let i = 0; i <= 10; i++) { const t = i / 10; crest.push([lerp(q[3][0], q[2][0], t), lerp(q[3][1], q[2][1], t) + 0.02 * Math.sin(t * 12 + W.t * 3)]); }
        lightStroke(ctx, [[[q[0], q[3]], 1], [crest, 1], [[q[1], q[2]], 1]], x, y, R, a * s, SEA, 1);
        const flat = [];
        for (let i = 0; i <= 8; i++) { const t = i / 8; flat.push([side * lerp(0.28, 0.86, t), 0.66 - 0.04 * t + 0.015 * Math.sin(t * 10 + W.t * 2)]); }
        lightStroke(ctx, [[flat, 1]], x, y, R, a * s * 0.5, SEA, 0.8);
        // 水墙里一道一道的浪纹
        const inner = [];
        for (let j = 1; j <= 3; j++) {
          const f = j / 4, pts = [];
          for (let i = 0; i <= 8; i++) { const t = i / 8; const bx0 = lerp(q[0][0], q[1][0], t), by0 = lerp(q[0][1], q[1][1], t), tx0 = lerp(q[3][0], q[2][0], t), ty0 = lerp(q[3][1], q[2][1], t); pts.push([lerp(bx0, tx0, f) + 0.012 * Math.sin(t * 9 + W.t * 1.7 + j), lerp(by0, ty0, f)]); }
          inner.push([pts, 1]);
        }
        lightStroke(ctx, inner, x, y, R, a * s * 0.35, SEA, 0.7);
        glowE(ctx, SP.blue, x + (q[3][0] + q[2][0]) / 2 * R, y + (q[3][1] + q[2][1]) / 2 * R, 0.14 * R, 0.1 * R, a * s * 0.5);
      }
      // 干地上的路与走过去的人
      lightStroke(ctx, [[linePts(0, 0.7, 0, 0.02), sm(0.25, 0.6, s)]], x, y, R, a * 0.7, GW, 1.2);
      const cross = sm(0.35, 1, s);
      if (cross > 0.01) {
        figFill(ctx, a * 0.85);
        for (let j = 0; j < 11; j++) {
          const t = clamp(cross * 1.25 - j * 0.1, 0, 1);
          if (t <= 0) continue;
          const py = lerp(0.68, 0.06, t), sc = lerp(0.26, 0.07, t);
          lightFig(ctx, x + Math.sin(j * 2.1) * 0.03 * R, y + py * R, sc * R, 'walk', -1, { ph: W.t * 3 + j, arms: false });
        }
      }
    }
    // 摩西举杖
    const mk = sm(0.45, 0.8, p) * a;
    if (mk > 0.01) { figFill(ctx, mk); lightFig(ctx, x - 0.52 * R, y + 0.64 * R, 0.62 * R, 'staff', 1); }
  }
  function galJericho(ctx, x, y, R, p, a) {
    const r = LV.hbRahab;
    lightStroke(ctx, [[linePts(-0.84, 0.62, 0.84, 0.62), sm(0, 0.15, p)]], x, y, R, a * 0.55, GL, 1);
    // 七日围绕：一圈一圈的光点
    const laps = sm(0.04, 0.5, p) * 7;
    ctx.globalCompositeOperation = 'lighter';
    ctx.fillStyle = rgba(GW, a * 0.8);
    for (let lap = 0; lap < 7; lap++) {
      const f = clamp(laps - lap, 0, 1);
      if (f <= 0) break;
      const n = Math.floor(f * 14);
      for (let i = 0; i < n; i++) {
        const an = (i / 14) * TAU + lap * 0.22 + Math.PI / 2;
        const rx = (0.8 + lap * 0.014) * R, ry = (0.2 + lap * 0.008) * R;
        ctx.beginPath(); ctx.arc(x + Math.cos(an) * rx, y + (0.5 * R) + Math.sin(an) * ry, Math.max(0.7, 0.014 * R), 0, TAU); ctx.fill();
      }
    }
    // 城：墙分成几段；倒塌时各段倾倒、下沉、淡去——惟有喇合的那一段（挂着朱红线绳的窗）仍立着
    const ck = sm(0.08, 0.35, p), fall = sm(0.55, 0.82, p);
    const SEG = [[-0.58, -0.36], [-0.36, -0.14], [-0.14, 0.06], [0.06, 0.2], [0.2, 0.42], [0.42, 0.58]];
    const RAHAB = 4;
    for (let i = 0; i < SEG.length; i++) {
      const [s0, s1] = SEG[i], keep = i === RAHAB;
      const f = keep ? 0 : fall, al = a * (1 - f) * ck;
      if (al < 0.01) continue;
      const tower = i === 0 || i === SEG.length - 1, topY = tower ? 0.04 : 0.22;
      const pts = [[s0, 0.62], [s0, topY]];
      const nC = Math.max(2, Math.round((s1 - s0) / 0.07));
      for (let c = 0; c < nC; c++) { const xa = lerp(s0, s1, c / nC), xb = lerp(s0, s1, (c + 0.5) / nC), xc = lerp(s0, s1, (c + 1) / nC); pts.push([xa, topY - 0.05], [xb, topY - 0.05], [xb, topY], [xc, topY]); }
      pts.push([s1, 0.62]);
      const piv = [(s0 + s1) / 2, 0.62], ang = f * (i % 2 ? 0.9 : -0.8), dy = f * f * 0.22;
      const tp = pts.map(q => { const dx0 = q[0] - piv[0], dy0 = q[1] - piv[1]; return [piv[0] + dx0 * Math.cos(ang) - dy0 * Math.sin(ang), piv[1] + dx0 * Math.sin(ang) + dy0 * Math.cos(ang) + dy]; });
      lightStroke(ctx, [[tp, 1]], x, y, R, al, GL, 1);
      if (i === 2 && f < 0.5) lightStroke(ctx, [[arcPts(0.04, 0.62, 0.07, 0.14, Math.PI, TAU, 10).map(q => [q[0] - 0.04, q[1]]), 1]], x, y, R, al, GL, 0.9);
    }
    // 城里的房屋（在墙后）
    const hk = ck * (1 - fall) * a;
    if (hk > 0.01) lightStroke(ctx, [[[[-0.3, 0.22], [-0.3, 0.1], [-0.12, 0.1], [-0.12, 0.22]], 1], [[[0.0, 0.22], [0.0, 0.06], [0.16, 0.06], [0.16, 0.22]], 1]], x, y, R, hk * 0.6, GL, 0.8);
    // 倒塌扬起的尘（一团淡淡的光）
    const dust = Math.sin(Math.PI * fall) * a;
    if (dust > 0.01) { ctx.globalCompositeOperation = 'lighter'; glowE(ctx, SP.gold, x - 0.1 * R, y + 0.55 * R, 0.8 * R, 0.25 * R, dust * 0.4); }
    // 喇合的窗与朱红线绳
    const wk = Math.max(r, 0.4 * sm(0.3, 0.5, p)) * a;
    if (wk > 0.01) {
      const wx = 0.29, wy = 0.33;
      ctx.globalCompositeOperation = 'lighter';
      glowAt(ctx, SP.warm, x + wx * R, y + wy * R, 0.14 * R, wk * 0.9);
      lightStroke(ctx, [[[[wx - 0.05, wy - 0.05], [wx + 0.05, wy - 0.05], [wx + 0.05, wy + 0.05], [wx - 0.05, wy + 0.05], [wx - 0.05, wy - 0.05]], 1]], x, y, R, wk, GW, 0.9);
      const cord = r * a;
      if (cord > 0.01) {
        const cp = [];
        for (let i = 0; i <= 10; i++) { const t = i / 10; cp.push([wx + 0.02 + 0.015 * Math.sin(t * 5 + W.t * 1.2) * t, wy + 0.05 + t * 0.26]); }
        lightStroke(ctx, [[cp, sm(0, 0.6, r)]], x, y, R, cord, [255, 70, 60], 1.3);
        glowE(ctx, SP.red, x + (wx + 0.03) * R, y + (wy + 0.18) * R, 0.06 * R, 0.16 * R, cord * 0.7);
        figFill(ctx, cord * 0.8);
        lightFig(ctx, x + wx * R, y + (wy + 0.045) * R, 0.1 * R, 'stand', -1, { arms: false });
      }
    }
  }
  const GAL_DRAW = { abel: galAbel, enoch: galEnoch, noah: galNoah, moses: galMoses, jericho: galJericho };
  function galAlpha(key) {
    const g = GAL[key];
    if (LV[g.lv] < 0.002) return 0;
    return Math.max(LV[g.grp], LV.hbWit * 0.85) * (0.75 + 0.25 * nightK());
  }
  function drawGallery(ctx) {
    if (!SP) return;
    for (const key in GAL) {
      const a = galAlpha(key);
      if (a < 0.01) continue;
      const P = galPos(key), g = GAL[key];
      const sig = [qz(a), qz(LV[g.lv]), qz(LV.hbSea), qz(LV.hbRahab), Math.round(P.x), Math.round(P.y), Math.round(P.R), W.w, W.h].join(',');
      cached(ctx, 'gal_' + key, [P.x - P.R * 1.5, P.y - P.R * 1.5, P.R * 3, P.R * 3], sig, 0.1, 'lighter', c => {
        c.save();
        c.globalCompositeOperation = 'lighter';
        // 异象：一团柔和的光
        glowAt(c, SP.pale, P.x, P.y, P.R * 1.4, a * 0.13);
        c.strokeStyle = rgba([255, 236, 200], a * 0.12);
        c.lineWidth = Math.max(1, 1.2 * SU());
        c.beginPath(); c.arc(P.x, P.y, P.R * 1.12, 0, TAU); c.stroke();
        U.safe('hebrews.gal.' + key, () => GAL_DRAW[key](c, P.x, P.y, P.R, LV[g.lv], a * P.k));
        c.restore();
      });
    }
    ctx.globalAlpha = 1;
    ctx.globalCompositeOperation = 'source-over';
  }

  // ════════════════════════════════════════════════════════════
  //  画：满天的星、海边的沙、亚伯拉罕的帐棚（11:8–12）
  // ════════════════════════════════════════════════════════════
  function drawStars(ctx) {
    const k = LV.hbStars;
    if (k < 0.005) return;
    const n = clamp(W.night * 1.3, 0, 1) * (1 - clamp(LV.storm || 0, 0, 1)) * (1 - 0.7 * clamp(LV.gloom || 0, 0, 1));
    if (n < 0.01) return;
    blit(ctx, layer('stars', 0, 0, W.w, W.horizonY, [qz(k), qz(n), W.w, W.h].join(','), 0.1, g => renderStars(g, k, n)), 'lighter');
  }
  function renderStars(ctx, k, n) {
    const N = PORT ? 460 : 820, show = k * N, u = Math.max(0.8, W.unit);
    ctx.globalCompositeOperation = 'lighter';
    ctx.fillStyle = 'rgb(255,248,232)';
    for (let i = 0; i < N; i++) {
      if (i > show) break;
      const fa = clamp(show - i, 0, 1);
      const x = hsh(i * 1.37 + 0.3) * W.w, y = Math.pow(hsh(i * 2.71 + 0.9), 1.2) * W.horizonY * 0.97;
      const b = hsh(i * 5.1 + 0.7), sz = (0.7 + 1.5 * b * b * b) * u;
      const tw = 0.55 + 0.45 * Math.sin(W.t * (0.8 + 2.2 * hsh(i * 3.3)) + i);
      ctx.globalAlpha = Math.min(1, fa * n * tw * (0.45 + 0.55 * b));
      ctx.fillRect(x - sz / 2, y - sz / 2, sz, sz);
      if (b > 0.93 && SP) { glowAt(ctx, SP.white, x, y, sz * 3.2, fa * n * tw * 0.4); ctx.fillStyle = 'rgb(255,248,232)'; }
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  function drawSand(ctx) {
    const k = LV.hbSand;
    if (k < 0.005 || !SP) return;
    const u = SU();
    ctx.globalCompositeOperation = 'lighter';
    for (let i = 0; i < 140; i++) {
      let x, y;
      if (i < 90) { const xf = lerp(PORT ? 0.3 : 0.29, PORT ? 0.44 : 0.47, hsh(i * 1.9)); x = xf * W.w; y = gY(2, xf) + (2 + 16 * hsh(i * 3.7)) * u; if (y > W.h - 2) continue; }
      else { const xf = lerp(0.47, 0.6, hsh(i * 2.3)); x = xf * W.w; y = Math.min(gY(1, xf), W.waterlineY(1)) + (0.5 + 3 * hsh(i * 4.1)) * u; }
      const tw = 0.4 + 0.6 * Math.pow(Math.max(0, Math.sin(W.t * (1 + 2 * hsh(i)) + i * 1.3)), 2);
      glowAt(ctx, SP.gold, x, y, (1.6 + 1.6 * hsh(i * 7.7)) * u, k * tw * (0.5 + 0.5 * nightK()));
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  function drawTents(ctx) {
    const k = LV.hbTents;
    if (k < 0.005 || !SP) return;
    const s = LS(1);
    ctx.globalCompositeOperation = 'lighter';
    [X.tent0, X.tent1, X.tent2].forEach((xf, i) => {
      const x = xf * W.w, g = gY(1, xf) + 1, w = (15 + 3 * (i % 2)) * s, h = w * 0.72;
      glowE(ctx, SP.gold, x, g - h * 0.3, w * 2.2, h * 1.1, k * 0.35);
      ctx.fillStyle = rgba([255, 232, 184], k * 0.28);
      ctx.beginPath(); ctx.moveTo(x - w, g); ctx.lineTo(x - w * 0.12, g - h); ctx.lineTo(x + w * 0.16, g - h * 0.95); ctx.lineTo(x + w, g); ctx.closePath(); ctx.fill();
      ctx.strokeStyle = rgba([255, 244, 214], k * 0.85);
      ctx.lineWidth = Math.max(0.7, 1 * s);
      ctx.stroke();
      glowE(ctx, SP.warm, x, g - h * 0.25, w * 0.3, h * 0.35, k * 0.8);
    });
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }

  // ════════════════════════════════════════════════════════════
  //  画：如同云彩围着我们的见证人；摆在前头的路程（12:1–2）
  // ════════════════════════════════════════════════════════════
  function drawWitnesses(ctx) {
    const k = LV.hbWit;
    if (k < 0.005 || !SP) return;
    const A = witArc(), pl = arcAt(A, A.a0), pr = arcAt(A, A.a1), top = arcAt(A, 270)[1], R1 = (PORT ? 0.07 : 0.04) * W.w;
    cached(ctx, 'wit', [Math.min(pl[0], pr[0]) - R1 * 2, top - R1 * 2.2, Math.abs(pr[0] - pl[0]) + R1 * 4, Math.max(pl[1], pr[1]) - top + R1 * 3.4], [qz(k), qz(LV.hbRace > 0.5 ? 1 : 0), qz(W.daylight), W.w, W.h].join(','), 0.15, 'source-over', c => drawWitnesses0(c, k));
  }
  function drawWitnesses0(ctx, k) {
    const A = witArc(), u = SU(), n = 18, R1 = (PORT ? 0.07 : 0.04) * W.w, cheer = LV.hbRace > 0.5;
    const ap = f => clamp(k * 1.7 - Math.abs(f - 0.5) * 1.3, 0, 1);
    // 光明的云：大小不一，高低错落
    for (let i = 0; i < n; i++) {
      const f = (i + 0.5) / n, a = ap(f);
      if (a <= 0) continue;
      const [x, y] = arcAt(A, lerp(A.a0, A.a1, f), (hsh(i + 9) - 0.5) * R1 * 0.5);
      const r = R1 * (0.8 + 0.5 * hsh(i + 2));
      glowE(ctx, SP.cloudW, x, y + r * 0.25, r * 1.55, r * 0.6, a * (0.3 + 0.3 * W.daylight));
    }
    ctx.globalCompositeOperation = 'lighter';
    for (let i = 0; i < n; i++) {
      const f = (i + 0.5) / n, a = ap(f);
      if (a <= 0) continue;
      const [x, y] = arcAt(A, lerp(A.a0, A.a1, f), (hsh(i + 9) - 0.5) * R1 * 0.5);
      glowE(ctx, SP.gold, x, y - R1 * 0.15, R1 * 1.3, R1 * 0.7, a * 0.16);
      // 云上的见证人：发光的人影，三两个，高矮不一
      const m = 2 + Math.floor(hsh(i + 30) * 3);
      for (let j = 0; j < m; j++) {
        const fx0 = x + (j - (m - 1) / 2) * R1 * (0.36 + 0.1 * hsh(i * 5 + j)) + (hsh(i * 4 + j) - 0.5) * R1 * 0.2;
        const fy0 = y - (hsh(i * 7 + j + 1) - 0.3) * R1 * 0.3;
        const h = (PORT ? 8.5 : 11) * u * (0.8 + 0.35 * hsh(i * 7 + j));
        const ps = cheer && (i + j) % 3 === 0 ? 'raise' : 'stand';
        figFill(ctx, a * (0.55 + 0.35 * hsh(i * 3 + j)));
        lightFig(ctx, fx0, fy0, h, ps, fx0 < W.w / 2 ? 1 : -1, { arms: ps === 'raise' });
      }
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  function goalX() { return LV.hbTab > 0.3 ? tabG().mx / W.w : X.goal; }
  const RACE_V = 0.55;
  function drawRace(ctx) {
    const k = LV.hbRace;
    if (k < 0.005 || !SP) return;
    const s = LS(2), x0 = X.start, x1 = goalX(), n = 30;
    ctx.globalCompositeOperation = 'lighter';
    ctx.lineCap = 'round'; ctx.lineJoin = 'round';
    for (const [lw, al, c] of [[12, 0.12, [255, 214, 150]], [4, 0.34, [255, 236, 190]], [1.2, 0.8, [255, 250, 232]]]) {
      ctx.strokeStyle = rgba(c, al * k * dayBoost());
      ctx.lineWidth = lw * s;
      ctx.beginPath();
      for (let i = 0; i <= n; i++) { const xf = lerp(x0, x1, i / n), y = fY(xf, RACE_V) + 1 * s; if (i) ctx.lineTo(xf * W.w, y); else ctx.moveTo(xf * W.w, y); }
      ctx.stroke();
    }
    glowE(ctx, SP.gold, x1 * W.w, fY(x1, RACE_V), 30 * s, 10 * s, k * 0.6);
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  // 放下的重担
  function drawBundle(ctx) {
    if (!S.bundle) return;
    const s = LS(2), x = (X.start - 0.012) * W.w, y = fY(X.start - 0.012, RACE_V + 0.04);
    ctx.fillStyle = css([128, 100, 72], 2);
    ctx.beginPath(); ctx.ellipse(x, y - 3.2 * s, 6 * s, 3.6 * s, -0.1, 0, TAU); ctx.fill();
    ctx.strokeStyle = css([80, 60, 42], 2);
    ctx.lineWidth = Math.max(0.6, 0.9 * s);
    ctx.beginPath(); ctx.moveTo(x - 1 * s, y - 6.4 * s); ctx.quadraticCurveTo(x + 2 * s, y - 9 * s, x + 3 * s, y - 6 * s); ctx.stroke();
  }

  // ════════════════════════════════════════════════════════════
  //  画：那能摸的山；锡安山，天上的耶路撒冷；烈火（12）
  // ════════════════════════════════════════════════════════════
  const SINAI_P = (() => { const out = []; for (let i = 0; i <= 24; i++) out.push(hsh(i * 3.1 + 4.2)); return out; })();
  function drawSinai(ctx) {
    const k = LV.hbSinai;
    if (k < 0.005 || !SP) return;
    const G = sinaiG(), u = SU(), fl = 0.8 + 0.2 * Math.sin(W.t * 7) * Math.sin(W.t * 3.1);
    // 山后的红光
    ctx.globalCompositeOperation = 'lighter';
    glowAt(ctx, SP.fire, G.px, G.top + G.hgt * 0.1, (PORT ? 0.28 : 0.16) * W.w * fl, k * 0.28);
    ctx.globalCompositeOperation = 'source-over';
    // 黑暗的山：参差的岩脊；山顶被火照红
    const gr = ctx.createLinearGradient(0, G.top, 0, G.base);
    gr.addColorStop(0, 'rgba(92,40,34,' + Math.min(1, k * 1.5).toFixed(3) + ')');
    gr.addColorStop(0.3, 'rgba(38,30,36,' + Math.min(1, k * 1.5).toFixed(3) + ')');
    gr.addColorStop(1, 'rgba(18,18,26,' + Math.min(1, k * 1.5).toFixed(3) + ')');
    ctx.fillStyle = gr;
    ctx.beginPath();
    ctx.moveTo(G.px - G.half, G.base);
    for (let i = 0; i <= 24; i++) {
      const t = i / 24 * 2 - 1, hgt = Math.pow(1 - Math.abs(t), 1.15) * (0.82 + 0.18 * SINAI_P[i]) - (i % 2) * 0.03;
      ctx.lineTo(G.px + t * G.half, G.base - (G.base - G.top) * Math.max(0, hgt));
    }
    ctx.lineTo(G.px + G.half, G.base);
    ctx.closePath(); ctx.fill();
    // 岩缝里的火光
    ctx.strokeStyle = 'rgba(255,120,60,' + (k * 0.35 * fl).toFixed(3) + ')';
    ctx.lineWidth = Math.max(0.7, 1 * u);
    ctx.beginPath();
    for (let j = 0; j < 5; j++) {
      const t0 = (hsh(j + 70) - 0.5) * 0.5, y0 = G.top + (G.base - G.top) * (0.1 + 0.08 * j);
      ctx.moveTo(G.px + t0 * G.half, y0); ctx.lineTo(G.px + (t0 + 0.05) * G.half, y0 + (G.base - G.top) * 0.12);
    }
    ctx.stroke();
    // 山顶的火
    for (let i = 0; i < 3; i++) flame(ctx, G.px + (i - 1) * 9 * u, G.top + 4 * u, (PORT ? 18 : 26) * u * k * (i % 2 ? 1 : 0.72), k, i * 3 + 1);
    ctx.globalCompositeOperation = 'lighter';
    glowAt(ctx, SP.fire, G.px, G.top + 2 * u, (PORT ? 0.08 : 0.05) * W.w * fl, k * 0.8);
    ctx.globalCompositeOperation = 'source-over';
    // 密云与烟：浓黑的烟柱升起，在山顶上铺开
    for (let i = 0; i < 16; i++) {
      const t = (W.t * 0.06 + i / 16) % 1, r = (PORT ? 0.05 : 0.034) * W.w * (0.6 + 2.2 * t);
      const x = G.px + Math.sin(i * 2.3 + W.t * 0.2) * r * 0.5 + t * t * (hsh(i) - 0.3) * G.half * 1.2;
      glowAt(ctx, SP.smoke, x, G.top - t * G.hgt * 1.1, r, k * 0.62 * Math.sin(Math.min(1, t * 1.4) * Math.PI * 0.5 + 0.1) * (1 - t * 0.5));
    }
    ctx.globalAlpha = 1;
  }
  function drawZion(ctx) {
    const z = LV.hbZion;
    if (z < 0.005 || !SP) return;
    const Z = zionG(), wl = Z.x1 - Z.x0, mx = (Z.x0 + Z.x1) / 2;
    const bx0 = Math.max(0, mx - wl * 0.66), bx1 = Math.min(W.w, mx + wl * 0.66);
    const by0 = Math.max(0, Z.y - Z.h * 6.2), by1 = Math.min(W.h, Z.y + Z.h * 2.6);
    if (bx1 - bx0 < 2 || by1 - by0 < 2) return;
    const sig = [qz(z), qz(LV.hbFire), qz(W.daylight), W.w, W.h].join(',');
    blit(ctx, layer('zion', bx0, by0, bx1 - bx0, by1 - by0, sig, LV.hbFire > 0.05 ? 0.1 : 0.25, g => renderZion(g)), 'source-over');
  }
  function renderZion(ctx) {
    const z = LV.hbZion;
    const Z = zionG(), H = Z.H, u = SU(), fire = LV.hbFire, day = W.daylight;
    const x0 = Z.x0, x1 = Z.x1, yb = Z.y, wh = Z.h, wl = x1 - x0, yTop = yb - wh * 4.2;
    ctx.save();
    ctx.globalCompositeOperation = 'lighter';
    // 城的光
    glowE(ctx, SP.gold, (x0 + x1) / 2, yb - wh * 1.6, wl * 0.62, wh * 4.2, z * (0.22 + 0.2 * fire) * dayBoost());
    // 城里的房屋：两层，远的淡；圣所之下的矮些
    const grad = a => { const g = ctx.createLinearGradient(0, yTop, 0, yb); g.addColorStop(0, rgba([255, 248, 232], a)); g.addColorStop(1, rgba([255, 230, 190], a * 0.18)); return g; };
    const NB = PORT ? 11 : 17;
    // 白昼里：房屋先以不透明的暖白画出（天是亮的，光加在光上就看不出形），夜里只以光画
    const solid = clamp(day * 1.2, 0, 1);
    for (let layer = 0; layer < 2; layer++) {
      ctx.globalCompositeOperation = solid > 0.3 ? 'source-over' : 'lighter';
      ctx.fillStyle = grad(z * (layer ? 0.36 : 0.2) * (1 + 0.5 * solid));
      ctx.beginPath();
      for (let i = 0; i < NB; i++) {
        const cx = lerp(x0 + wl * 0.03, x1 - wl * 0.03, (i + 0.5 + layer * 0.45) / (NB + 0.45));
        const near = Math.exp(-Math.pow((cx - H.x) / (H.w * 0.7), 2));
        const bh = wh * (0.9 + 1.9 * hsh(i * 3 + layer * 17)) * (1 - 0.7 * near) * (layer ? 0.75 : 1.2);
        const bw = wl / NB * (0.45 + 0.3 * hsh(i + 50 + layer)), by = yb - wh * 0.9;
        ctx.moveTo(cx - bw / 2, by); ctx.lineTo(cx - bw / 2, by - bh);
        if (hsh(i + 70 + layer) > 0.55) ctx.arc(cx, by - bh, bw / 2, Math.PI, TAU);
        else { ctx.lineTo(cx, by - bh - bw * 0.35); ctx.lineTo(cx + bw / 2, by - bh); }
        ctx.lineTo(cx + bw / 2, by); ctx.closePath();
      }
      ctx.fill();
    }
    ctx.globalCompositeOperation = 'lighter';
    // 窗里的光
    for (let i = 0; i < NB; i++) {
      const cx = lerp(x0 + wl * 0.03, x1 - wl * 0.03, (i + 0.95) / (NB + 0.45)), near = Math.exp(-Math.pow((cx - H.x) / (H.w * 0.7), 2));
      glowAt(ctx, SP.gold, cx, yb - wh * (1.3 + 0.8 * hsh(i + 90)) * (1 - 0.5 * near), 2.4 * u, z * (0.55 + 0.35 * Math.sin(W.t * 0.9 + i)));
    }
    // 城墙与望楼
    const nt = PORT ? 8 : 12, tw = wl * 0.014;
    ctx.globalCompositeOperation = solid > 0.3 ? 'source-over' : 'lighter';
    ctx.fillStyle = grad(z * 0.42 * (1 + 0.4 * solid));
    ctx.beginPath();
    ctx.moveTo(x0, yb); ctx.lineTo(x0, yb - wh);
    for (let i = 0; i <= nt; i++) {
      const tx = lerp(x0, x1, i / nt);
      ctx.lineTo(tx - tw, yb - wh); ctx.lineTo(tx - tw, yb - wh * 1.65); ctx.lineTo(tx, yb - wh * 1.95); ctx.lineTo(tx + tw, yb - wh * 1.65); ctx.lineTo(tx + tw, yb - wh);
    }
    ctx.lineTo(x1, yb - wh); ctx.lineTo(x1, yb); ctx.closePath();
    ctx.fill();
    ctx.globalCompositeOperation = 'lighter';
    ctx.strokeStyle = rgba([255, 250, 236], z * 0.55);
    ctx.lineWidth = Math.max(0.8, 1 * u);
    ctx.beginPath(); ctx.moveTo(x0, yb - wh); ctx.lineTo(x1, yb - wh); ctx.stroke();
    // 垛口上一排光点
    for (let i = 0; i <= 40; i++) glowAt(ctx, SP.pale, lerp(x0, x1, i / 40), yb - wh * 1.02, 1.8 * u, z * 0.6);
    // 门：透出白光
    for (let i = 0; i < 3; i++) {
      const gx = lerp(x0, x1, (i + 1) / 4), gw = wl * 0.02;
      ctx.fillStyle = rgba([255, 250, 238], z * 0.85);
      ctx.beginPath(); ctx.moveTo(gx - gw, yb); ctx.lineTo(gx - gw, yb - wh * 0.55); ctx.arc(gx, yb - wh * 0.55, gw, Math.PI, TAU); ctx.lineTo(gx + gw, yb); ctx.closePath(); ctx.fill();
      glowAt(ctx, SP.white, gx, yb - wh * 0.4, gw * 3.2, z * 0.5);
    }
    // 我们的神乃是烈火：城上一层火焰般的光
    if (fire > 0.01) {
      for (let i = 0; i <= 44; i++) {
        const fxp = lerp(x0, x1, i / 44), hh = wh * (0.8 + 1.2 * Math.abs(Math.sin(W.t * 2.6 + i * 1.7)));
        ctx.fillStyle = rgba([255, 168, 76], fire * z * 0.3);
        ctx.beginPath(); ctx.moveTo(fxp - wl * 0.013, yb - wh * 1.6); ctx.quadraticCurveTo(fxp, yb - wh * 1.6 - hh * 1.6, fxp + wl * 0.013, yb - wh * 1.6); ctx.closePath(); ctx.fill();
      }
      glowE(ctx, SP.fire, (x0 + x1) / 2, yb - wh * 1.8, wl * 0.62, wh * 3.4, fire * z * 0.32);
    }
    // 城下的云（在前）
    ctx.globalCompositeOperation = 'source-over';
    for (let i = 0; i < 15; i++) {
      const f = i / 14, cx = lerp(x0 - wl * 0.05, x1 + wl * 0.03, f) + Math.sin(W.t * 0.11 + i) * 3 * u, r = wl * (0.045 + 0.025 * hsh(i + 30));
      glowE(ctx, SP.cloudW, cx, yb + wh * 0.15, r * 1.5, r * 0.5, z * (0.45 + 0.35 * day));
    }
    ctx.restore();
    ctx.globalAlpha = 1;
  }
  // 千万的天使：城的四周，如星的光点（间或看得出是发光的人形）
  function drawAngels(ctx) {
    const a = LV.hbAngels;
    if (a < 0.005 || !SP) return;
    const Z = zionG();
    const bx0 = Math.max(0, Z.x0 - 0.12 * W.w), by0 = PORT ? 0.3 * W.h : 0.06 * W.h, by1 = Z.y - Z.h;
    if (by1 - by0 < 2) return;
    blit(ctx, layer('angels', bx0, by0, W.w - bx0, by1 - by0, [qz(a), W.w, W.h].join(','), 0.12, g => renderAngels(g)), 'lighter');
  }
  function renderAngels(ctx) {
    const a = LV.hbAngels;
    const Z = zionG(), u = SU(), N = PORT ? 140 : 220, show = a * N;
    ctx.globalCompositeOperation = 'lighter';
    for (let i = 0; i < N; i++) {
      if (i > show) break;
      const fa = clamp(show - i, 0, 1);
      const x = lerp(Z.x0 - (PORT ? 0 : 0.1) * W.w, Z.x1, hsh(i * 3.3 + 1)), y = lerp(PORT ? 0.33 * W.h : 0.085 * W.h, Z.y - Z.h * 2, Math.pow(hsh(i * 7.1 + 2), 0.85));
      const tw = 0.5 + 0.5 * Math.sin(W.t * (1 + hsh(i * 1.3)) + i * 2.1);
      if (i % 9 === 0) { figFill(ctx, fa * a * 0.8 * (0.6 + 0.4 * tw)); lightFig(ctx, x, y, 8 * u, i % 2 ? 'raise' : 'stand', 1, { arms: false }); }
      else glowAt(ctx, SP.pale, x, y, (2 + 2.5 * hsh(i * 5.5)) * u, fa * a * tw * 0.8);
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }

  // ════════════════════════════════════════════════════════════
  //  画：接待客旅的桌子；天上的光落在他们身上（13）
  // ════════════════════════════════════════════════════════════
  const TABLE_V = 0.4;
  function drawTable(ctx) {
    const k = LV.hbTable;
    if (k < 0.005 || !SP) return;
    const s = LS(2), x = X.table * W.w, y = fY(X.table, TABLE_V), w = (PORT ? 17 : 22) * s;
    ctx.globalAlpha = k;
    ctx.fillStyle = css([104, 80, 58], 2, 0.5);
    ctx.beginPath(); ctx.ellipse(x, y + 1 * s, w * 1.5, 3.6 * s, 0, 0, TAU); ctx.fill();
    ctx.fillStyle = css([112, 82, 56], 2);
    ctx.fillRect(x - w, y - 8 * s, w * 2, 2.2 * s);
    ctx.fillRect(x - w + 1.5 * s, y - 6 * s, 1.6 * s, 6 * s);
    ctx.fillRect(x + w - 3 * s, y - 6 * s, 1.6 * s, 6 * s);
    ctx.fillStyle = css([214, 176, 118], 2, 1, 0.08);
    for (const ox of [-0.55, -0.2]) { ctx.beginPath(); ctx.ellipse(x + ox * w, y - 9.4 * s, 3.6 * s, 1.8 * s, 0, 0, TAU); ctx.fill(); }
    ctx.fillStyle = css([150, 110, 80], 2);
    ctx.beginPath(); ctx.moveTo(x + 0.2 * w, y - 8 * s); ctx.lineTo(x + 0.2 * w - 2 * s, y - 12.5 * s); ctx.lineTo(x + 0.2 * w + 2 * s, y - 12.5 * s); ctx.closePath(); ctx.fill();
    // 桌上的油灯
    const lx = x + 0.62 * w, ly = y - 9 * s;
    ctx.fillStyle = css([160, 112, 76], 2);
    ctx.beginPath(); ctx.ellipse(lx, ly, 3.6 * s, 1.5 * s, 0, 0, TAU); ctx.fill();
    ctx.globalCompositeOperation = 'lighter';
    const fl = 0.85 + 0.15 * Math.sin(W.t * 9) * Math.sin(W.t * 5.3);
    glowAt(ctx, SP.warm, lx, ly - 3 * s, 90 * s * fl, k * (0.12 + 0.45 * nightK()));
    glowAt(ctx, SP.gold, lx + 1.6 * s, ly - 3 * s, 3 * s * fl, k);
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  function drawGuest(ctx) {
    const g = LV.hbGuest;
    if (g < 0.005 || !SP || !has('guest')) return;
    const f = fig('guest');
    if (!f || f.alpha < 0.02) return;
    const h = headOf('guest', 0.55), s = LS(2);
    ctx.globalCompositeOperation = 'lighter';
    glowAt(ctx, SP.pale, h[0], h[1], 34 * s, g * f.alpha * (0.3 + 0.35 * nightK()));
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }
  function drawBless(ctx) {
    const b = LV.hbBless;
    if (b < 0.005 || !SP) return;
    const x = X.table * W.w, y = fY(X.table, TABLE_V), top = zionG().y;
    ctx.globalCompositeOperation = 'lighter';
    column(ctx, x, top, y + 6 * LS(2), (PORT ? 0.34 : 0.15) * W.w, b * 0.22);
    glowE(ctx, SP.gold, x, y, (PORT ? 0.26 : 0.12) * W.w, 16 * LS(2), b * 0.45);
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }

  // ── 转瞬的光：神的道如两刃的剑掠过全地（4:12）；每人心中一点光 ──
  function drawTransients(ctx) {
    if (!FXL.length || !SP) return;
    ctx.globalCompositeOperation = 'lighter';
    for (const e of FXL) {
      if (e.t < 0) continue;
      const q = clamp(e.t / e.dur, 0, 1);
      if (e.type === 'sweep') {
        const y = gY(2, 0.62) - 42 * LS(2), x = lerp(-0.1, 1.1, ease(q)) * W.w, L = 0.22 * W.w, a = Math.sin(q * Math.PI);
        const g = ctx.createLinearGradient(x - L, 0, x, 0);
        g.addColorStop(0, 'rgba(255,244,214,0)'); g.addColorStop(1, 'rgba(255,250,236,' + (0.85 * a).toFixed(3) + ')');
        ctx.strokeStyle = g; ctx.lineWidth = Math.max(1.2, 2 * SU());
        ctx.beginPath(); ctx.moveTo(x - L, y + 2); ctx.lineTo(x, y); ctx.stroke();
        glowAt(ctx, SP.white, x, y, 26 * SU(), a * 0.8);
        ctx.globalAlpha = a * 0.08; ctx.fillStyle = 'rgb(255,248,230)'; ctx.fillRect(0, 0, W.w, W.h);
      } else if (e.type === 'heart') {
        if (!has(e.id)) continue;
        const h = headOf(e.id, 0.62), a = Math.sin(q * Math.PI);
        glowAt(ctx, SP.gold, h[0], h[1], 16 * LS(2), a * 0.9);
        glowAt(ctx, SP.white, h[0], h[1], 4 * LS(2), a);
      }
    }
    ctx.globalCompositeOperation = 'source-over';
    ctx.globalAlpha = 1;
  }

  // ════════════════════════════════════════════════════════════
  //  布景
  // ════════════════════════════════════════════════════════════
  // 人在近地里纵深的缓动（只是画面；重演时直接到位）
  const VT = new Map();
  function sink(id, v) { const f = fig(id); if (!f) return; if (W.replaying) { f.v = v; VT.delete(id); } else VT.set(id, v); }

  function drawUnder(ctx, pass) {
    if (!isCur()) return;
    layout();
    sprites();
    if (!SP) return;
    if (pass === 'sky') {
      U.safe('hebrews.stars', () => drawStars(ctx));
      U.safe('hebrews.angels', () => drawAngels(ctx));
      U.safe('hebrews.wit', () => withAlpha(ctx, LV.hbWitA, () => drawWitnesses(ctx)));
      U.safe('hebrews.zion', () => drawZion(ctx));
      U.safe('hebrews.heaven', () => drawHeaven(ctx));
      U.safe('hebrews.gallery', () => withAlpha(ctx, LV.hbWitA, () => drawGallery(ctx)));
    } else if (pass === 'far') {
      U.safe('hebrews.sinai', () => drawSinai(ctx));
      U.safe('hebrews.lamps0', () => drawLampsLayer(ctx, 0));
    } else if (pass === 'mid') {
      U.safe('hebrews.wild', () => drawWild(ctx));
      U.safe('hebrews.tents', () => drawTents(ctx));
      U.safe('hebrews.lamps1', () => drawLampsLayer(ctx, 1));
    } else if (pass === 'near') {
      U.safe('hebrews.rest', () => drawRest(ctx));
      U.safe('hebrews.sand', () => drawSand(ctx));
      U.safe('hebrews.beam', () => drawBeam(ctx));
      U.safe('hebrews.tab', () => drawTabernacle(ctx));
      U.safe('hebrews.dissolve', () => drawDissolve(ctx));
      U.safe('hebrews.pattern', () => drawPattern(ctx));
      U.safe('hebrews.come', () => drawCome(ctx));
      U.safe('hebrews.stair', () => withAlpha(ctx, 0.35 + 0.65 * LV.hbHeaven, () => drawStair(ctx)));
      U.safe('hebrews.way', () => drawWay(ctx));
      U.safe('hebrews.race', () => drawRace(ctx));
      U.safe('hebrews.bundle', () => drawBundle(ctx));
      U.safe('hebrews.bless', () => drawBless(ctx));
      U.safe('hebrews.table', () => drawTable(ctx));
      U.safe('hebrews.radiance', () => drawRadiance(ctx));
      U.safe('hebrews.guest', () => drawGuest(ctx));
      U.safe('hebrews.oil', () => drawOil(ctx));
      U.safe('hebrews.lamps2', () => drawLampsLayer(ctx, 2));
    } else if (pass === 'air') {
      U.safe('hebrews.veil', () => drawVeil(ctx));
      U.safe('hebrews.shadow', () => drawShadow(ctx));
      U.safe('hebrews.cloud', () => drawCloud(ctx));
      U.safe('hebrews.gather', () => drawGather(ctx));
      U.safe('hebrews.motes', () => drawRestMotes(ctx));
      U.safe('hebrews.fxl', () => drawTransients(ctx));
    }
    ctx.globalAlpha = 1;
    ctx.globalCompositeOperation = 'source-over';
  }
  function draw(ctx, pass) {
    if (!isCur()) return;
    layout();
    sprites();
    if (!SP) return;
    if (pass === 'seaNear') {
      U.safe('hebrews.boat', () => drawBoat(ctx));
      U.safe('hebrews.anchor', () => drawAnchor(ctx));
    }
    ctx.globalAlpha = 1;
    ctx.globalCompositeOperation = 'source-over';
  }

  function pick(x, y, r) {
    if (!isCur()) return null;
    layout();
    let best = null;
    const test = (label, px, py, d0) => {
      if (!isFinite(px) || !isFinite(py)) return;
      const d = Math.max(0, Math.hypot(px - x, py - y) - (d0 || 0));
      if (d < r && (!best || d < best.d)) best = { label, x: px, y: py, d };
    };
    const H = heaven();
    if (LV.hbHeaven > 0.4) { test('天上的圣所', H.x0 + (H.xv - H.x0) * 0.5, H.y, H.h * 0.4); test('施恩的宝座', H.mx, H.y, H.h * 0.35); }
    if (LV.hbAnchor > 0.95 && LV.hbAnchorA > 0.5 && LV.hbBoat > 0.5) test('锚', H.ax, H.ay - 6, 6);
    if (LV.hbBoat > 0.5) { const B = boatPos(); test('船', B.x, B.y - B.L * 0.2, B.L * 0.3); }
    if (LV.hbTab > 0.5) {
      const G = tabG();
      test('帐幕', (G.tx0 + G.tv) / 2, (G.top + G.base) / 2, G.th * 0.3);
      if (LV.hbTear > 0.5) test('约柜', G.mx, G.base - G.th * 0.25, G.th * 0.15);
      else test('幔子', G.mx, (G.top + G.base) / 2, G.th * 0.25);
      test('祭坛', G.altar, G.g(G.altar) - 8 * G.s, 6 * G.s);
    }
    if (LV.hbWay > 0.5) { const p = wayPts(); const q = p[Math.floor(p.length * 0.75)]; test('又新又活的路', q[0], q[1], 6); }
    if (LV.hbStair > 0.5) { const e = stairEnds(); test('又新又活的路', lerp(e[0], e[2], 0.5), lerp(e[1], e[3], 0.5), 8); }
    for (const key in GAL) {
      if (galAlpha(key) < 0.3 || LV.hbWitA < 0.5) continue;
      const P = galPos(key);
      const label = key === 'noah' ? '挪亚' : key === 'jericho' ? (LV.hbRahab > 0.5 ? '喇合' : '耶利哥') : key === 'moses' ? (LV.hbSea > 0.5 ? '红海' : '摩西') : GAL[key].name;
      test(label, P.x, P.y, P.R * 0.7);
    }
    if (LV.hbTents > 0.5) [X.tent0, X.tent1, X.tent2].forEach(xf => test('帐棚', xf * W.w, gY(1, xf) - 6 * LS(1), 6 * LS(1)));
    if (LV.hbSand > 0.5) test('海边的沙', (PORT ? 0.37 : 0.38) * W.w, gY(2, PORT ? 0.37 : 0.38), 10);
    if (LV.hbWit > 0.5 && LV.hbWitA > 0.5) { const A = witArc(), p = arcAt(A, 270); test('见证人', p[0], p[1], 20); }
    if (LV.hbRace > 0.5) { const xf = lerp(X.start, goalX(), 0.5); test('路程', xf * W.w, fY(xf, RACE_V), 6); }
    if (S.bundle) test('重担', (X.start - 0.012) * W.w, fY(X.start - 0.012, RACE_V + 0.04) - 4, 5);
    if (LV.hbSinai > 0.4) { const G = sinaiG(); test('那能摸的山', G.px, G.top + (G.base - G.top) * 0.3, 20); }
    if (LV.hbZion > 0.4) { const Z = zionG(); test('锡安山', (Z.x0 + Z.x1) / 2, Z.y - Z.h, 20); if (LV.hbAngels > 0.5) test('千万的天使', lerp(Z.x0, Z.x1, 0.2), Z.y - Z.h * 4, 16); }
    if (LV.hbTable > 0.5) test('桌子', X.table * W.w, fY(X.table, TABLE_V) - 8 * LS(2), 8);
    // 满天的星：灵在最亮的几颗星近旁时
    if (LV.hbStars > 0.5 && nightK() > 0.4) for (let i = 0; i < 60; i++) { if (hsh(i * 5.1 + 0.7) < 0.93) continue; test('天上的星', hsh(i * 1.37 + 0.3) * W.w, Math.pow(hsh(i * 2.71 + 0.9), 1.2) * W.horizonY * 0.97, 0); }
    return best;
  }

  const SCENE = {
    init() { sprites(); },
    resize() { layout(); },
    update(dt) {
      if (!isCur()) { if (FXL.length) FXL.length = 0; return; }
      const f = dt * (W.fast || 1);
      for (let i = FXL.length - 1; i >= 0; i--) { FXL[i].t += f; if (FXL[i].t >= FXL[i].dur) FXL.splice(i, 1); }
      for (const [id, v] of VT) {
        const q = fig(id);
        if (!q) { VT.delete(id); continue; }
        const st = 0.12 * f;
        if (Math.abs(v - q.v) <= st) { q.v = v; VT.delete(id); } else q.v += Math.sign(v - q.v) * st;
      }
    },
    drawUnder,
    draw,
    reset() { FXL.length = 0; VT.clear(); },
    restore() {
      FXL.length = 0;
      for (const [id, v] of VT) { const q = fig(id); if (q) q.v = v; }
      VT.clear();
    },
    pick,
    // 看完与恢复是否一致（走查工具比较它）；程度（hb…）已由 W.lt 比较
    sig() { return { S: JSON.stringify(S) }; },
    get debug() { return { layers: Object.keys(LAYERS).map(k => [k, LAYERS[k].n || 0, LAYERS[k].c.width + 'x' + LAYERS[k].c.height]), S: Object.assign({}, S), lv: MY.reduce((o, k) => { o[k] = +LV[k].toFixed(3); return o; }, {}), port: PORT }; },
  };

  // ════════════════════════════════════════════════════════════
  //  幕的开端：夜，众先知的灯（与上一幕怎样结束无关）
  // ════════════════════════════════════════════════════════════
  function setup() {
    layout();
    const L = { deep: 1, light: 1, gather: 1, dayNight: 1, vault: 1, clouds: 0.3, land: 1, grass: 0.8, herbs: 0.6, trees: 0.15,
      lights: 1, moon: 1, stars: 1, life: 1, good: 0, sabbath: 0, given: 1 };
    for (const k in L) if (W.hasLevel(k)) W.set(k, L[k], true);
    for (const k of MY) W.set(k, 0, true);
    W.set('hbLamps', 1, true); W.set('hbShadow', 1, true); W.set('hbOil', 1, true); W.set('hbAnchorA', 1, true); W.set('hbWitA', 1, true); W.set('hbWayA', 1, true);
    W.set('bare', 0.12, true); W.set('bloom', 0.45, true);
    W.freeClock = false;
    W.setOrigin('grass', W.w * 0.62, W.ridgeBaseY(2, W.w * 0.62));
    W.setOrigin('herbs', W.w * 0.58, W.ridgeBaseY(2, W.w * 0.58));
    W.setOrigin('trees', W.w * 0.995, W.ridgeBaseY(2, W.w * 0.995));
    W.goTo(0.06, 0, true);
    const lx = W.w * 0.9, ly = W.ridgeBaseY(2, lx);
    W.setPop('fish', 50, W.w * 0.2, W.h * 0.8, true);
    W.setPop('whale', 1, W.w * 0.14, W.h * 0.78, true);
    W.setPop('bird', 14, W.w * 0.6, W.h * 0.3, true);
    W.setPop('cattle', 0, lx, ly, true);
    W.setPop('beast', 0, lx, ly, true);
    W.setPop('creeper', 6, lx, ly, true);
    W.setPop('human', 0, lx, ly, true);
    S = fresh();
    S.beamX = X.jesus;
    FXL.length = 0; VT.clear();
    const c = C();
    c.clear({ fade: false });
    // 一群人坐在冷灰的影子里（一生因怕死而为奴仆）；身旁一盏油灯
    const P0 = ['sit', 'weep', 'sit', 'bow', 'sit', 'sit'];
    PEOPLE.forEach((q, i) => {
      add(q.id, { label: q.label, sex: q.sex, age: q.age, x: X[q.id], layer: 2, v: q.v, robe: q.robe, accent: q.accent, beard: q.beard,
        facing: i % 2 ? -1 : 1, pose: P0[i], glow: 0.06, from: 'none' });
    });
    avoid([0.38, 1]);
  }

  // ════════════════════════════════════════════════════════════
  //  话语（每一句的故事约二十至三十秒）
  // ════════════════════════════════════════════════════════════
  const ROBE = { linen: [236, 232, 220], linenAcc: [184, 160, 124], hp: [54, 72, 142], hpAcc: [228, 192, 104], guest: [142, 128, 108] };
  function peopleFace(target) { each(id => face(id, target)); }
  // 不再跟随：停在此刻所在（重演时跟随者已到位）
  function unfollow(id) { const f = fig(id); if (!f) return; if (f.tx != null && W.replaying) { f.nx = f.tx; } f.follow = null; f.tx = null; }
  function bolt(b, x) { if (inst(b) || !GS.weather || !GS.weather.bolt) return; U.safe('hebrews.bolt', () => GS.weather.bolt({ x, near: false })); }
  // 众人在院子里（10:19–22）/ 桌子四围（13）的位置：[dx, v]
  const COURT = [[-0.095, 0.3], [-0.072, 0.44], [-0.05, 0.28], [-0.028, 0.42], [-0.006, 0.26], [0.016, 0.44]];
  const SEATS = { p1: [-0.062, 0.36], p2: [-0.022, 0.1], p4: [0.02, 0.1], p5: [0.05, 0.06], p6: [-0.03, 0.68], p3: [0.03, 0.68],
    guest: [0.06, 0.3], priest1: [0.1, 0.18], priest2: [0.122, 0.36], hp: [0.145, 0.12] };
  const seatX = id => PX(XL.table + SEATS[id][0]);

  const STAGES = [
    // ── 1 · 1:5 你是我的儿子，我今日生你 ────────────────────────────
    {
      kind: 'name', utter: '你是我的儿子，我今日生你', cmd: 'render 光辉 --of 神的荣耀 --as 儿子  # 今日生你', ref: '1:5',
      verse: [
        { text: '他是神荣耀所发的光辉，是神本体的真像，<br>常用他权能的命令托住万有。', ref: '希伯来书 1:3', hold: 7 },
        { text: '所有的天使，神从来对哪一个说：<br>你是我的儿子，我今日生你？', ref: '希伯来书 1:5', hold: 6 },
        { text: '再者，神使长子到世上来的时候，就说：<br>神的使者都要拜他。', ref: '希伯来书 1:6', hold: 6 },
      ],
      apply(c) {
        layout();
        T(c, [
          [0, b => { S.beamX = X.jesus; lv('hbGather', 1, b); W.goTo(0.285, 18, inst(b)); sfx(b, 'stars'); }],
          [1, () => { face('p4', 1); pose('p4', 'sit'); face('p2', 1); face('p6', 1); }],
          [1.3, b => { lv('hbBeam', 1, b); sfx(b, 'harp', { soft: true }); }],
          [3.1, b => { lv('hbDescend', 1, b); lv('hbLamps', 0, b); }],
          [4.3, b => {
            add('jesus', Object.assign({}, LOOK().jesus || { label: '耶稣', sex: 'm', robe: [232, 224, 206] }, { x: X.jesus, layer: 2, v: 0.1, facing: -1, pose: 'stand', glow: 0.5, from: inst(b) ? 'none' : 'light' }));
            lv('hbRadiance', 1, b);
            flashW(b, 0.22); ringOn(b, 'jesus', 0.22); sparkleOn(b, 'jesus', 40, [255, 240, 206], 0.6);
            sfx(b, 'chime');
          }],
          // 常用他权能的命令托住万有：一圈金光扫过全地
          [7.4, b => {
            if (inst(b)) return;
            const h = headOf('jesus', 0.6), R = Math.hypot(W.w, W.h);
            ringAt(b, h[0], h[1], R, [255, 232, 180], 5.5, 2.2);
            for (const [xf, yf] of [[0.2, 0.8], [0.5, 0.72], [0.8, 0.85], [0.9, 0.7], [0.6, 0.9]]) bless(xf * W.w, yf * W.h, 0.3 * W.w);
            sfx(b, 'harp', { soft: true });
          }],
          [8.6, b => { lv('hbVoice', 1, b); pose('jesus', 'gaze'); sfx(b, 'bell', { soft: true }); }],
          [13.2, b => lv('hbVoice', 0, b)],
          // 神的使者都要拜他
          [15.6, b => {
            const o = { label: '天使', sex: 'm', age: 'adult', layer: 2, facing: -1, angel: true, glow: 1, pose: 'stand', from: inst(b) ? 'none' : 'light' };
            add('angelA', Object.assign({ x: PX(0.718), v: 0.02 }, o));
            add('angelB', Object.assign({ x: PX(0.772), v: 0.16 }, o));
            sfx(b, 'angel');
          }],
          [17.6, b => { pose('angelA', 'bow'); pose('angelB', 'bow'); face('jesus', 1); pose('jesus', 'stand'); }],
          [20, b => { lv('hbBeam', 0.22, b); face('jesus', -1); }],
        ]);
      },
    },

    // ── 2 · 2:13 看哪，我与神所给我的儿女 ────────────────────────────
    {
      kind: 'bless', utter: '看哪，我与神所给我的儿女', cmd: 'usermod -aG 弟兄 儿女  # 并不以为耻', ref: '2:13',
      verse: [
        { text: '所以，他称他们为弟兄也不以为耻，<br>说：我要将你的名传与我的弟兄，在会中我要颂扬你；<br>又说：……看哪，我与神所给我的儿女。', ref: '希伯来书 2:11–13', hold: 8 },
        { text: '惟独见那成为比天使小一点的耶稣；<br>因为受死的苦，就得了尊贵荣耀为冠冕，<br>叫他因着神的恩，为人人尝了死味。', ref: '希伯来书 2:9', hold: 7.5 },
        { text: '儿女既同有血肉之体，他也照样亲自成了血肉之体，<br>特要藉着死败坏那掌死权的，就是魔鬼，<br>并要释放那些一生因怕死而为奴仆的人。', ref: '希伯来书 2:14–15', hold: 8 },
      ],
      apply(c) {
        layout();
        T(c, [
          [0, b => {
            W.goTo(0.36, 12, inst(b)); lv('hbBeam', 0, b); lv('hbOil', 0, b);
            fly('angelA', PX(0.718), 0.12, { dur: 2.4 }); fly('angelB', PX(0.772), 0.1, { dur: 2.4 });
          }],
          [0.8, () => walk('jesus', X.jmid, { speed: 0.032 })],
          [2.6, () => { rm('angelA'); rm('angelB'); }],
          [3.2, () => { face('p4', 1); face('p5', -1); face('p6', 1); face('p3', 1); pose('p2', 'sit'); face('p2', 1); }],
          [4.2, b => { face('jesus', -1); pose('jesus', 'raise'); sfx(b, 'harp', { soft: true }); }],
          [7.5, () => { face('jesus', 1); }],
          // 为人人尝了死味：他的光低下去，几乎熄灭
          [9.4, b => {
            pose('jesus', 'kneel'); glow('jesus', 0.04); lv('hbRadiance', 0, b);
            W.set('gloom', 0.42, inst(b));
            sfx(b, 'wind', { soft: true, low: true });
          }],
          [13.8, b => {
            W.set('gloom', 0, inst(b));
            pose('jesus', 'stand'); glow('jesus', 0.62); lv('hbRadiance', 1, b);
            flashW(b, 0.25); ringOn(b, 'jesus', 0.3); sparkleOn(b, 'jesus', 36, [255, 240, 206], 0.7);
            sfx(b, 'harp');
          }],
          // 释放那些一生因怕死而为奴仆的人：冷影退散，众人站起来
          [18.2, b => { lv('hbShadow', 0, b); sfx(b, 'chains', { soft: true }); sfx(b, 'wind', { soft: true }); }],
          [18.9, () => { pose('p4', 'stand'); glow('p4', 0.26); }],
          [19.4, () => { pose('p2', 'stand'); glow('p2', 0.26); }],
          [19.9, () => { pose('p5', 'stand'); glow('p5', 0.26); }],
          [20.4, () => { pose('p1', 'stand'); glow('p1', 0.26); pose('p3', 'stand'); glow('p3', 0.26); }],
          [20.9, () => { pose('p6', 'stand'); glow('p6', 0.26); }],
          [22.6, b => { peopleFace('jesus'); each(id => pose(id, 'raise')); PIDS.forEach(id => sparkleOn(b, id, 10, [255, 236, 190], 0.6)); sfx(b, 'crowd', { soft: true }); }],
          [25.8, () => { allPose('stand'); face('jesus', -1); }],
        ]);
      },
    },

    // ── 3 · 3:15 你们今日若听他的话，就不可硬着心 ────────────────────
    {
      kind: 'call', utter: '你们今日若听他的话，就不可硬着心', cmd: 'listen --today && unset HARD_HEART  # 另有一安息', ref: '3:15',
      verse: [
        { text: '圣灵有话说：你们今日若听他的话，<br>就不可硬着心，<br>像在旷野惹他发怒、试探他的时候一样。', ref: '希伯来书 3:7–8', hold: 7 },
        { text: '这样看来，必另有一安息日的安息为神的子民存留。<br>因为那进入安息的，乃是歇了自己的工，<br>正如神歇了他的工一样。', ref: '希伯来书 4:9–10', hold: 7.5 },
        { text: '神的道是活泼的，是有功效的，比一切两刃的剑更快，……<br>连心中的思念和主意都能辨明。', ref: '希伯来书 4:12', hold: 7 },
      ],
      apply(c) {
        layout();
        T(c, [
          // 风吹过：地一时成了旷野；中丘上四十年间苍白的帐棚
          [0, b => {
            W.set('gale', 0.42, inst(b)); lv('hbWild', 1, b);
            W.set('bare', 0.72, inst(b)); W.set('grass', 0.42, inst(b)); W.set('herbs', 0.3, inst(b)); W.set('bloom', 0.05, inst(b));
            W.goTo(0.44, 12, inst(b)); sfx(b, 'wind');
          }],
          [1.6, () => { face('p1', -1); face('p5', 1); face('p3', -1); pose('p4', 'bow'); }],
          [3.8, b => W.set('gale', 0.16, inst(b))],
          // 今日：地又青了，安息的金光落下，众人坐下安歇
          [8.4, b => {
            lv('hbWild', 0, b); W.set('gale', 0, inst(b));
            W.set('bare', 0, inst(b)); W.set('grass', 1, inst(b)); W.set('herbs', 0.9, inst(b)); W.set('bloom', 1, inst(b));
            lv('hbRest', 1, b); sfx(b, 'harp', { soft: true });
            if (!inst(b)) bless(X.jmid * W.w, gY(2, X.jmid), 0.35 * W.w);
          }],
          [9.4, () => { peopleFace('jesus'); pose('p4', 'stand'); }],
          [10.4, () => each(id => pose(id, 'sit'))],
          [11.6, () => pose('jesus', 'sit')],
          // 神的道：一道光如两刃的剑掠过全地；各人心里一点光
          [17.2, b => {
            flash(b, { type: 'sweep', dur: 1.7 });
            PIDS.forEach((id, i) => flash(b, { type: 'heart', id, t: -(0.5 + i * 0.12), dur: 1.6 }));
            lv('hbRest', 0.4, b); sfx(b, 'whisper'); sfx(b, 'chime', { soft: true });
          }],
          [19.2, () => pose('jesus', 'stand')],
        ]);
      },
    },

    // ── 4 · 5:6 你是照着麦基洗德的等次永远为祭司 ─────────────────────
    {
      kind: 'name', utter: '你是照着麦基洗德的等次永远为祭司', cmd: 'sudo priest --order 麦基洗德 --ttl ∞  # 升入高天', ref: '5:6',
      verse: [
        { text: '我们既然有一位已经升入高天尊荣的大祭司，<br>就是神的儿子耶稣，便当持定所承认的道。', ref: '希伯来书 4:14', hold: 7 },
        { text: '如此，基督也不是自取荣耀作大祭司，<br>乃是在乎向他说「你是我的儿子，我今日生你」的那一位；<br>就如经上又有一处说：<br>「你是照着麦基洗德的等次永远为祭司。」', ref: '希伯来书 5:5–6', hold: 8.5 },
        { text: '因我们的大祭司并非不能体恤我们的软弱。……<br>所以，我们只管坦然无惧地来到施恩的宝座前，<br>为要得怜恤，蒙恩惠，作随时的帮助。', ref: '希伯来书 4:15–16', hold: 8 },
      ],
      apply(c) {
        layout();
        T(c, [
          [0, b => { W.goTo(0.44, 10, inst(b)); W.set('moon', 0, inst(b)); lv('hbRest', 0, b); walk('jesus', X.asc, { speed: 0.03 }); each(id => pose(id, 'stand')); }],
          [3.6, b => { face('jesus', -1); pose('jesus', 'raise'); peopleFace('jesus'); lv('hbCloud', 1, b); sfx(b, 'harp', { soft: true }); }],
          // 升入高天：一朵光明的云把他接去
          [5, b => {
            const cy = cloudPos()[1] / W.h + (PORT ? 0.03 : 0.035);
            fly('jesus', X.asc, cy, { dur: 4.6, pose: 'raise' });
            each(id => pose(id, 'gaze'));
            sfx(b, 'angel', { soft: true });
          }],
          [9.8, b => { rm('jesus'); lv('hbCloudUp', 1, b); lv('hbHeaven', 1, b); sfx(b, 'harp'); }],
          // 父的声音：天上的圣所里光一亮
          [12.4, b => {
            lv('hbThrone', 0.5, b);
            if (!inst(b)) { const H = heaven(); ringAt(b, H.mx, H.y, M() * 0.3, [255, 236, 190], 3, 1.6); flashW(b, 0.12); }
            sfx(b, 'bell', { soft: true });
          }],
          [16, b => lv('hbCloud', 0, b)],
          // 坦然无惧地来到施恩的宝座前
          [18.2, b => {
            lv('hbThrone', 1, b);
            each((id, i) => walk(id, PX(XL[id] + 0.03), { speed: 0.02, pose: 'raise' }));
            sfx(b, 'crowd', { soft: true });
          }],
          [22.8, () => each((id, i) => pose(id, i % 2 ? 'kneel' : 'raise'))],
        ]);
      },
    },

    // ── 5 · 7:21 主起了誓，决不后悔 ───────────────────────────────
    {
      kind: 'promise', utter: '主起了誓，决不后悔', cmd: 'anchor --into 幔内 --oath  # 两件不更改的事', ref: '7:21',
      verse: [
        { text: '再者，耶稣为祭司，并不是不起誓立的。……<br>那立他的对他说：<br>「主起了誓，决不后悔，你是永远为祭司。」', ref: '希伯来书 7:20–21', hold: 7 },
        { text: '我们有这指望，如同灵魂的锚，<br>又坚固又牢靠，且通入幔内。<br>作先锋的耶稣……就为我们进入幔内。', ref: '希伯来书 6:19–20', hold: 7.5 },
        { text: '凡靠着他进到神面前的人，他都能拯救到底；<br>因为他是长远活着，替他们祈求。', ref: '希伯来书 7:25', hold: 6.5 },
      ],
      apply(c) {
        layout();
        T(c, [
          // 风浪里一只小船漂来
          [0, b => {
            W.goTo(0.47, 12, inst(b));
            lv('hbBoat', 1, b); lv('hbBoatIn', 1, b); lv('hbToss', 1, b); lv('hbThrone', 0.35, b);
            W.set('gale', 0.62, inst(b)); W.set('storm', 0.28, inst(b)); W.set('clouds', 0.62, inst(b));
            each(id => pose(id, 'stand'));
            sfx(b, 'wave'); sfx(b, 'wind');
          }],
          [2.4, () => { peopleFace(-1); pose('p1', 'point'); }],
          // 主起了誓
          [4.6, b => {
            lv('hbThrone', 0.8, b);
            if (!inst(b)) { const H = heaven(); ringAt(b, H.mx, H.y, M() * 0.34, [255, 236, 190], 3, 1.8); flashW(b, 0.14); }
            sfx(b, 'seal');
          }],
          [6, () => pose('p1', 'stand')],
          // 锚链升起，通入幔内
          [8.4, b => { lv('hbAnchor', 1, b); sfx(b, 'chains', { soft: true }); }],
          [13.3, b => {
            if (!inst(b)) { const H = heaven(); ringAt(b, H.ax, H.ay, M() * 0.16, [255, 240, 200], 2.2, 1.6); sparkleAt(b, H.ax, H.ay, 30, [255, 240, 204], 12, 'top'); }
            sfx(b, 'harp');
          }],
          [13.8, b => { lv('hbToss', 0, b); W.set('gale', 0, inst(b)); W.set('storm', 0, inst(b)); W.set('clouds', 0.3, inst(b)); }],
          [15, () => peopleFace(1)],
          // 替他们祈求：光一粒一粒顺着链子下来
          [17.2, b => { lv('hbPulse', 1, b); each((id, i) => pose(id, i % 2 ? 'pray' : 'kneel')); sfx(b, 'harp', { soft: true }); }],
          [22.4, () => allPose('stand')],
        ]);
      },
    },

    // ── 6 · 8:5 都要照着在山上指示你的样式 ───────────────────────────
    {
      kind: 'cmd', utter: '都要照着在山上指示你的样式', cmd: 'cp -r 天上的样式 ./帐幕  # 形状和影像', ref: '8:5',
      verse: [
        { text: '他们供奉的事本是天上事的形状和影像，<br>正如摩西将要造帐幕的时候，蒙神警戒他，说：<br>「你要谨慎，作各样的物件都要照着在山上指示你的样式。」', ref: '希伯来书 8:5', hold: 8 },
        { text: '我们有这样的大祭司，<br>已经坐在天上至大者宝座的右边，<br>在圣所，就是真帐幕里，作执事；<br>这帐幕是主所支的，不是人所支的。', ref: '希伯来书 8:1–2', hold: 8 },
        { text: '众祭司就常进头一层帐幕，行拜神的礼。<br>至于第二层帐幕，惟有大祭司一年一次独自进去。', ref: '希伯来书 9:6–7', hold: 7 },
      ],
      apply(c) {
        layout();
        T(c, [
          [0, b => {
            lv('hbAnchorA', 0.3, b); lv('hbPulse', 0, b); lv('hbPattern', 1, b); lv('hbThrone', 0.3, b);
            W.goTo(0.5, 12, inst(b)); peopleFace(1); sfx(b, 'scroll');
          }],
          // 地上照着样式立起帐幕
          [1.5, b => { lv('hbTab', 1, b); sfx(b, 'build'); }],
          [4, b => sfx(b, 'build')],
          [6.6, b => sfx(b, 'build')],
          [9.2, b => { sfx(b, 'build', { soft: true }); lv('hbPattern', 0.25, b); }],
          [9.8, b => { lv('hbLamp', 1, b); lv('hbAltar', 1, b); sfx(b, 'fire', { soft: true }); }],
          // 祭司与大祭司
          [11.2, b => {
            // 祭司们在院子的门里显出来
            const G = tabG(), gx = (G.x0 + G.cw * 0.05) / W.w, dx = G.cw * 0.09 / W.w;
            add('priest1', { label: '祭司', sex: 'm', age: 'adult', x: gx + dx * 2, layer: 2, v: 0.18, facing: 1, robe: ROBE.linen, accent: ROBE.linenAcc, hair: 'cloth', glow: 0.25, pose: 'stand' });
            add('priest2', { label: '祭司', sex: 'm', age: 'adult', x: gx + dx, layer: 2, v: 0.28, facing: 1, robe: ROBE.linen, accent: ROBE.linenAcc, hair: 'cloth', glow: 0.25, pose: 'stand' });
            add('hp', { label: '大祭司', sex: 'm', age: 'elder', x: gx, layer: 2, v: 0.12, facing: 1, robe: ROBE.hp, accent: ROBE.hpAcc, hair: 'cloth', beard: true, glow: 0.36, prop: null, pose: 'stand' });
          }],
          [13, () => {
            const G = tabG();
            walk('priest1', (G.x0 + G.cw * 0.2) / W.w, { speed: 0.022 });
            walk('priest2', (G.x0 + G.cw * 0.12) / W.w, { speed: 0.022 });
            walk('hp', (G.tx0 - 4 * G.s) / W.w, { speed: 0.024 });
          }],
          // 众祭司常进头一层帐幕
          [18.8, () => {
            const G = tabG();
            walk('priest1', (G.tx0 + G.tl * 0.3) / W.w, { speed: 0.02 }); sink('priest1', 0.01);
            walk('priest2', (G.tx0 + G.tl * 0.52) / W.w, { speed: 0.02 }); sink('priest2', 0.01);
            walk('hp', (G.tv - G.tl * 0.02) / W.w, { speed: 0.024 }); sink('hp', 0.01);
          }],
          // 大祭司一年一次独自进到幔子后面：香的烟云升起
          [22.4, b => { const G = tabG(); walk('hp', G.mx / W.w, { speed: 0.02 }); lv('hbIncense', 1, b); sfx(b, 'bell', { soft: true }); }],
          [25.6, () => { const G = tabG(); walk('hp', (G.tv - G.tl * 0.1) / W.w, { speed: 0.02, pose: 'stand' }); }],
        ]);
      },
    },

    // ── 7 · 10:7 神啊，我来了，为要照你的旨意行 ───────────────────────
    {
      kind: 'promise', utter: '神啊，我来了，为要照你的旨意行', cmd: 'git commit --once -m "照你的旨意" && tear 幔子', ref: '10:7',
      verse: [
        { text: '所以基督到世上来的时候，就说：……<br>那时我说：神啊，我来了，为要照你的旨意行；<br>我的事在经卷上已经记载了。', ref: '希伯来书 10:5–7', hold: 7.5 },
        { text: '但基督献了一次永远的赎罪祭，就在神的右边坐下了。', ref: '希伯来书 10:12', hold: 5.5 },
        { text: '弟兄们，我们既因耶稣的血得以坦然进入至圣所，<br>是藉着他给我们开了一条又新又活的路，<br>从幔子经过，这幔子就是他的身体。', ref: '希伯来书 10:19–20', hold: 8 },
        { text: '就当存着诚心和充足的信心来到神面前；', ref: '希伯来书 10:22', hold: 5 },
      ],
      apply(c) {
        layout();
        T(c, [
          // 我来了：一道光自天上的至圣所降到地上的至圣所
          [0, b => {
            W.goTo(0.53, 14, inst(b));
            lv('hbCome', 1, b); lv('hbHeaven', 1, b); lv('hbThrone', 0.6, b); lv('hbPattern', 0, b);
            flashW(b, 0.12); sfx(b, 'harp');
          }],
          [2.2, () => { pose('priest1', 'bow'); pose('priest2', 'bow'); face('priest1', 1); face('priest2', 1); face('hp', 1); pose('hp', 'kneel'); peopleFace(1); }],
          // 献了一次永远的赎罪祭，就在神的右边坐下：坛上的火熄了；光退回天上
          [8.9, b => { lv('hbAltar', 0, b); lv('hbIncense', 0, b); lv('hbCome', 0.3, b); sfx(b, 'bell', { soft: true }); }],
          // 幔子从上到下裂开
          [15.7, b => {
            lv('hbTear', 1, b); lv('hbGlory', 1, b); lv('hbHVeil', 1, b);
            shake(b, 0.35); flashW(b, 0.18); sfx(b, 'quake', { soft: true }); sfx(b, 'wind', { soft: true });
          }],
          // 又新又活的路；通到天上的阶梯
          [17.4, b => { lv('hbWay', 1, b); lv('hbStair', 1, b); sfx(b, 'harp'); }],
          [18.6, () => { pose('priest1', 'stand'); pose('priest2', 'stand'); pose('hp', 'stand'); face('hp', -1); }],
          // 众人坦然走进院子
          [19.8, () => {
            const G = tabG();
            each((id, i) => { walk(id, (G.x0 + G.cw * (0.1 + i * 0.09)) / W.w, { speed: 0.03, pose: 'raise' }); sink(id, COURT[i][1]); });
          }],
          [25, b => { each((id, i) => pose(id, i % 2 ? 'kneel' : 'pray')); allGlow(0.36); sfx(b, 'sing', { soft: true }); }],
        ]);
      },
    },

    // ── 8 · 11:1 信就是所望之事的实底 ─────────────────────────────
    {
      kind: 'act', utter: '信就是所望之事的实底', cmd: 'assert(所望之事) && see --unseen', ref: '11:1',
      verse: [
        { text: '信就是所望之事的实底，是未见之事的确据。<br>古人在这信上得了美好的证据。', ref: '希伯来书 11:1–2', hold: 6.5 },
        { text: '亚伯因着信，献祭与神，比该隐所献的更美，……<br>他虽然死了，却因这信，仍旧说话。', ref: '希伯来书 11:4', hold: 7 },
        { text: '以诺因着信，被接去，不至于见死，<br>人也找不着他，因为神已经把他接去了；', ref: '希伯来书 11:5', hold: 6.5 },
        { text: '挪亚因着信，既蒙神指示他未见的事，<br>动了敬畏的心，预备了一只方舟，使他全家得救。', ref: '希伯来书 11:7', hold: 7 },
      ],
      apply(c) {
        layout();
        T(c, [
          // 天黑了；天上的圣所隐去（未见之事）；众人回到空旷的地上坐下
          [0, b => {
            W.goTo(0.845, 13, inst(b));
            lv('hbHeaven', 0.15, b); lv('hbThrone', 0, b); lv('hbCome', 0, b); lv('hbGlory', 0.4, b);
            lv('hbOil', 1, b); lv('hbAnchorA', 0.15, b); lv('hbWayA', 0.45, b);
            home({ pose: 'sit', speed: 0.034 });
            walk('priest1', PX(0.645), { speed: 0.03, pose: 'sit' }); sink('priest1', 0.2);
            walk('priest2', PX(0.628), { speed: 0.03, pose: 'sit' }); sink('priest2', 0.36);
            walk('hp', PX(0.665), { speed: 0.03, pose: 'sit' }); sink('hp', 0.1);
            sfx(b, 'stars');
          }],
          [3, b => { if (!inst(b)) ringAt(b, W.w * 0.5, W.h * 0.28, M() * 0.7, [214, 226, 255], 4, 1.2); }],
          [9, () => { each(id => face(id, 1)); ['priest1', 'priest2', 'hp'].forEach(id => face(id, 1)); }],
          // 亚伯
          [7.9, b => { lv('hbAbel', 1, b); lv('hbDimA', 1, b); sfx(b, 'fire', { soft: true }); }],
          [9.4, b => { const P = galPos('abel'); nameAt(b, '亚伯', P.x, P.y + P.R * 1.25, { size: 34 * SU() }); }],
          // 以诺
          [16.2, b => { lv('hbEnoch', 1, b); sfx(b, 'wind', { soft: true }); }],
          [18, b => { const P = galPos('enoch'); nameAt(b, '以诺', P.x, P.y + P.R * 1.25, { size: 34 * SU() }); }],
          [21.6, b => sfx(b, 'angel', { soft: true })],
          // 挪亚
          [24, b => { lv('hbNoah', 1, b); sfx(b, 'wave', { soft: true }); }],
          [25.6, b => { const P = galPos('noah'); nameAt(b, '挪亚', P.x, P.y + P.R * 1.25, { size: 34 * SU() }); }],
          [28.4, b => sfx(b, 'dove', { soft: true })],
        ]);
      },
    },

    // ── 9 · 6:14 论子孙，我必叫你的子孙多起来 ────────────────────────
    {
      kind: 'promise', utter: '论子孙，我必叫你的子孙多起来', cmd: 'seq 1 ∞ | stars  # 如同天上的星', ref: '6:14',
      verse: [
        { text: '当初神应许亚伯拉罕的时候，……就指着自己起誓，说：<br>「论福，我必赐大福给你；<br>论子孙，我必叫你的子孙多起来。」', ref: '希伯来书 6:13–14', hold: 7 },
        { text: '亚伯拉罕因着信，蒙召的时候就遵命出去，<br>往将来要得为业的地方去；<br>出去的时候，还不知往哪里去。', ref: '希伯来书 11:8', hold: 7 },
        { text: '他因着信，就在所应许之地作客，好像在异地居住帐棚，<br>与那同蒙一个应许的以撒、雅各一样。', ref: '希伯来书 11:9', hold: 6.5 },
        { text: '所以从一个仿佛已死的人就生出子孙，<br>如同天上的星那样众多，海边的沙那样无数。', ref: '希伯来书 11:12', hold: 6.5 },
      ],
      apply(c) {
        layout();
        T(c, [
          [0, b => { W.goTo(0.97, 12, inst(b)); lv('hbDimA', 0.32, b); lv('hbStars', 0.3, b); sfx(b, 'stars'); }],
          [3.6, b => lv('hbStars', 1, b)],
          // 亚伯拉罕遵命出去，还不知往哪里去（光的人影，走在中丘上）
          [8.4, b => {
            const o = { layer: 1, facing: -1, angel: true, glow: 1, from: inst(b) ? 'none' : 'light' };
            add('abraham', Object.assign({ label: '亚伯拉罕', sex: 'm', age: 'elder', x: X.abr0, hair: 'cloth', beard: true, robe: [242, 224, 180], prop: 'staff', pose: 'walk' }, o));
            add('sarah', Object.assign({ label: '撒拉', sex: 'f', age: 'elder', x: Math.min(0.995, X.abr0 + 0.02), hair: 'veil', robe: [238, 214, 184], prop: null, pose: 'walk' }, o));
            walk('abraham', X.abr1, { speed: 0.024 });
            walk('sarah', Math.min(0.995, X.abr1 + 0.028), { speed: 0.024 });
            sfx(b, 'camel', { soft: true, far: true });
          }],
          [10.4, b => { const h = headOf('abraham', 1); nameAt(b, '亚伯拉罕', h[0], h[1] - (PORT ? 30 : 46) * SU(), { size: 30 * SU() }); }],
          // 住在帐棚里，与以撒、雅各一样
          [16.7, b => {
            lv('hbTents', 1, b);
            const o = { layer: 1, facing: -1, angel: true, glow: 1, from: inst(b) ? 'none' : 'light', pose: 'stand' };
            add('isaac', Object.assign({ label: '以撒', sex: 'm', age: 'adult', x: X.tent2 - 0.018, hair: 'cloth', robe: [240, 226, 190] }, o));
            add('jacob', Object.assign({ label: '雅各', sex: 'm', age: 'adult', x: X.tent2 + 0.034, hair: 'cloth', robe: [236, 220, 196] }, o));
            sfx(b, 'chime', { soft: true });
          }],
          // 如同天上的星那样众多，海边的沙那样无数
          [24.5, b => {
            lv('hbSand', 1, b); pose('abraham', 'gaze'); pose('sarah', 'gaze'); face('abraham', 1);
            sfx(b, 'stars'); sfx(b, 'harp', { soft: true });
          }],
          [25.5, () => { each(id => face(id, 1)); pose('p3', 'gaze'); pose('p5', 'gaze'); }],
        ]);
      },
    },

    // ── 10 · 8:9 我拉着他们祖宗的手，领他们出埃及 ─────────────────────
    {
      kind: 'act', utter: '我拉着他们祖宗的手，领他们出埃及', cmd: 'exodus --by-hand 列祖 | cross 红海 --dry', ref: '8:9',
      verse: [
        { text: '不像我拉着他们祖宗的手，领他们出埃及的时候，<br>与他们所立的约。', ref: '希伯来书 8:9', hold: 5.5 },
        { text: '摩西……因着信，就离开埃及，不怕王怒；<br>因为他恒心忍耐，如同看见那不能看见的主。', ref: '希伯来书 11:24–27', hold: 7 },
        { text: '他们因着信，过红海如行干地；<br>埃及人试着要过去，就被吞灭了。', ref: '希伯来书 11:29', hold: 6 },
        { text: '以色列人因着信，围绕耶利哥城七日，城墙就倒塌了。<br>妓女喇合因着信，曾和和平平地接待探子，<br>就不与那些不顺从的人一同灭亡。', ref: '希伯来书 11:30–31', hold: 8.5 },
      ],
      apply(c) {
        layout();
        T(c, [
          [0, b => {
            ['abraham', 'sarah', 'isaac', 'jacob'].forEach(id => rm(id));
            lv('hbTents', 0, b); lv('hbSand', 0, b); lv('hbDimA', 0, b);
            W.goTo(0.13, 14, inst(b));
          }],
          // 摩西与那不能看见的主（光柱先显出：拉着他们祖宗的手领他们的，是那不能看见的主）
          [1, b => { lv('hbMoses', 0.4, b); lv('hbDimB', 1, b); sfx(b, 'wind', { soft: true }); }],
          [6.9, b => { lv('hbMoses', 1, b); sfx(b, 'chime', { soft: true }); }],
          [7.2, b => { const P = galPos('moses'); nameAt(b, '摩西', P.x, P.y + P.R * 1.25, { size: 34 * SU() }); }],
          // 过红海如行干地
          [15.2, b => { lv('hbSea', 1, b); sfx(b, 'wave'); sfx(b, 'wind', { soft: true }); }],
          // 耶利哥：七日围绕，城墙倒塌
          [22.3, b => { lv('hbJericho', 1, b); sfx(b, 'horn'); }],
          [25.2, b => { shake(b, 0.18); sfx(b, 'collapse', { soft: true }); sfx(b, 'shout', { soft: true }); }],
          // 喇合的窗与朱红线绳
          [26.6, b => { lv('hbRahab', 1, b); sfx(b, 'chime'); }],
          [27.4, b => { const P = galPos('jericho'); nameAt(b, '喇合', P.x, P.y + P.R * 1.25, { size: 34 * SU() }); }],
        ]);
      },
    },

    // ── 11 · 12:2 为我们信心创始成终的耶稣 ─────────────────────────
    {
      kind: 'act', utter: '为我们信心创始成终的耶稣', cmd: 'run 路程 --patient --look-to 耶稣', ref: '12:2',
      verse: [
        { text: '我又何必再说呢？若要一一细说，<br>基甸、巴拉、参孙、耶弗他、大卫、撒母耳，<br>和众先知的事，时候就不够了。', ref: '希伯来书 11:32', hold: 7 },
        { text: '我们既有这许多的见证人，如同云彩围着我们，<br>就当放下各样的重担，脱去容易缠累我们的罪，<br>存心忍耐，奔那摆在我们前头的路程，', ref: '希伯来书 12:1', hold: 8 },
        { text: '仰望为我们信心创始成终的耶稣。<br>他因那摆在前面的喜乐，就轻看羞辱，<br>忍受了十字架的苦难，便坐在神宝座的右边。', ref: '希伯来书 12:2', hold: 8 },
      ],
      apply(c) {
        layout();
        T(c, [
          // 天快亮了：一幅幅光的画都升入一圈光明的云
          [0, b => { W.goTo(0.29, 17, inst(b)); lv('hbWit', 1, b); lv('hbDimB', 0.85, b); lv('hbHeaven', 0.55, b); lv('hbOil', 0, b); lv('hbWayA', 0.25, b); lv('hbAnchorA', 0.3, b); sfx(b, 'angel', { soft: true }); }],
          [2, () => { each(id => pose(id, 'stand')); ['priest1', 'priest2', 'hp'].forEach(id => pose(id, 'stand')); }],
          [4, () => each(id => pose(id, 'gaze'))],
          // 放下各样的重担，奔那摆在我们前头的路程
          [8.4, b => {
            S.runner = true;
            lv('hbRace', 1, b);
            hold('p3', 'bundle'); pose('p3', 'stand');
            walk('p3', X.start, { speed: 0.03 }); sink('p3', RACE_V);
            sfx(b, 'chime', { soft: true });
          }],
          [11, b => { hold('p3', null); S.bundle = true; sfx(b, 'build', { soft: true }); }],
          [12, b => {
            run('p3', goalX(), { speed: 0.05 });
            each(id => { if (id !== 'p3') { face(id, 1); pose(id, 'raise'); } });
            sfx(b, 'march', { soft: true });
          }],
          // 仰望：天上的光大大地亮起来
          [17.8, b => { lv('hbHeaven', 1, b); lv('hbThrone', 1, b); flashW(b, 0.14); sfx(b, 'harp'); }],
          [20.4, () => { face('p3', 1); pose('p3', 'gaze'); }],
          [23, b => { pose('p3', 'raise'); sparkleOn(b, 'p3', 24, [255, 240, 206], 0.8); }],
          [25.4, () => each(id => { if (id !== 'p3') pose(id, 'stand'); })],
        ]);
      },
    },

    // ── 12 · 12:26 再一次我不单要震动地，还要震动天 ─────────────────
    {
      kind: 'promise', utter: '再一次我不单要震动地，还要震动天', cmd: 'shake 地 天 --once && keep --unshakable', ref: '12:26',
      verse: [
        { text: '你们原不是来到那能摸的山；<br>此山有火焰、密云、黑暗、暴风、<br>角声与说话的声音。', ref: '希伯来书 12:18–19', hold: 7 },
        { text: '你们乃是来到锡安山，永生神的城邑，<br>就是天上的耶路撒冷。那里有千万的天使，', ref: '希伯来书 12:22', hold: 6.5 },
        { text: '当时他的声音震动了地，但如今他应许说：<br>「再一次我不单要震动地，还要震动天。」', ref: '希伯来书 12:26', hold: 6.5 },
        { text: '所以我们既得了不能震动的国，就当感恩，……<br>因为我们的神乃是烈火。', ref: '希伯来书 12:28–29', hold: 6.5 },
      ],
      apply(c) {
        layout();
        T(c, [
          // 那能摸的山：火焰、密云、黑暗、暴风、角声
          [0, b => {
            lv('hbSinai', 1, b); lv('hbWitA', 0, b); lv('hbHeaven', 0.12, b); lv('hbThrone', 0, b); lv('hbRace', 0, b);
            W.set('storm', 0.8, inst(b)); W.set('gloom', 0.3, inst(b)); W.set('gale', 0.45, inst(b)); W.set('clouds', 0.85, inst(b));
            W.goTo(0.37, 14, inst(b));
            shake(b, 0.3); sfx(b, 'thunder');
            each(id => pose(id, 'kneel')); ['priest1', 'priest2', 'hp'].forEach(id => pose(id, 'kneel'));
          }],
          [1.4, b => bolt(b, sinaiG().px)],
          [2.4, b => sfx(b, 'horn')],
          [3.8, b => { bolt(b, sinaiG().px - 30); shake(b, 0.25); }],
          [5.6, b => { bolt(b, sinaiG().px + 20); sfx(b, 'horn'); sfx(b, 'thunder', { soft: true }); }],
          // 锡安山，天上的耶路撒冷，千万的天使
          [8.4, b => {
            lv('hbSinai', 0, b);
            W.set('storm', 0, inst(b)); W.set('gloom', 0, inst(b)); W.set('gale', 0, inst(b)); W.set('clouds', 0.35, inst(b));
            lv('hbZion', 1, b); lv('hbAngels', 1, b); lv('hbHeaven', 1, b);
            sfx(b, 'angel'); sfx(b, 'sing', { soft: true });
          }],
          [9.6, () => { each(id => pose(id, 'gaze')); ['priest1', 'priest2', 'hp'].forEach(id => pose(id, 'gaze')); }],
          // 再一次震动天地：人手所造的帐幕化作光尘，不被震动的常存
          [16.2, b => {
            S.gone = true; S.bundle = false;
            lv('hbTab', 0, b); lv('hbWay', 0, b); lv('hbLamp', 0, b); lv('hbGlory', 0, b);
            shake(b, 1.1); flashW(b, 0.3); sfx(b, 'quake');
          }],
          [16.8, () => { each(id => pose(id, 'kneel')); ['priest1', 'priest2', 'hp'].forEach(id => pose(id, 'kneel')); }],
          [17.8, b => shake(b, 0.8)],
          [19.4, b => shake(b, 0.55)],
          // 我们的神乃是烈火
          [24, b => {
            lv('hbFire', 1, b);
            each(id => pose(id, 'raise'));
            sfx(b, 'fire', { soft: true }); sfx(b, 'harp');
          }],
          [28.4, b => lv('hbFire', 0.22, b)],
        ]);
      },
    },

    // ── 13 · 13:5 我总不撇下你，也不丢弃你 ─────────────────────────
    {
      kind: 'promise', utter: '我总不撇下你，也不丢弃你', cmd: 'while true; do stay; done  # 总不撇下', ref: '13:5',
      verse: [
        { text: '你们务要常存弟兄相爱的心。<br>不可忘记用爱心接待客旅；<br>因为曾有接待客旅的，不知不觉就接待了天使。', ref: '希伯来书 13:1–2', hold: 7.5 },
        { text: '你们存心不可贪爱钱财，要以自己所有的为足；<br>因为主曾说：「我总不撇下你，也不丢弃你。」', ref: '希伯来书 13:5', hold: 6.5 },
        { text: '耶稣基督昨日、今日、一直到永远，是一样的。', ref: '希伯来书 13:8', hold: 6 },
        { text: '愿恩惠常与你们众人同在。阿们！', ref: '希伯来书 13:25', hold: 5.5 },
      ],
      apply(c) {
        layout();
        T(c, [
          // 黄昏：摆上桌子，众人围坐
          [0, b => {
            W.goTo(0.765, 8, inst(b)); lv('hbTable', 1, b); lv('hbFire', 0, b);
            each(id => { walk(id, seatX(id), { speed: 0.03, pose: 'sit' }); sink(id, SEATS[id][1]); });
            ['priest1', 'priest2', 'hp'].forEach(id => { walk(id, seatX(id), { speed: 0.03, pose: 'sit' }); sink(id, SEATS[id][1]); });
            sfx(b, 'crowd', { soft: true });
          }],
          // 一位客旅从远处来
          [1.4, b => {
            add('guest', { label: '客旅', sex: 'm', age: 'adult', x: PX(0.95), layer: 2, v: 0.14, facing: -1, robe: ROBE.guest, accent: [200, 186, 160], prop: 'staff', glow: 0.2, pose: 'walk' });
            walk('guest', seatX('guest') + 0.02, { speed: 0.045 });
          }],
          [6.4, () => { face('p4', 1); pose('p4', 'stand'); face('p5', 1); }],
          [8.4, b => { walk('guest', seatX('guest'), { speed: 0.02, pose: 'sit' }); pose('p4', 'sit'); lv('hbGuest', 1, b); }],
          // 我总不撇下你：天上的光落在他们身上
          [9, b => { lv('hbBless', 1, b); allGlow(0.46); ['priest1', 'priest2', 'hp'].forEach(id => glow(id, 0.42)); sfx(b, 'harp'); }],
          // 昨日、今日、一直到永远：夜过去，天又亮了；天上的光不变
          [16.8, b => { W.goTo(0.3, 9, inst(b)); lv('hbBless', 0.55, b); sfx(b, 'bell', { soft: true }); }],
          // 不知不觉接待了天使：客旅站起来，他的光升上去
          [19.4, b => { pose('guest', 'stand'); glow('guest', 1); lv('hbGuest', 1.6, b); sfx(b, 'angel', { soft: true }); }],
          [20.6, b => { const f = fig('guest'); fly('guest', f ? f.nx : seatX('guest'), PORT ? 0.46 : 0.36, { dur: 3.2, pose: 'raise' }); }],
          [23.6, b => { sparkleOn(b, 'guest', 30, [255, 244, 220], 0.5); rm('guest'); lv('hbGuest', 0, b); }],
          // 愿恩惠常与你们众人同在
          [24.4, b => {
            lv('hbBless', 1, b);
            each(id => { pose(id, 'raise'); face(id, 1); }); ['priest1', 'priest2', 'hp'].forEach(id => pose(id, 'raise'));
            W.setPop('bird', 30, W.w * 0.6, W.h * 0.35, inst(b));
            if (!inst(b)) {
              const R = Math.hypot(W.w, W.h);
              ringAt(b, X.table * W.w, fY(X.table, TABLE_V) - 30 * LS(2), R, [255, 238, 196], 5.5, 2);
              for (const [xf, yf] of [[0.2, 0.8], [0.5, 0.75], [0.8, 0.86], [0.92, 0.72], [0.62, 0.9]]) bless(xf * W.w, yf * W.h, 0.3 * W.w);
            }
            sfx(b, 'harp'); sfx(b, 'bird', { soft: true });
          }],
          [28.4, () => { each(id => pose(id, 'stand')); ['priest1', 'priest2', 'hp'].forEach(id => pose(id, 'stand')); }],
        ]);
      },
    },
  ];

  // ════════════════════════════════════════════════════════════
  //  幕
  // ════════════════════════════════════════════════════════════
  GS.book.act({
    id: ACT, book: '希伯来书', books: [58], title: '信心', sub: '希伯来书 1 — 13', tint: [255, 226, 176], music: 'abraham',
    outro: 24,
    intro: [
      { text: '神既在古时藉着众先知多次多方地晓谕列祖，<br>就在这末世藉着他儿子晓谕我们；<br>又早已立他为承受万有的，也曾藉着他创造诸世界。', ref: '希伯来书 1:1–2', hold: 8.5 },
    ],
    setup, stages: STAGES, scene: SCENE,
    // 书成之后，按住本幕的人与物，显出它的经文
    behold: {
      '耶稣': { text: '耶稣基督昨日、今日、一直到永远，是一样的。', ref: '希伯来书 13:8' },
      '天使': { text: '天使岂不都是服役的灵、奉差遣为那将要承受救恩的人效力吗？', ref: '希伯来书 1:14' },
      '弟兄': { text: '你们务要常存弟兄相爱的心。', ref: '希伯来书 13:1' },
      '圣徒': { text: '请你们问引导你们的诸位和众圣徒安。', ref: '希伯来书 13:24' },
      '儿女': { text: '看哪，我与神所给我的儿女。', ref: '希伯来书 2:13' },
      '祭司': { text: '凡祭司天天站着事奉神，屡次献上一样的祭物，这祭物永不能除罪。', ref: '希伯来书 10:11' },
      '大祭司': { text: '凡从人间挑选的大祭司，是奉派替人办理属神的事，为要献上礼物和赎罪祭。', ref: '希伯来书 5:1' },
      '亚伯拉罕': { text: '亚伯拉罕因着信，蒙召的时候就遵命出去，往将来要得为业的地方去；出去的时候，还不知往哪里去。', ref: '希伯来书 11:8' },
      '撒拉': { text: '因着信，连撒拉自己，虽然过了生育的岁数，还能怀孕，因她以为那应许她的是可信的。', ref: '希伯来书 11:11' },
      '以撒': { text: '以撒因着信，就指着将来的事给雅各、以扫祝福。', ref: '希伯来书 11:20' },
      '雅各': { text: '雅各因着信，临死的时候，给约瑟的两个儿子各自祝福，扶着杖头敬拜神。', ref: '希伯来书 11:21' },
      '客旅': { text: '不可忘记用爱心接待客旅；因为曾有接待客旅的，不知不觉就接待了天使。', ref: '希伯来书 13:2' },
      '天上的圣所': { text: '在圣所，就是真帐幕里，作执事；这帐幕是主所支的，不是人所支的。', ref: '希伯来书 8:2' },
      '施恩的宝座': { text: '所以，我们只管坦然无惧地来到施恩的宝座前，为要得怜恤，蒙恩惠，作随时的帮助。', ref: '希伯来书 4:16' },
      '锚': { text: '我们有这指望，如同灵魂的锚，又坚固又牢靠，且通入幔内。', ref: '希伯来书 6:19' },
      '船': { text: '所以，我们当越发郑重所听见的道理，恐怕我们随流失去。', ref: '希伯来书 2:1' },
      '帐幕': { text: '因为有预备的帐幕，头一层叫作圣所，里面有灯台、桌子，和陈设饼。', ref: '希伯来书 9:2' },
      '幔子': { text: '第二幔子后又有一层帐幕，叫作至圣所，', ref: '希伯来书 9:3' },
      '约柜': { text: '有金香炉，有包金的约柜，柜里有盛吗哪的金罐和亚伦发过芽的杖，并两块约版；', ref: '希伯来书 9:4' },
      '祭坛': { text: '我们有一祭坛，上面的祭物是那些在帐幕中供职的人不可同吃的。', ref: '希伯来书 13:10' },
      '又新又活的路': { text: '是藉着他给我们开了一条又新又活的路，从幔子经过，这幔子就是他的身体。', ref: '希伯来书 10:20' },
      '亚伯': { text: '亚伯因着信，献祭与神，比该隐所献的更美，因此便得了称义的见证，就是神指他礼物作的见证。', ref: '希伯来书 11:4' },
      '以诺': { text: '以诺因着信，被接去，不至于见死，人也找不着他，因为神已经把他接去了；', ref: '希伯来书 11:5' },
      '挪亚': { text: '挪亚因着信，既蒙神指示他未见的事，动了敬畏的心，预备了一只方舟，使他全家得救。', ref: '希伯来书 11:7' },
      '摩西': { text: '他因着信，就离开埃及，不怕王怒；因为他恒心忍耐，如同看见那不能看见的主。', ref: '希伯来书 11:27' },
      '红海': { text: '他们因着信，过红海如行干地；埃及人试着要过去，就被吞灭了。', ref: '希伯来书 11:29' },
      '耶利哥': { text: '以色列人因着信，围绕耶利哥城七日，城墙就倒塌了。', ref: '希伯来书 11:30' },
      '喇合': { text: '妓女喇合因着信，曾和和平平地接待探子，就不与那些不顺从的人一同灭亡。', ref: '希伯来书 11:31' },
      '帐棚': { text: '他因着信，就在所应许之地作客，好像在异地居住帐棚，与那同蒙一个应许的以撒、雅各一样。', ref: '希伯来书 11:9' },
      '天上的星': { text: '所以从一个仿佛已死的人就生出子孙，如同天上的星那样众多，海边的沙那样无数。', ref: '希伯来书 11:12' },
      '海边的沙': { text: '所以从一个仿佛已死的人就生出子孙，如同天上的星那样众多，海边的沙那样无数。', ref: '希伯来书 11:12' },
      '见证人': { text: '我们既有这许多的见证人，如同云彩围着我们，', ref: '希伯来书 12:1' },
      '路程': { text: '所以，你们要把下垂的手、发酸的腿挺起来；也要为自己的脚，把道路修直了，', ref: '希伯来书 12:12–13' },
      '重担': { text: '就当放下各样的重担，脱去容易缠累我们的罪，存心忍耐，奔那摆在我们前头的路程，', ref: '希伯来书 12:1' },
      '那能摸的山': { text: '你们原不是来到那能摸的山；此山有火焰、密云、黑暗、暴风、角声与说话的声音。', ref: '希伯来书 12:18–19' },
      '锡安山': { text: '你们乃是来到锡安山，永生神的城邑，就是天上的耶路撒冷。', ref: '希伯来书 12:22' },
      '千万的天使': { text: '那里有千万的天使，有名录在天上诸长子之会所共聚的总会，', ref: '希伯来书 12:22–23' },
      '桌子': { text: '只是不可忘记行善和捐输的事，因为这样的祭是神所喜悦的。', ref: '希伯来书 13:16' },
    },
  });
})(window.GS);
